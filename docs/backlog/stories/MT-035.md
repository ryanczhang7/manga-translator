---
id: MT-035
title: The detect stage writes a page's regions in reading order
slug: the-detect-stage-writes-a-page-s-regions
epic: EPIC-03
type: feature
status: done
phase: DONE
branch: story/MT-035-the-detect-stage-writes-a-page-s-regions
depends_on: [MT-006, MT-007, MT-008, MT-009]
required_gates: [integration]
---

## Context

**This story closes EPIC-03's assembly gap, and it exists because MT-009 PO-4
named it.** The epic's done-when says *"Given a page scan, the **pipeline**
produces regions with masks ... in right-to-left top-to-bottom reading order."*
MT-007 shipped `regions_from_detection`, MT-008 shipped `merge_columns`, MT-009
shipped `order_regions` — three correct, well-tested pure functions — and a
caller sweep in MT-009 found **zero source callers** of
`store.project.write_regions`. Nothing in `src/mangatl/pipeline/` calls any of
them. The epic has every piece and no assembly; MT-006's `run_chapter` walks
pages through a `PassThroughStage` and nothing else. This story builds the stage
that makes the epic's sentence true.

Two documents bound it. `architecture.md` §2 *The adapter split* says the
**logic** of a detection stage is a pure function over arrays taking an
*injected session object*, tested against a fake session in `tests/core` which a
required gate reads, with the real model getting a smoke test in
`tests/integration` — *"Without this split, every required gate could pass over
stories whose artifact nothing exercised."* `pipeline/stage.py` says a stage is a
`Protocol` rather than a base class, and that **`Project.transaction()` is not
reentrant** (MT-005 PO-5), so the stage owns its transaction, one per page per
stage (§6).

**Which required gates would fail if this story's artifact broke: `unit` and
`coverage`.** Both are `required` in `project.conf` and both carry
`covers | src/mangatl/**`, which reads `src/mangatl/pipeline/**` and
`src/mangatl/detect/**`. AC-1 to AC-9 are all asserted in `tests/core` against
fakes and really execute on CI — measured at 34 s and 24 s on the MT-009 PR.
`coverage-core`'s 100 % bar covers only `domain`, `typeset` and `bench`, so it
does **not** read either module this story adds; that is a fact about the bar,
not a gap, because `unit` and `coverage` do.

`required_gates: [integration]` follows MT-007 PO-4 and MT-008 PO-3 exactly:
this story adds a file under `src/mangatl/detect/**`, which `covers | integration
| src/mangatl/detect/**` reads, and the line escalates that gate to required **on
the machine running the loop**, where it executes 11 real tests on the GPU. On CI
`STORY_REQUIRES` is empty and it stays optional, where it skips in 1.5 s for want
of a CUDA provider and the gitignored weights. **Only AC-10 depends on it**, and
AC-10 is deliberately not a central claim: the epic's ordering sentence is AC-1,
which runs in `unit`. MT-009 PO-2 is the standing warning here and it is honoured
rather than argued with.

## Acceptance criteria

<!-- AC-1 to AC-6 are the stage; AC-7 to AC-9 are the detect adapter; AC-10 is
     the real detector and runs only under `integration`. -->

- **AC-1** — Given a page and a detector that returns regions in an order that is
  **not** reading order, when the detect stage runs that page, then
  `project.read_regions(ordinal)` returns exactly those regions in
  `domain.reading_order.order_regions` order — `reading_index` 0 is the
  top-right region, `reading_index` n-1 the bottom-left.
  *Control:* the fixture's detector order must differ from the reading order, and
  the test asserts that difference on the fixture itself — otherwise a stage that
  wrote the detector's order straight through would satisfy AC-1 by accident. A
  stage that omits the ordering step must fail this criterion; that mutation
  cannot run in RED and is deferred below.
- **AC-2** — Given a page, when the stage runs it, then the injected detector is
  called **exactly once**, with the exact bytes of
  `project.chapter.source_dir / page.filename`, and the contents of `source_dir`
  are unchanged afterwards — same filenames, same bytes. `architecture.md` §5:
  the user's scans are read-only to this app, always.
- **AC-3** — Given a page whose regions are already in the store, when the
  stage's `is_done` is consulted, then it is `True`; given a page with no stored
  regions, then it is `False`. And given a single-stage `run_chapter` over a page
  that already has regions, then `PageSkipped` is emitted for that page and the
  injected detector is **not** called.
- **AC-4** — Given a page whose regions were written by an earlier run, when the
  stage runs again and the detector now returns **fewer** regions, then the store
  holds exactly the new set, in reading order, with no rows surviving from the
  previous run — `read_regions` returns exactly the new regions and nothing else.
- **AC-5** — Given a detector that returns no regions at all, when the stage runs,
  then the page's stored regions are empty, no exception is raised, and `is_done`
  for that page subsequently reports **`False`**.
  *This is a decision, not an oversight,* and it is written as a criterion so it
  is falsifiable: a genuinely textless page is re-detected on every run. See
  `## Contract` C-6 for why that is the honest answer and what the alternative
  would cost.
- **AC-6** — Given a detector that raises, when the stage runs a page that
  already has stored regions, then the exception propagates out of `run` (which
  is what makes `run_chapter` emit `RunAborted` naming the page) and the page's
  stored regions are **unchanged** — the stage writes once, after it has every
  region, and never clears first.
- **AC-7** — Given a fake `DetectorSession` and a page's encoded image bytes, when
  `detect.page.detect_page_regions` runs, then the session's `run` is called
  **exactly once** and its argument is the letterboxed canvas —
  `MODEL_INPUT_SIDE` square, `float32` — and not the decoded page at its own
  dimensions.
  *Control:* a chain that handed the decoded page straight to the session passes
  an array of the page's own shape, which this criterion must reject; the test
  asserts the shape, so the control is the assertion.
- **AC-8** — Given a fake session whose probability map places two columns of one
  utterance inside a single detected box, when `detect_page_regions` runs, then it
  returns **one** region whose `merged_from` records both inputs, while
  `regions_from_detection` on the *same* session output returns **two** — the
  MT-008 merge is in the chain.
  *Control:* the two-region baseline is the control and is asserted in the same
  test, on the same session output. Without it the criterion is satisfied by a
  synthetic page the detector only ever found one region on, which would make it
  measure nothing.
- **AC-9** — Given a fake session and a page of size `(W, H)` with `W != H`, when
  `detect_page_regions` runs, then every returned region's mask decodes to
  exactly `(H, W)` and every polygon vertex lies inside the page rectangle — the
  probability map was carried back through the letterbox transform into page
  coordinates.
  *Control:* a chain that skipped `probability_to_page` yields masks at model
  scale (`MODEL_INPUT_SIDE` square), which this criterion rejects. `W != H` is
  load-bearing: on a square page a transposed or unscaled mask has the right
  shape and the criterion goes blind.
- **AC-10** — *(`integration`, GPU only)* Given the real detector session and
  `spikes/MT-002/pages/011.jpg` intaken as a one-page project, when the detect
  stage runs that page, then the store holds at least one region and
  `read_regions(ordinal)` equals `sort_regions` of the same region set — the real
  chain composes end to end and reaches the store in reading order. Skips cleanly
  on a checkout without the gitignored weights and pages, as
  `tests/integration/test_column_merge_pages.py` already does.
  **No criterion above depends on this one.** It is real-detector evidence, and on
  CI it does not run.

## Contract

*Owned by the Lead PO before RED. **RED may amend any block in place, with a
reason**, and GREEN builds what the amended block says.*

### C-1 — Injection is forced by the `lint` gate. It is not a style preference.

This is the decision the story turns on, and it was settled by running the tool
rather than by reading its configuration.

`pyproject.toml`'s contract *"Only detect, ocr and clean import onnxruntime"* is
a `forbidden` contract listing `mangatl.pipeline` among its `source_modules`, and
it does **not** set `allow_indirect_imports`. Import-linter therefore reports
transitive chains. Every module in `mangatl.detect` reaches `onnxruntime`:
`columns -> postprocess -> session -> onnxruntime`. Probed with a throwaway
config against the real tree, touching no project file:

```
$ uv run lint-imports --config <scratch>/indirect.ini
Analyzed 53 files, 94 dependencies.
does a forbidden contract catch an INDIRECT chain BROKEN
Contracts: 0 kept, 1 broken.

mangatl.detect.columns is not allowed to import onnxruntime:

-   mangatl.detect.columns -> mangatl.detect.postprocess (l.60)
    mangatl.detect.postprocess -> mangatl.detect.session (l.56)
    mangatl.detect.session -> onnxruntime (l.32)
```

So **`mangatl.pipeline` may not import `mangatl.detect` at all** — not
`columns`, not `postprocess`, not even the `DetectorSession` protocol in
`session`. A stage that constructed or merely *named* its detector would break
`lint`, a required gate, on the first run.

Two consequences, both of which the design below obeys:

1. The stage takes its detector **injected, behind a type it can name without
   importing `detect`** — a plain `Callable` over `bytes` and `RawRegion`, both
   stdlib and `domain`.
2. The composition of the MT-007/MT-008 functions ships **inside `mangatl.detect`**,
   because that is the only package allowed to hold it.

This lands where `architecture.md` §2 was already pointing, and it also answers
MT-009 PO-2's warning in the right direction: every criterion except AC-10 is
driven by fakes in `tests/core`, under `unit` and `coverage`, which really run on
CI. Tests are not part of the `mangatl` root package, so `tests/integration` may
import both sides and wire the real thing; that is AC-10.

**Independently reproduced by the orchestrator before dispatch, on different
inputs and without reusing the probe above** — `rules.md`: an instruction that
names a *mechanism* is a claim, and this one is the hinge of the whole design. A
synthetic three-package tree in the scratchpad, nothing of this project in it,
under a `forbidden` contract with `allow_indirect_imports` unset:

```
$ lint-imports --config <scratch>/probe.ini
Analyzed 7 files, 3 dependencies.
orch must not reach onnxruntime even indirectly BROKEN
Contracts: 0 kept, 1 broken.

synth.orch is not allowed to import onnxruntime:

-   synth.orch.stage -> synth.leaf.mid (l.1)
    synth.leaf.mid -> synth.leaf.session (l.1)
    synth.leaf.session -> onnxruntime (l.1)
```

Three hops, caught. Then the two facts that make it bite here, read off this
tree rather than assumed: `detect/session.py:32` imports `onnxruntime` at module
level, and every other `detect` module reaches it —
`columns.py:60 -> postprocess.py:56 -> session.py:32`. Contract 5 lists
`mangatl.pipeline` among its `source_modules` and sets no
`allow_indirect_imports`.

So C-1 holds for **every** module in `mangatl.detect`, the `DetectorSession`
protocol included — because that protocol lives in the very file that does the
import. `pipeline/` imports nothing from `detect` today, so there is no existing
violation to repair, only one to avoid creating.

### C-2 — `src/mangatl/detect/page.py` — new module

```python
__all__ = ["detect_page_regions"]

def detect_page_regions(
    session: DetectorSession,
    image_bytes: bytes,
) -> list[RawRegion]: ...
```

The whole detect chain for one page, composed from what MT-007 and MT-008 already
shipped, in the order `postprocess`'s module docstring fixes (MT-007 C-4) — read
those steps out, do not re-derive them:

1. decode `image_bytes` to an RGB `uint8` array (`PIL`; `detect` may import it);
2. `letterbox(page)` → `(canvas, transform)`;
3. `session.run(canvas)` → `RawDetectorOutput`;
4. `decode_boxes(raw.blk, transform, (width, height))`;
5. `probability_to_page(raw.seg, transform, (width, height))`;
6. `regions_from_detection(prob, boxes, (width, height))`;
7. `merge_columns(regions, integer_boxes(boxes, width, height))`.

`page_size` is `(width, height)` while `prob.shape` is `(height, width)`; the
mismatch raises rather than transposing a page silently (MT-007 A-6). The
reference for this composition is `tests/integration/test_column_merge_pages.py`'s
`detected` fixture, which performs exactly these seven steps inline today —
**this story moves that composition into shipped source**, which is the point.

**AMENDMENT R-2, made in RED 2026-09-16: step 1 says `.convert("RGB")`
explicitly.** "Decode to an RGB `uint8` array" is not what `Image.open` gives
you: a greyscale scan decodes to mode `L`, `np.asarray` of it is two-dimensional,
and `letterbox`'s `canvas[y0:y1, x0:x1] = resized` then fails to broadcast a
2-D resize into a `(side, side, 3)` canvas. `011.jpg` happens to decode as RGB,
so the corpus would not have caught it and a user's greyscale scan would. The
step is therefore
`np.asarray(Image.open(BytesIO(image_bytes)).convert("RGB"), dtype=np.uint8)` -
which is exactly what `test_column_merge_pages.py`'s `detected` fixture already
does, so this amendment only writes down what the reference composition was
doing. `(width, height)` for steps 4 to 6 comes from `page.shape[:2]`, which is
`(height, width)`; C-2 uses the reversed order throughout and the tests pin it on
two mutually transposed page sizes.

**Independently reproduced by the orchestrator before accepting the amendment**,
on inputs of my own choosing (a 137x91 greyscale JPEG, nothing the subagent
used) and my own probe code, against the *shipped* `letterbox`:

```
decoded mode: L
bare shape: (91, 137)
R-2 REPRODUCED: ValueError: could not broadcast input array from shape (680,1024) into shape (680,1024,3)
convert('RGB') shape: (91, 137, 3)
with convert: canvas (1, 3, 1024, 1024) float32 -> OK
```

A greyscale source really does decode to a 2-D array, `letterbox` really does
refuse it, and `.convert("RGB")` really does fix it. Accepted — and note the
harm is not a crash in a corner: it is every greyscale scan, which for manga is
not the corner case.


**It does not order.** `architecture.md` §2 puts reading order outside `detect`
explicitly (*"`detect` ... explicitly does not own: reading order"*), and
`merge_columns`' own docstring says so. The return is MT-008's `(y0, x0)` merge
order. Ordering is the stage's, C-3.

Production wiring, for whoever assembles it later:
`partial(detect_page_regions, load_detector(model_path, providers))`. Nothing in
`src` performs that wiring in this story — see `## Out of scope`, and the
escalation recorded there.

### C-3 — `src/mangatl/pipeline/detect_stage.py` — new module

```python
__all__ = ["DetectStage", "PageDetector"]

#: Page image bytes in, regions out, in any order. The narrowest type the stage
#: can name: `bytes` is stdlib and `RawRegion` is `domain`, so naming it costs
#: `pipeline` no import of `detect` (C-1).
PageDetector = Callable[[bytes], Sequence[RawRegion]]

@dataclass                      # NOT frozen - see AMENDMENT R-1 below
class DetectStage:
    detect: PageDetector
    name: str = "detect"

    def run(self, ctx: PageContext) -> None: ...
    def is_done(self, ctx: PageContext) -> bool: ...
```

**AMENDMENT R-1, made in RED 2026-09-16: `frozen=True` is dropped.** The block
above said `@dataclass(frozen=True)`, and a frozen `DetectStage` **is not a
`Stage`** as far as `mypy` is concerned. Measured against the shipped
`pipeline/stage.py` under `mypy --strict`, with `PassThroughStage()` in the same
`list[Stage]` as a control:

```
Protocol member Stage.name expected settable variable, got read-only attribute
    # @dataclass(frozen=True),  name: str = "detect"
Protocol member Stage.name expected instance variable, got class variable
    # @dataclass(frozen=True),  name: ClassVar[str] = "detect"
(no error)
    # @dataclass,               name: str = "detect"
(no error)
    # PassThroughStage          -- the shipped baseline, which validates the probe
```

The fourth bullet below this block was already right about `ClassVar` and right
about why - `Stage` declares `name: str` as an instance attribute - but it
diagnosed only one of the two ways to fail that declaration. `frozen=True` makes
`name` a **read-only** attribute, and a settable protocol member needs a mutable
one; there is no spelling of a frozen dataclass that mypy will accept here, so
the choice is "not frozen" or "not a dataclass". `PassThroughStage` is a plain
mutable class, so dropping `frozen` also matches the only precedent in the file.

**Why this had to be caught now rather than later.** `typecheck` runs `mypy src`,
and nothing in `src` annotates a `DetectStage` as a `Stage` in this story -
production wiring is out of scope. So the gate would have stayed green and the
trap would have surfaced in whichever later story first assembles a real run, as
a change to a class this story shipped. RED pins the one runtime-visible
consequence instead, in
`test_the_stage_name_is_a_settable_instance_attribute_as_the_protocol_requires`:
a frozen dataclass raises `dataclasses.FrozenInstanceError` on assignment, and
that test was watched to fail with exactly that error against C-3 as originally
written.

**Independently reproduced by the orchestrator before accepting the amendment**
(`rules.md`: a claim that the contract itself is wrong is reproduced on different
inputs, without reusing the subagent's code). A probe of my own, with my own
names and no `mangatl` import at all — a `Widget` protocol declaring `label: str`
and four candidate implementations in one `list[Widget]`:

```
$ uv run mypy --strict <scratch>/r1/probe_r1.py
probe_r1.py:40: error: List item 0 has incompatible type "FrozenPlain"; expected "Widget"
probe_r1.py:40: note: Protocol member Widget.label expected settable variable, got read-only attribute
probe_r1.py:40: error: List item 1 has incompatible type "FrozenClassVar"; expected "Widget"
probe_r1.py:40: note: Protocol member Widget.label expected instance variable, got class variable
Found 2 errors in 1 file (checked 1 source file)
```

`UnfrozenPlain` (`@dataclass`, `label: str`) and `PlainClassAttr` (a plain class
with `label = "d"`) produced **no error**. So R-1 holds, both of its failure
modes are real and distinct, and the shape the amendment chose is one of the two
that type-check. Accepted.


*Consequence GREEN should know about:* a non-frozen dataclass with `eq=True` has
`__hash__ = None`, so a `DetectStage` cannot go in a `set` or a `dict` key.
Nothing hashes a stage today - `run_chapter` only iterates its `Sequence` - so
this is recorded rather than solved. If a later story needs it, `@dataclass(
eq=False)` restores identity hashing and still satisfies the protocol; no test
here compares two stages with `==`.

`run` is four steps and nothing else:

1. `image_bytes = (ctx.project.chapter.source_dir / ctx.page.filename).read_bytes()`;
2. `regions = self.detect(image_bytes)`;
3. `ordered = sort_regions(regions)` — `mangatl.domain.reading_order`;
4. `ctx.project.write_regions(ctx.page.ordinal, ordered)`.

`is_done` is `bool(ctx.project.read_regions(ctx.page.ordinal))`.

Four things about that shape are load-bearing:

- **`sort_regions`, not `order_regions`.** Position is the only carrier of
  reading order (MT-009 PO-1): `write_regions` derives `reading_index` from the
  position in the sequence it is handed, and no region carries an index of its
  own. `sort_regions(regions)` is `[regions[i] for i in order_regions(regions)]`.
- **The stage opens no transaction.** `write_regions` opens its own, and
  `Project.transaction()` is deliberately not reentrant (MT-005 PO-5) — a stage
  that wrapped the write would turn it into a `RuntimeError`. This needs no
  separate criterion: AC-1's test raises if it is ever violated.
- **One write, at the end.** `write_regions` is a page-level replace that also
  deletes rows at or past `len(regions)` (MT-007 A-11), so AC-4's "no stragglers"
  is already the store's behaviour and the stage must not add a clearing write of
  its own. That is what AC-6 pins.
- **`name` is a field with a default, not a `ClassVar`, and the dataclass is not
  frozen.** The `Stage` protocol declares `name: str` as an instance attribute; a
  `ClassVar` does not satisfy a protocol member typed that way and `mypy` will
  say so - and so does a frozen field, for the independent reason amendment R-1
  above measures.

`pipeline/detect_stage.py` imports: `mangatl.domain.region`,
`mangatl.domain.reading_order`, `mangatl.pipeline.stage`, and stdlib. **No
`numpy`, no `PIL`, no `mangatl.detect`.** If RED or GREEN finds it wants any of
those, the seam is in the wrong place — stop and say so rather than adding the
import.

### C-4 — no existing export changes signature

Grepped before dispatch, over `src/` and `tests/`:

- `order_regions`, `sort_regions` — unchanged; this story is a new caller.
- `merge_columns`, `regions_from_detection`, `integer_boxes`, `letterbox`,
  `decode_boxes`, `probability_to_page`, `load_detector` — unchanged; new caller
  in `detect/page.py`.
- `Project.write_regions`, `Project.read_regions` — unchanged; new caller.
- `Stage`, `PageContext`, `PassThroughStage`, `run_chapter` — unchanged.

So the caller-list hazard the skill describes does not arise here: nothing
existing is re-shaped, and there is no old signature left standing for a missed
caller to keep compiling against. `PassThroughStage` stays: it is the identity
element the runner's own tests need, not a placeholder this story deletes.

### C-5 — the oracle partition

| Kind | Criteria | Instruction to RED |
|---|---|---|
| **Settled** — read out, do not re-derive | the seven-step chain and its constants (MT-007 C-4 and `postprocess`'s docstring), `COLUMN_MAX_GAP_PX` / `COLUMN_MIN_OVERLAP` (MT-008), `BAND_OVERLAP_FRACTION` and the band sweep (MT-009), `write_regions`' page-level replace and positional `reading_index` (MT-005 / MT-007 A-11), `Stage` / `PageContext` (MT-006) | These are decided. Cite them; do not tune them, and do not re-measure a threshold this story merely passes through. |
| **Mechanical** — pin exactly | AC-2, AC-3, AC-4, AC-6, AC-7, AC-9 | Module paths, the `PageDetector` alias, `DetectStage.name == "detect"`, call counts, the exact bytes handed to the detector, the `is_done` truth table, the `PageSkipped` event. Precision beats invention; leave nothing open-ended. |
| **Oracle-free** — invent the metric, demand a hard control | AC-8, and AC-10 | AC-8: design a synthetic probability map and box that genuinely merges under MT-008's rule, and assert the two-region baseline from the *same* session output in the same test. AC-10: there is no settled region count for `011.jpg` — MT-008 measured 12 before merging on one run, which is evidence and not an oracle, so assert `>= 1` and order equality, never a count. |

### C-6 — why `is_done` is "has regions", and what that costs

`stage.py` says done-ness is per stage per page, and the only thing the detect
stage writes is regions, so "has regions" is the only honest thing to read back.
The consequence, pinned by AC-5: a page on which the detector genuinely finds
nothing is re-detected on every run. That is accepted for v1 because re-detecting
is idempotent and costs one GPU inference — no API spend, no data loss — whereas
the alternatives each buy a new problem: a per-stage completion marker is a
schema change this story has no mandate for, and `page.status` is per page and
already owned by the runner (`architecture.md` §5, `runner._mark_done`). If a
later story adds per-stage markers, AC-5 is the criterion it amends.

### C-7 — test fixtures RED can build on

- `tests/core/conftest.py` has `one_bit_png(width, height, rects)` (MT-008) and
  `png_bytes(...)` (MT-005), which between them build a page file and region
  masks without a dependency on the real detector.
- `tests/core/test_project.py` and `test_region_store.py` show how a `Project` is
  created on `tmp_path` and read back.
- `tests/core/test_pipeline.py` shows how `run_chapter` is driven with fake
  stages and a collecting `emit`.
- `tests/core/test_detect_postprocess.py` shows synthetic `prob`/`boxes` arrays —
  the raw material for the fake session in AC-7 to AC-9.
- `fixtures/detect/README.md` pins the half-open rectangle convention the mask
  fixtures use.

**The page file must exist on disk** for AC-2: `read_chapter` decodes every page
at intake, so the project's `source_dir` has to hold a real, decodable image.

## Deferred verifications

Three mutations. Each names a condition that must go red, and none of them can
run in RED: in RED neither module exists, so there is nothing to mutate. **Owner:
GATES** for all three — source is writable there, and if the story bounces back
to RED mid-cycle the corrected assertions are earned by the same experiment.
Each is made with `bash scripts/mutate.sh FILE 'EXPR' -- CMD`, which restores the
file and verifies the restore; `sed -i` is working around the lock (law 5).

**DV-1 — the ordering step, dropped.** With `sort_regions(regions)` in
`pipeline/detect_stage.py` replaced by `regions`, **AC-1's test must fail**, and
the expected shape of the failure is a `read_regions` sequence equal to the
detector's order. If it does not fail, the AC-1 fixture's detector order is not
actually different from its reading order and the criterion is vacuous.
*Why RED cannot run it:* there is no stage to mutate.
**Owner: GATES.** **Result — run at GATES 2026-09-16, and AC-1 failed as
required.** `ordered = sort_regions(regions)` replaced by `ordered = regions`:

```
$ bash scripts/mutate.sh src/mangatl/pipeline/detect_stage.py \
    's|ordered = sort_regions(regions)|ordered = regions|' \
    -- uv run pytest -q tests/core/test_detect_stage.py tests/core/test_detect_page.py

>       assert list(fixture.project.read_regions(_TARGET_ORDINAL)) == fixture.reading_order
E       assert [RawRegion(po...rged_from=())] == [RawRegion(po...rged_from=())]
E         At index 0 diff: RawRegion(polygon=((2, 20), (12, 20), (12, 28), (2, 28), (2, 20)), ... confidence=0.25 ...)
E                       != RawRegion(polygon=((28, 2), (38, 2), (38, 8), (28, 8), (28, 2)), ... confidence=0.5 ...)

FAILED tests/core/test_detect_stage.py::test_a_pages_regions_are_stored_in_reading_order_whatever_order_they_arrived_in
FAILED tests/core/test_detect_stage.py::test_a_rerun_that_finds_fewer_regions_leaves_none_of_the_previous_run_behind
FAILED tests/core/test_detect_stage.py::test_a_detector_that_raises_leaves_the_pages_stored_regions_untouched
3 failed, 20 passed in 1.78s

=== mutate: command exited 1; restored (verified byte-for-byte against ...detect_stage.py.20260917T000647Z.744546.bak) ===
  99:         ordered = sort_regions(regions)
```

**The failure has the predicted shape**: `read_regions` came back in the
detector's order — index 0 is the `confidence=0.25` bottom-left region where
reading order wants the `confidence=0.5` top-right one. So the AC-1 fixture's
detector order is genuinely different from its reading order and the criterion
is **not vacuous**. Three tests caught it, matching RED's M1 prediction (AC-1,
AC-4, AC-6) exactly. File restored and verified byte-for-byte.

**DV-2 — the merge step, dropped.** With `merge_columns(...)` in
`detect/page.py` replaced by its first argument, **AC-8 must fail**, reporting
two regions where it expects one. This is the control on the *chain*, distinct
from MT-008's control on the *rule*: MT-008 proved `merge_columns` merges;
nothing yet proves this story calls it.
*Why RED cannot run it:* there is no adapter to mutate.
**Owner: GATES.** **Result — run at GATES 2026-09-16, and AC-8 failed on the
criterion with its control intact.** `merge_columns(...)` replaced by its first
argument:

```
$ bash scripts/mutate.sh src/mangatl/detect/page.py \
    's|return merge_columns(regions, integer_boxes(boxes, width, height))|return regions|' \
    -- uv run pytest -q tests/core/test_detect_stage.py tests/core/test_detect_page.py

FAILED tests/core/test_detect_page.py::test_two_columns_of_one_utterance_in_one_box_come_back_as_a_single_region[(320, 240)]
FAILED tests/core/test_detect_page.py::test_two_columns_of_one_utterance_in_one_box_come_back_as_a_single_region[(240, 320)]
FAILED tests/core/test_detect_page.py::test_every_region_of_a_non_square_page_is_masked_and_drawn_in_page_pixels[(320, 240)]
FAILED tests/core/test_detect_page.py::test_every_region_of_a_non_square_page_is_masked_and_drawn_in_page_pixels[(240, 320)]
4 failed, 19 passed in 1.41s
```

Re-run against AC-8's test alone, to read out **which** assertion went red —
the thing that distinguishes a criterion failing from its control failing:

```
            f"the control failed, not the criterion: `regions_from_detection` found"
>       assert len(merged) == 1, (
E       AssertionError: 2 regions before merging and 2 after; the chain does not call merge_columns
E       assert 2 == 1

=== mutate: ... restored (verified byte-for-byte) ===
  93:     return merge_columns(regions, integer_boxes(boxes, width, height))
```

**It failed on the criterion, not on the baseline.** The control assertion
above it — *"the control failed, not the criterion"* — passed, so
`regions_from_detection` still found its two regions and it is the *merge* that
went missing. That is the whole distinction DV-2 exists to draw: MT-008 proved
`merge_columns` merges the rule; this proves **this story calls it**. Reported
`2 == 1` on both page sizes. File restored both runs.

**DV-3 — a wrong value, not a missing step.** With
`ctx.project.write_regions(ctx.page.ordinal, ordered)` mutated to
`write_regions(0, ordered)` — one token, a plausible typo, nothing omitted —
**AC-1's test must fail**. It only can if the AC-1 fixture uses a page whose
ordinal is **not 0**, which is therefore a requirement on RED and not an
accident: a suite that catches an omission can be blind to a corruption, and a
single-page project at ordinal 0 makes this mutation invisible.
*Why RED cannot run it:* there is no stage to mutate.
**Owner: GATES.** **Result — run at GATES 2026-09-16, and AC-1 failed as
required.** `write_regions(ctx.page.ordinal, ordered)` mutated to
`write_regions(0, ordered)` — one token, a plausible typo, nothing omitted:

```
$ bash scripts/mutate.sh src/mangatl/pipeline/detect_stage.py \
    's|write_regions(ctx.page.ordinal, ordered)|write_regions(0, ordered)|' \
    -- uv run pytest -q tests/core/test_detect_stage.py tests/core/test_detect_page.py

E       AssertionError: the fixture must already hold a previous run's regions
E       assert 0 == 3
E        +  where 0 = len(())

FAILED ...::test_a_pages_regions_are_stored_in_reading_order_whatever_order_they_arrived_in
FAILED ...::test_is_done_is_true_for_a_page_with_stored_regions_and_false_without
FAILED ...::test_a_run_over_pages_that_already_have_regions_skips_them_without_detecting
FAILED ...::test_a_rerun_that_finds_fewer_regions_leaves_none_of_the_previous_run_behind
FAILED ...::test_a_page_the_detector_finds_no_text_on_stores_nothing_and_stays_not_done[previously-detected]
FAILED ...::test_a_detector_that_raises_leaves_the_pages_stored_regions_untouched
6 failed, 17 passed in 1.38s

=== mutate: command exited 1; restored (verified byte-for-byte against ...detect_stage.py.20260917T000725Z.747756.bak) ===
  100:         ctx.project.write_regions(ctx.page.ordinal, ordered)
```

**The requirement this placed on RED was honoured and is what made the mutation
visible**: `_TARGET_ORDINAL = 1`, not 0 — the test file says so at line 73 with
this DV cited as the reason. Six tests caught a corruption that omits nothing,
matching RED's M3 prediction exactly. The story's claim that "a suite that
catches an omission can be blind to a corruption" is now discharged in the
direction that matters: this one is not blind. File restored and verified.

## Model guidance

**Record the resolved model of every dispatch here, by name, never the word
"default"** (`rules.md`). `.claude/agents/` declares `opus` for every role, but a
session setting or an explicit override can still win and the orchestrator cannot
see which did — so each subagent is asked to say whether it was overridden, and
the answer is written down.

- RED — resolved model: **Opus 5** (`claude-opus-5`), self-reported by the
  subagent at the end of the phase. `.claude/agents/test-developer.md` declares
  `model: opus`; the subagent reports **no override** was applied, so the
  declaration and the resolution agree.
- GREEN — resolved model: **Opus 5** (`claude-opus-5`), self-reported by the
  subagent at the end of the phase. `.claude/agents/feature-developer.md`
  declares `model: opus`; the subagent reports **no override** was applied, so
  the declaration and the resolution agree.
- GATES — **no subagent was dispatched.** Every required gate passed on the
  first full run, so there was nothing to send a feature-developer to fix, and
  an agent given no work finds some. The orchestrator ran DV-1 to DV-3 and
  `gates.sh` itself, as **Opus 5** (`claude-opus-5`) — the session model, not a
  dispatch resolution. Recorded rather than left blank so the row is not read
  later as an unrecorded dispatch.

**No model change is proposed.** The oracle partition in C-5 is the part of the
brief measured to matter (MT-008/MT-009: a partitioned brief on the weaker model
produced sharper controls than a stronger model without one), and it is carried
in the dispatch prompt. This story is mostly `mechanical` work, which is the
category where invention is least wanted.

**Success condition for RED, falsifiable either way:** RED's handoff either
contains a two-region baseline for AC-8 measured on the *same* fake-session
output as the one-region assertion, or it does not. If it does not, the
partition did not carry and this section should say so — the story's only
oracle-free criterion will have gone out without its control, which is exactly
the failure the partition exists to prevent.

**Result: the partition carried.** The handoff's negative-control table gives the
baseline as **2** on both page sizes, measured outside pytest on the very
`RawDetectorOutput` the one-region assertion consumes, with the merge rule's two
clauses measured beside it (gap **6 px** against a limit of 12, y-overlap
**1.0000** against a limit of 0.50). It also carries a control the story did not
ask for and should have: the same fixture with the columns 54 px apart, which
must stay **two** regions — so AC-8 now discriminates in both directions rather
than only against a chain that skips the merge. Recorded because it is the
comparison this section exists to make, and this time the answer is yes.

## Out of scope

The most valuable section in this file, because a wiring story is the one most
likely to sprawl into "and while I'm here".

- **OCR.** MT-010 owns transcription. This stage writes regions and nothing else;
  no `line` row is created, no text is read. MT-010 is **not** a dependency of
  this story — it is a sibling that will need its own stage.
- **The OCR *stage*.** MT-010 as written delivers OCR logic, not a `Stage`. The
  same assembly gap will therefore exist for OCR after MT-010 lands. Naming it
  here so it is not re-discovered at the epic's end; filing it is a separate
  decision and not this story's.
- **Translation, cleaning, typesetting, baking.** EPIC-04 and later. This story
  adds exactly one stage to a runner that already sequences them.
- **Any change to the functions this story calls.** `regions_from_detection`,
  `merge_columns`, `order_regions`, `sort_regions`, `write_regions`,
  `read_regions`, `letterbox`, `decode_boxes`, `probability_to_page`,
  `integer_boxes`, `load_detector` are all frozen here. This story composes them.
  If one of them is wrong, that is an escalation, not an edit — and per `rules.md`
  the claim is reproduced independently before anything moves.
- **Panel-aware reading order.** `architecture.md` D10, and MT-009 PO-5 recorded
  evidence against D10's own sizing (2 of 5 corpus pages need panel awareness).
  That revisit is a separate story the user has not asked for. This stage calls
  the panel-blind sweep D10 specifies, unchanged. Note the standing tripwire:
  MT-009's `test_the_unusual_pages_are_ordered_differently_from_their_annotation`
  goes red if panel awareness is ever added, by design.
- **CLI and UI surfaces.** Nothing new is printed, shown or exposed. `cli.py` and
  `ui/` are untouched; no progress event is added to `domain.events`
  (`RegionsDetected` is named in `architecture.md` §6 but is MT-018's, together
  with the readout that consumes it).
- **The production composition root — and this one is an open architectural
  question, escalated rather than answered.** Under the contract probed in C-1,
  `app`, `cli`, `ui` and `pipeline` are *all* forbidden from reaching
  `onnxruntime`, transitively included — so as configured today, **no module in
  the shipped package may construct a real detector**, and the only place the
  real stage can be assembled is a test. That is a genuine tension between
  contract 1 (which puts `pipeline` above `detect`) and contract 5, it predates
  this story, and resolving it means either `allow_indirect_imports` on contract
  5, a documented `ignore_imports` exception for one named edge, or a different
  composition root — a decision for the user and an `architecture.md` amendment,
  not for this story's GREEN. **This story does not need it resolved**: injection
  keeps `pipeline` clean, and AC-10 assembles the real chain in
  `tests/integration`, which no contract governs. The first story that starts a
  real run from the UI does need it resolved.
- **Regenerating or re-measuring MT-008's corpus numbers.** AC-10 asserts `>= 1`
  region and order equality, never a count. MT-008's "12 regions on `011.jpg`" is
  one run's measurement, not an oracle.

## Test plan

Three files, at the cheapest level that can falsify each criterion. Nothing is
tested end to end that a fake can falsify, and nothing is faked that only the
real model can answer.

| File | Criteria | Level and why |
|---|---|---|
| `tests/core/test_detect_page.py` | AC-7, AC-8, AC-9 | Unit, against a fake `DetectorSession`. The logic is arithmetic over arrays (`architecture.md` §2), and `unit` + `coverage` are the required gates that really run on CI. |
| `tests/core/test_detect_stage.py` | AC-1 to AC-6 | Unit for the stage's own claims, **integration with the real `run_chapter` and the real `Project`** for AC-3's skip. The store is where the contract lives, so no `Project` is faked anywhere: `is_done` reads the database on every test. |
| `tests/integration/test_detect_stage_page.py` | AC-10 | End to end, GPU only. The one path that needs the real weights; skips cleanly without them. |

### How each criterion is made falsifiable

- **AC-1** — the fixture's detector order is the exact **reverse** of the reading
  order, so every position differs and a pass-through stage is wrong at index 0.
  Two control assertions come first: `order_regions(detector_order)` differs from
  the identity permutation, and equals `[2, 1, 0]`. The page under test is at
  **ordinal 1**, never 0, which is what makes DV-3 able to fail.
- **AC-2** — `detector.seen == [the page's own bytes]` plus "these are not any
  other page's bytes", over a chapter whose three pages have three distinct sizes
  *and* three distinct fills. The read-only promise is a before/after sha256
  snapshot over `rglob("*")`, so a new **directory** beside the scans fails it
  too.
- **AC-3** — the truth table on one page, both rows, plus the two untouched
  pages; then the whole `run_chapter` event stream as an ordered projection
  (`RunStarted`, `PageSkipped` 0/1/2, `RunFinished`) with `detector.calls == 0`.
- **AC-4** — three regions, then two, with the second pair *also* handed over out
  of reading order, so a stage that ordered only on the first write still fails.
- **AC-5** — parametrised `fresh` and `previously-detected`, because the second
  is the one that pins `write_regions(n, ())` clearing a page rather than being a
  no-op.
- **AC-6** — the only criterion that catches a stage clearing the page before it
  detects. Verified to be the only one: of the eight mutations probed below, M4
  (a clearing write added to the top of `run`) is caught by this test **and by
  nothing else in the suite** — 1 failed, 21 passed.
- **AC-7** — shape and dtype of the array the session was handed, plus the grey
  `PAD_VALUE` margins. Shape alone cannot tell a letterbox from a plain
  `cv2.resize` to 1024 square; the padding assertion is the half that can.
- **AC-8** — one region with `merged_from == (0, 1)`, against a **two-region
  baseline computed from the same `RawDetectorOutput` object** the chain
  consumed. Plus a hard negative control in its own test: the same fence box and
  the same y-overlap with the columns 54 px apart instead of 18, which must stay
  two regions.
- **AC-9** — masks decode to exactly `(H, W)` and every vertex is on the page,
  over **two mutually transposed non-square page sizes**, plus the mask extent
  against the painted columns grown by the dilation reach — which is what catches
  a map resized without cropping the letterbox padding.
- **AC-10** — `>= 1` region and `read_regions == sort_regions(what the detector
  returned)`, never a count.

### Also pinned (C-2, C-3, C-1)

`mangatl.detect.page.__all__ == ["detect_page_regions"]`;
`mangatl.pipeline.detect_stage.__all__ == ["DetectStage", "PageDetector"]`;
the return type is a `list[RawRegion]`; `DetectStage(detect=...).name ==
"detect"`; `PageDetector`'s meaning by `get_origin`/`get_args` rather than by
object identity, so `typing.Callable` and `collections.abc.Callable` both
satisfy it; `name` is a settable instance attribute (amendment R-1); and C-1's
seam as a **unit** tripwire — `detect_stage.py`'s own AST must import nothing
matching `mangatl.detect`, `numpy`, `PIL`, `cv2` or `onnxruntime`, so an
implementer who reaches for one is told which import broke the seam, by name, in
the phase that added it.

### What is deliberately not asserted

- `run_chapter` emitting `RunAborted` when the detector raises. MT-006 AC-4 owns
  it; re-asserting it here tests the runner.
- Any region count on `011.jpg`. MT-008's 12 is one run's measurement.
- Any threshold this story passes through. `MASK_THRESHOLD`,
  `COLUMN_MAX_GAP_PX`, `COLUMN_MIN_OVERLAP`, `BAND_OVERLAP_FRACTION`,
  `MODEL_INPUT_SIDE` and `PAD_VALUE` are read out of the shipped constants and
  never restated as literals, so a story that re-derives one moves these tests
  with it instead of silently disagreeing.

## Handoff: RED -> GREEN

### The command

```
uv run pytest -q tests/core tests/ui
```

Narrow while iterating:
`uv run pytest -q tests/core/test_detect_page.py tests/core/test_detect_stage.py`.
AC-10: `uv run pytest -q tests/integration -m "gpu or network"`.

### The failure, verbatim

```
$ uv run pytest -q tests/core tests/ui
=================================== ERRORS ====================================
_______________ ERROR collecting tests/core/test_detect_page.py _______________
ImportError while importing test module 'C:\Users\ryanc\Projects\manga-translator\tests\core\test_detect_page.py'.
Hint: make sure your test modules/packages have valid Python names.
Traceback:
..\..\AppData\Roaming\uv\python\cpython-3.12.14-windows-x86_64-none\Lib\importlib\__init__.py:90: in import_module
    return _bootstrap._gcd_import(name[level:], package, level)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
tests\core\test_detect_page.py:55: in <module>
    from mangatl.detect.page import detect_page_regions
E   ModuleNotFoundError: No module named 'mangatl.detect.page'
______________ ERROR collecting tests/core/test_detect_stage.py _______________
ImportError while importing test module 'C:\Users\ryanc\Projects\manga-translator\tests\core\test_detect_stage.py'.
Hint: make sure your test modules/packages have valid Python names.
Traceback:
..\..\AppData\Roaming\uv\python\cpython-3.12.14-windows-x86_64-none\Lib\importlib\__init__.py:90: in import_module
    return _bootstrap._gcd_import(name[level:], package, level)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
tests\core\test_detect_stage.py:50: in <module>
    from mangatl.pipeline.detect_stage import DetectStage, PageDetector
E   ModuleNotFoundError: No module named 'mangatl.pipeline.detect_stage'
=========================== short test summary info ===========================
ERROR tests/core/test_detect_page.py
ERROR tests/core/test_detect_stage.py
!!!!!!!!!!!!!!!!!!! Interrupted: 2 errors during collection !!!!!!!!!!!!!!!!!!!
2 errors in 0.71s
```

**This is the right failure and it is also the *only* thing observed.** Both
modules are absent, so collection is interrupted and **no assertion in either
file has executed** — not one negative control, not one threshold check. The
first thing the story requires is that these two module paths exist, so an
import error is the correct first red; everything below exists because it is not
enough. `## Gate results` is not written by a `--fast` run, by design.

`bash scripts/gates.sh --fast` read the right shape: `format` PASS (50 files),
`lint` PASS (5 contracts kept, floor 5), `typecheck` PASS (28 source files) —
so the new test files are **admissible** to the gates that will judge them — and
`unit`, `coverage`, `coverage-core` FAIL at exit 2 with the import error above.
No timeout, no config error, no lint rule tripped by a test file.

### One line per test

`tests/core/test_detect_page.py` — 10 tests (4 of them parametrised over the two
page sizes `(320, 240)` and `(240, 320)`):

| Test | Asserts | AC |
|---|---|---|
| `test_the_chain_is_exported_from_mangatl_detect_page_under_that_name` | module path and `__all__ == ["detect_page_regions"]` | C-2 |
| `test_the_chain_returns_a_list_of_raw_regions` | the return is a `list` of `RawRegion` | C-2 |
| `test_the_session_is_handed_the_letterboxed_canvas_exactly_once` ×2 | one call; `(1, 3, 1024, 1024)` `float32`; grey `PAD_VALUE` margins; the fake's transform equals `letterbox`'s | AC-7 |
| `test_two_columns_of_one_utterance_in_one_box_come_back_as_a_single_region` ×2 | 1 region, `merged_from == (0, 1)`, against a 2-region baseline on the same session output | AC-8 |
| `test_two_separate_bubbles_in_one_box_are_not_merged_by_the_chain` ×2 | 2 in, 2 out, no `merged_from` — the merge is MT-008's rule, not a fuse | AC-8 control |
| `test_every_region_of_a_non_square_page_is_masked_and_drawn_in_page_pixels` ×2 | mask shape `(H, W)`; every vertex on the page; extent = painted columns + dilation reach | AC-9 |

`tests/core/test_detect_stage.py` — 13 tests (one parametrised ×2):

| Test | Asserts | AC |
|---|---|---|
| `test_the_stage_is_exported_from_mangatl_pipeline_detect_stage_under_that_name` | module path, `__all__`, `.detect`, `.name == "detect"`, `is_done` returns a `bool` | C-3 |
| `test_the_stage_name_is_a_settable_instance_attribute_as_the_protocol_requires` | `DetectStage` is not a frozen dataclass | C-3 R-1 |
| `test_the_page_detector_alias_is_page_bytes_in_and_a_sequence_of_regions_out` | `PageDetector` is `(bytes) -> Sequence[RawRegion]`, by introspection | C-3 |
| `test_the_stage_module_imports_nothing_that_contract_five_forbids` | the module's AST imports no `mangatl.detect`, `numpy`, `PIL`, `cv2`, `onnxruntime` | C-1 |
| `test_a_pages_regions_are_stored_in_reading_order_whatever_order_they_arrived_in` | `read_regions(1)` is top-right, top-left, bottom-left; fixture order differs | AC-1 |
| `test_the_detector_is_called_once_with_the_pages_own_bytes` | one call, page 1's exact bytes, not another page's | AC-2 |
| `test_running_the_stage_leaves_the_users_scans_byte_for_byte_unchanged` | sha256 snapshot of `source_dir` unchanged, directories included | AC-2 |
| `test_is_done_is_true_for_a_page_with_stored_regions_and_false_without` | the truth table, both rows, and that it is per page | AC-3 |
| `test_a_run_over_pages_that_already_have_regions_skips_them_without_detecting` | the `run_chapter` projection, `detector.calls == 0`, outcome `finished` | AC-3 |
| `test_a_rerun_that_finds_fewer_regions_leaves_none_of_the_previous_run_behind` | exactly the new two, in reading order, the third gone | AC-4 |
| `test_a_page_the_detector_finds_no_text_on_stores_nothing_and_stays_not_done` ×2 | `read_regions == ()`, no raise, `is_done is False` | AC-5 |
| `test_a_detector_that_raises_leaves_the_pages_stored_regions_untouched` | the exception propagates out of `run`; the stored regions are identical | AC-6 |

`tests/integration/test_detect_stage_page.py` — 1 test, `gpu`:

| Test | Asserts | AC |
|---|---|---|
| `test_the_real_chain_reaches_the_store_in_reading_order` | `>= 1` region; `read_regions == sort_regions(detector output)`; the stored order is a fixed point | AC-10 |

### Files touched

- `tests/core/test_detect_page.py` — new
- `tests/core/test_detect_stage.py` — new
- `tests/integration/test_detect_stage_page.py` — new
- `docs/backlog/stories/MT-035.md` — `## Test plan`, this section, and
  amendments **R-1** (C-3) and **R-2** (C-2), both made in place with a reason

No production source, no config, no fixture file. `.gitignore` needed no change:
nothing new writes into the tree.

### The export shape these tests already pin

Stated as fact, not suggestion: a test already imports each of these and a wrong
guess is an ImportError or an AttributeError.

**`src/mangatl/detect/page.py`**

```python
__all__ = ["detect_page_regions"]

def detect_page_regions(session: DetectorSession, image_bytes: bytes) -> list[RawRegion]: ...
```

- `session` is positional-first and `image_bytes` second: every call site in the
  tests is positional, and AC-10 uses `partial(detect_page_regions, session)`,
  which only works in that order.
- the return must be a `list` (`isinstance` is asserted), of `RawRegion`.
- the only attributes read off `session` are `run(canvas) -> RawDetectorOutput`.
  `get_providers` exists on the fake but **must not be called** — a fake that
  answered a question nobody asked would hide that; nothing asserts it is
  unused, so this is guidance rather than a pin.

**`src/mangatl/pipeline/detect_stage.py`**

```python
__all__ = ["DetectStage", "PageDetector"]

PageDetector = Callable[[bytes], Sequence[RawRegion]]

@dataclass
class DetectStage:
    detect: PageDetector
    name: str = "detect"
    def run(self, ctx: PageContext) -> None: ...
    def is_done(self, ctx: PageContext) -> bool: ...
```

- `__all__` is asserted as that exact **list**, in that order.
- the field is named `detect` and `DetectStage(detect=...)` must work as a
  keyword; `stage.detect is detector` is asserted, so it is readable and holds
  the object handed in.
- `name` defaults to `"detect"` and must be **assignable** (amendment R-1).
- `is_done` must return an actual `bool` — `isinstance(..., bool)` is asserted,
  so a bare truthy tuple will not do. `bool(ctx.project.read_regions(...))` is
  the shape.
- `PageDetector` is checked by `get_origin`/`get_args`, so either
  `collections.abc.Callable` or `typing.Callable` passes. The parameter list must
  be exactly `[bytes]` and the return `Sequence[RawRegion]`.

**Not constrained — the implementer's choice.** Whether `run` uses locals or
inlines the four steps; the order of imports; whether `is_done` calls
`read_regions` or a narrower query (as long as it is settable-`bool`); module
docstrings; whether `detect/page.py` keeps the page array in a local or a helper;
`eq=`/`slots=` on the dataclass as long as `name` stays assignable.

### Fake-session and fake-detector shapes GREEN must satisfy

**`_FakeSession` in `test_detect_page.py`** answers `run(canvas)` with a
`RawDetectorOutput` whose:

- `blk` is `(1, 1, 7)` `float32` — one anchor, `cx cy w h obj cls0 cls1` in
  **canvas** coordinates, `obj = 1.0`, `cls1 = 0.9`, so `decode_boxes` accepts it
  at confidence 0.9 and class 1 (`bubble`);
- `seg` is `(1, 1, 1024, 1024)` `float32`, 0.0 everywhere except the painted
  columns at 1.0;
- `det` is `(1, 2, 1024, 1024)` `float32` zeros — carried, never read.

It paints in **page** coordinates and re-derives `(ratio, pad_x, pad_y)` itself
from `MODEL_INPUT_SIDE` rather than calling `letterbox`, so it cannot land its
rectangles correctly against a wrong transform. The two derivations are asserted
equal in the AC-7 test, so a disagreement is named once rather than surfacing as
a region count.

Fixture geometry, all half-open page rectangles: fence `(60, 40, 180, 140)`;
merging columns `(80, 60, 86, 120)` and `(104, 60, 110, 120)` — **18 px apart**;
separate columns `(80, 60, 86, 120)` and `(140, 60, 146, 120)` — **54 px apart**.
18 is chosen against two settled constants: the dilation reach is 6 px each side,
so a painted gap `g` reaches `merge_columns` as `g - 12`, and staying two
components through the dilation needs `g >= 13` while merging under
`COLUMN_MAX_GAP_PX = 12` needs `g <= 24`. 18 is the middle of `[13, 24]`.

**`_FakeDetector` in `test_detect_stage.py`** is a callable object, not a
function: `__call__(self, image_bytes: bytes) -> Sequence[RawRegion]` returning a
`list`, recording every argument in `self.seen`, and optionally raising
`_DetectorExploded`. So `PageDetector` must be satisfiable by *any* callable, not
only by a plain function — a `Callable` alias, which is what C-3 says.

The fixture chapter is three PNGs on `tmp_path/scans/`: `p1.png` 30x20,
`p2.png` 40x30, `p3.png` 50x40, each with a distinct fill, and **the page under
test is ordinal 1**. Region masks are 40x30 1-bit PNGs from `conftest`'s
`one_bit_png`; confidences are 0.5, 0.75 and 0.25 so the store round trip is
bit-identical and whole `RawRegion`s can be compared with `==`.

### Negative controls: the expected values, as numbers

Collection is interrupted, so **none of these ran inside the framework**. Every
number below was measured **outside pytest**, with `uv run python` on a scratch
script that performs C-2's seven steps inline exactly as
`tests/integration/test_column_merge_pages.py`'s `detected` fixture does.
**Confirming them against the shipped modules is GREEN's job.**

**AC-8 — the two-region baseline. This is the number the story's `## Model
guidance` asks for, measured on the same fake-session output as the one-region
assertion.**

| Quantity | Threshold | Candidate range | Measured (outside pytest) |
|---|---|---|---|
| `regions_from_detection` on the merging fixture — the baseline | must be exactly **2**, or AC-8 is vacuous | 0 (nothing found) … n | **2** on `(320, 240)` and **2** on `(240, 320)` |
| `merge_columns` on those two — the criterion | exactly **1** | 1 or 2 | **1** on both page sizes |
| `merged_from` of the survivor | `(0, 1)` | `()`, `(0,)`, `(1,)`, `(0, 1)` | **`(0, 1)`** on both |
| gap between the two pre-merge extents | `<= COLUMN_MAX_GAP_PX` = **12** px | 0 … 120 | **6 px** |
| y-overlap as a fraction of the shorter extent | `>= COLUMN_MIN_OVERLAP` = **0.50** | 0.0 … 1.0 | **1.0000** |
| pre-merge extents | — | — | `(74, 54, 92, 126)` and `(98, 54, 116, 126)` |
| merged extent / polygon | — | — | `(74, 54, 116, 126)` / `((74,54),(115,54),(115,125),(74,125),(74,54))` |
| merged confidence | `[0.0, 1.0]` | — | **0.2778** (1.0 on 360 painted px of a 1296 px dilated mask) |

**AC-8's hard negative control — the same fixture with the columns 54 px apart:**

| Quantity | Threshold | Measured |
|---|---|---|
| baseline regions | must still be **2**, or it is not a control | **2** |
| after `merge_columns` | must stay **2** | **2** |
| gap | must exceed **12** px | **42 px** |
| y-overlap | must still be `>= 0.50`, so the *gap* clause is what separates them | **1.0000** |
| `merged_from` of each survivor | `()` | **`()`, `()`** |

**AC-7 — the canvas control:**

| Quantity | Expected | Measured |
|---|---|---|
| canvas shape | `(1, 3, 1024, 1024)` | **`(1, 3, 1024, 1024)`** |
| canvas dtype | `float32` | **`float32`** |
| `(ratio, pad_x, pad_y)` | `(3.2, 0, 128)` landscape / `(3.2, 128, 0)` portrait | **exactly that, and equal to `letterbox`'s own `LetterboxTransform`** |
| padded margin value | `PAD_VALUE / 255` = **0.4470588** | **0.44705883** (`float32`) |
| canvas centre pixel | the page's fill, `0 / 255` = 0.0 | **0.0** |
| the page's own shape, which the canvas must **not** be | `(240, 320)` / `(320, 240)` | asserted different |

**AC-9 — the coordinate controls:**

| Quantity | Expected | Measured |
|---|---|---|
| mask shape | `(H, W)` = `(240, 320)` / `(320, 240)` | **exactly that** |
| mask shape at model scale, which it must not be | `(1024, 1024)` | asserted different |
| mask extent vs. painted columns + 6 px reach | `(74, 54, 116, 126)` ± 1 px | **`(74, 54, 116, 126)`, exact on both page sizes** |
| vertices outside the page rect | **0** | **0** |
| a chain skipping `probability_to_page` | must not pass | `ValueError: prob.shape is (1024, 1024), which is not (height, width) = (240, 320) for page_size (width, height) = (320, 240)` |
| a chain resizing without cropping the padding | must not pass | extent `y0` = **69**, 15 px out — which is why the extent tolerance is 1 px and not 15 |

**AC-1 — the reading-order control:**

| Quantity | Expected | Measured |
|---|---|---|
| `order_regions(detector_order)` | must differ from `[0, 1, 2]` | **`[2, 1, 0]`** |
| `sort_regions(detector_order)` | `[top-right, top-left, bottom-left]` | **`True`** |
| `order_regions(the AC-4 pair)` | must differ from `[0, 1]` | **`[1, 0]`** |

**AC-10 — measured on the real model, `CUDAExecutionProvider`, 2026-09-16.**
Informational only; no count is asserted:

*Orchestrator's note on where these came from.* The model and the pages are
real; the **composition under them was the scratch reference**, not shipped
source, because in RED no shipped source exists. Verified rather than assumed —
the AC-10 file does import the missing module and does fail at collection:

```
$ uv run pytest -q tests/integration -m "gpu or network"
tests\integration\test_detect_stage_page.py:54: in <module>
    from mangatl.detect.page import detect_page_regions
E   ModuleNotFoundError: No module named 'mangatl.detect.page'
1 error in 0.42s
```

So "12 passed in 2.26s" is what the gate will report **if** GREEN ships a chain
equivalent to the reference, and is not a measurement of this branch. Read the
row as a prediction with a mechanism behind it. The `11 before` half of it *is* a
measurement of this branch, taken by the orchestrator in PLANNED.

| Quantity | Measured |
|---|---|
| provider | `CUDAExecutionProvider` |
| `011.jpg`, one-page project | 1125 x 1600, ordinal 0 |
| regions the chain returned | **11** (MT-008 measured 12 before merging; 1 merge here, so this is consistent — and still not an oracle) |
| regions stored | **11** |
| `read_regions == sort_regions(detector output)` | **True** |
| stored order is a fixed point of `sort_regions` | **True** |
| first three stored polygon origins | `(894, 59)`, `(124, 207)`, `(238, 720)` — top-right first |
| regions recording a merge | 1 |
| whole `integration` gate with this file added | **12 passed in 2.26s** (11 before) |

### Every test was observed to fail, and to fail for its own reason

An import error tells GREEN nothing about whether these assertions discriminate,
so that was measured separately. A **scratch reference implementation** of C-2
and C-3 was written *outside the project tree*, in the session scratchpad, and
grafted into `sys.modules` by a pytest plugin on `PYTHONPATH` — writing nothing
into `src/`, touching no project file, and leaving no trace. Against it:

```
$ PYTHONPATH=<scratchpad> uv run pytest -q tests/core tests/ui -p ref_impl
315 passed in 7.86s
```

Then eight defects were introduced into that scratch reference, one at a time:

| # | Defect | Tests that went red |
|---|---|---|
| M1 | `sort_regions(regions)` → `regions` (**= DV-1**) | AC-1, AC-4, AC-6 — **3 failed, 19 passed** |
| M2 | `merge_columns(...)` → its first argument (**= DV-2**) | AC-8 ×2, AC-9 ×2 — **4 failed, 18 passed** |
| M3 | `write_regions(ctx.page.ordinal, …)` → `write_regions(0, …)` (**= DV-3**) | AC-1, AC-3 ×2, AC-4, AC-5, AC-6 — **6 failed, 16 passed** |
| M4 | a clearing `write_regions(ordinal, ())` added to the top of `run` | **AC-6 only — 1 failed, 21 passed** |
| M5 | `session.run(page)` instead of `session.run(canvas)` | AC-7 ×2 — **2 failed, 20 passed** |
| M6 | `probability_to_page` skipped | 9 failed, 13 passed |
| M7 | `is_done` reads `page_status == "done"` instead of regions | AC-3 ×2, AC-5 — **3 failed, 19 passed** |
| M8 | map resized to the page but the letterbox padding never cropped | AC-9 ×2 and 7 others — 9 failed, 1 passed |

M4 is the one worth reading twice: the story says a stage that cleared the page
first would pass every other criterion in the file, and that is now measured
rather than argued — **exactly one** test catches it.

M2 fails on the criterion and not on its own control, which is the shape AC-8
needs:

```
>       assert len(merged) == 1, (
E       AssertionError: 2 regions before merging and 2 after; the chain does not call merge_columns
E       assert 2 == 1
```

And amendment R-1's test was watched to fail against C-3 **as originally
written**:

```
E   dataclasses.FrozenInstanceError: cannot assign to field 'name'
FAILED tests/core/test_detect_stage.py::test_the_stage_name_is_a_settable_instance_attribute_as_the_protocol_requires
1 failed, 12 passed in 0.82s
```

### Contract C-4 was checked against the tree

Swept with `rg` over `src/` and `tests/` before writing a line of test code, for
`order_regions`, `sort_regions`, `merge_columns`, `regions_from_detection`,
`integer_boxes`, `letterbox`, `decode_boxes`, `probability_to_page`,
`load_detector`, `write_regions`, `read_regions`, `Stage`, `PageContext`,
`PassThroughStage` and `run_chapter`. **C-4's claim holds: this story re-shapes
no existing export and is a pure new caller.** Every one of those names is
defined in exactly one module and called only from that module's package, from
`tests/`, or — for `PassThroughStage` and `run_chapter` — from `src/mangatl/cli.py`,
which this story does not touch. There is no old signature left standing for a
missed caller to keep compiling against. `detect_page_regions`, `DetectStage`,
`PageDetector` and `detect_stage` appear **nowhere** in the tree outside this
story's own files, so nothing is being redefined. Nothing to escalate.

### Declines: DV-1, DV-2 and DV-3

**All three are declined, in writing, and the entries are left to GATES, which
owns them.**

- **DV-1 — the ordering step, dropped.** *Declined: RED cannot run it.*
  `src/mangatl/pipeline/detect_stage.py` does not exist, so there is no stage to
  mutate. What RED can report is that the condition DV-1 depends on is satisfied:
  the AC-1 fixture's detector order is `[2, 1, 0]` in reading order, so it is
  genuinely different from the identity permutation and the criterion is not
  vacuous. The equivalent defect was measured on a scratch reference (M1 above)
  and AC-1's test went red.
- **DV-2 — the merge step, dropped.** *Declined: RED cannot run it.*
  `src/mangatl/detect/page.py` does not exist. The equivalent defect was measured
  on a scratch reference (M2 above) and AC-8's test went red on the criterion
  assertion with its baseline intact.
- **DV-3 — a wrong value, not a missing step.** *Declined: RED cannot run it.*
  No stage to mutate. RED has discharged the part that **is** a requirement on
  RED: the AC-1 fixture's page sits at **ordinal 1**, not 0, which is what makes
  the mutation visible. The equivalent defect was measured on a scratch reference
  (M3 above) and six tests went red.

A scratch reference is not the shipped module and the M-series results are not a
substitute for these three. They are evidence that the assertions discriminate;
DV-1 to DV-3 are evidence that *the code GREEN ships* is discriminated by them,
and only GATES can produce that.

### What GREEN should know before starting

1. **Build C-3 as amended, not as first written.** `@dataclass`, not
   `@dataclass(frozen=True)`. R-1 has the measured mypy output.
2. **`.convert("RGB")` is not optional** — R-2. A greyscale scan otherwise
   decodes to a 2-D array and `letterbox` fails to broadcast it.
3. **Confirm the negative-control numbers above**, not merely that the control
   tests pass. The two that matter most are AC-8's baseline (**2**) and the
   far-column control's gap (**42 px**): a control that measures the wrong thing
   makes every threshold in the suite look calibrated and prove nothing.
4. **If you find `pipeline/detect_stage.py` wanting `numpy`, `PIL` or
   `mangatl.detect`, stop.** The seam is wrong, C-1 is the hinge of the design,
   and `test_the_stage_module_imports_nothing_that_contract_five_forbids` will
   tell you which import did it before `lint` does.
5. **No timeouts to budget.** `pytest-timeout` is not installed and no
   `timeout` setting exists in `pyproject.toml`, so no test or hook in these
   files carries one and none needs one. For when that changes: measured under
   coverage instrumentation on this machine, not one of the 23 new core tests
   appears in `--durations=8` (the slowest test in the whole suite is an existing
   `hypothesis` property at 0.44 s); the three failing gates took 1 s, 2 s and
   2 s. **All timings in this handoff are from a local run**, on
   `CUDAExecutionProvider`; none is from a CI log, and AC-10 never runs on CI.
6. **`## Regressions` is deliberately absent.** This is a first RED, not a
   return: no test was corrected against existing code, so there is no
   unobserved assertion to earn there. R-1's frozen-dataclass finding is recorded
   above with its red, because it is a contract amendment rather than a
   regression.

## Gate results

<!-- gates.sh: written by bash scripts/gates.sh; do not edit or paste by hand -->

    run:    2026-09-17T00:11:31Z
    commit: c897d3c (working tree had uncommitted changes)
    tree:   b2f6d97ce5cf385a1ad9e9fc0b2aee2f14f9e322
    result: pass (9 ran, 0 unconfigured, 1 known)

    PASS         format (0s, observed 52)
    PASS         lint (0s, observed 5, floor 5)
    PASS         typecheck (0s, observed 30, floor 28)
    PASS         unit (11s, observed 315, floor 292)
    PASS         coverage (12s, observed 315, floor 292)
    PASS         coverage-core (11s, observed 310, floor 287)
    PASS         integration (4s, observed 12)
    PASS         build (38s)
    KNOWN        mutation (0s, exit 2; mutmut is not installed and not configured until the suite is real; stack.md §6) -> .claude/state/gate-logs/mutation.log

## Notes

### Sizing: one cycle, and where the seam is if RED disagrees

Ten criteria and two new modules is at the top of this project's demonstrated
cycle size, so the judgement is recorded rather than assumed. It is one cycle
because **neither half is shippable alone**: `detect/page.py` without the stage
is a fourth pure function nobody calls — precisely MT-009 PO-4's complaint, one
level up — and `DetectStage` without the adapter is a stage with no real
implementation to inject, whose only production wiring would live in a test.
Splitting re-creates the gap this story exists to close. For comparison, MT-007
shipped `session.py` and `postprocess.py` in one cycle; the implementation here is
thinner than either, because every rule it depends on is already written and
tested.

**If RED finds it does not fit, the seam is clean and pre-named:** AC-7 to AC-9
(`detect/page.py`) split from AC-1 to AC-6 (`pipeline/detect_stage.py`), with the
adapter first and the stage depending on it, and AC-10 going with the stage. Stop
and say so rather than trimming criteria to fit.

### Ordering against MT-010

MT-009 PO-4 recommended this story sit "after MT-010". It is filed as a **sibling
of MT-010 instead, depending only on MT-009**, because nothing in it needs OCR:
the detect stage writes regions, and MT-010 writes lines. Waiting would block a
closable gap behind an unrelated story, and going first means MT-010's own stage
can follow the `PageDetector` pattern C-3 establishes rather than inventing a
second one. Recorded here because it is a deliberate departure from the
recommendation that created the story.

### Where AC-6 came from, and what was trimmed

An earlier draft carried a criterion asserting `run_chapter` emits `RunAborted`
naming the page when the detector raises. That was cut: MT-006 AC-4 already owns
it and re-asserting it here tests the runner, not this stage. What survives as
AC-6 is the part that *is* this stage's claim — that it writes once, at the end,
and so leaves the previous run's regions intact when the detector fails. A stage
that cleared the page first would pass every other criterion in this file.

### PO-1 — the epic's done-when has one gap left after this story, and it is not this story's to absorb

Checked before dispatch. EPIC-03's done-when reads *"the pipeline produces
regions with masks, with furigana folded into their parent utterance ... in
right-to-left top-to-bottom reading order, each carrying transcribed vertical
Japanese."* Clause by clause against this story's criteria:

| Clause | Delivered by | True of the *pipeline* once MT-035 lands? |
|---|---|---|
| regions with masks | MT-007, composed here (AC-7 to AC-9) | yes |
| furigana folded, no ruby column standing alone | MT-008, composed here (AC-8) | yes |
| right-to-left, top-to-bottom reading order | MT-009, composed here (AC-1) | yes |
| each carrying transcribed vertical Japanese | MT-010 | **no** |

The last clause is the one to be explicit about rather than let pass. MT-010 is
the epic's final story and it delivers OCR *logic*, not a `Stage` — so when
MT-010 lands, the epic's done-when will be untrue of the pipeline for precisely
the reason MT-009 PO-4 filed *this* story: a correct pure function that nothing
invokes. The same gap, one stage later.

**Decision: MT-035 does not absorb it, and the sibling story is filed after
MT-010 lands rather than now.** Not on sizing grounds — on the grounds that
there is nothing yet to build against. An OCR stage takes an injected
`PageTranscriber` the way C-3's `DetectStage` takes a `PageDetector`, and the
shape of that callable is fixed by what MT-010 actually ships, which does not
exist. Filing it now would pin a signature to a guess, and a contract pinned to
a guess is worse than a gap that is written down. What this story does owe the
epic is the *pattern*, and C-3 is it.

**Reported to the user with the RED hand-off so it can be overruled before
GREEN.** Two ways to overrule: fold an OCR stage into MT-010's scope, or file
the sibling story now with its contract deliberately left open. Doing neither
means the epic closes on MT-010 one stage short of its own sentence — which is
the outcome this note exists to stop happening quietly.

### Measured before dispatch: the `integration` gate really does run here

`required_gates: [integration]` is only worth carrying if the gate executes on
the machine running the loop, so the `## Context` claim was measured rather than
inherited from MT-007 PO-4:

```
$ uv run pytest -q tests/integration -m "gpu or network"
...........                                                              [100%]
11 passed in 2.21s
```

Eleven tests, no skips, against the real 94 MB `.onnx` under
`spikes/MT-002/models/comic-text-detector-onnx/` and the pages under
`spikes/MT-002/pages/` — both gitignored, both present. So AC-10 has somewhere
to run, and an `integration` gate reporting BLOCKED or all-skipped on this
machine at GATES is a change in the environment, not the expected state.

### Observation for the harness, not for this story: `check-boundaries.sh` at RED

Run by the orchestrator at the end of RED, to know the state the phase leaves:

```
FAIL  story MT-035 is in phase 'RED'; a PR should be opened from REVIEW or DONE
FAIL  story MT-035: ## Deferred verifications has no result and no waiver. The
      phase that owned it has passed and nothing says what happened.
```

The first is correct and expected — no PR is being opened from RED. The second
is **correctly unsatisfied but wrongly worded**: at RED the phase that owns
DV-1 to DV-3 is GATES, which has *not* passed. The check appears to gate on the
story having a DV block at all rather than on the owning phase having been
reached.

This produces no false pass — the phase FAIL fires alongside it on every run
from RED, so nothing reaches REVIEW on the strength of it — so it is noise
rather than a hole, and it clears the moment GATES pastes the three results.
**Recorded rather than fixed**: editing a CI script mid-story is scope this
story does not have, and `## Out of scope` is the section this project trusts.
Raised with the user at the RED hand-off; if it is worth a harness story, it is
one of its own.

### The handoff's mutation table, verified against the shipped modules

RED's M-series was measured against a **scratch reference**, not shipped source,
so until GREEN landed it was a claim (`rules.md`: *"when the claim is 'this suite
discriminates', the check is a mutation you run"*). Two were re-run by the
orchestrator against the committed implementation with `scripts/mutate.sh`,
choosing the one whose predicted catch is a **single** assertion first, since a
lone assertion is where a vacuous test hides.

**M4 — a clearing `write_regions(ctx.page.ordinal, ())` inserted at the top of
`run`.** Predicted: AC-6 and nothing else.

```
$ bash scripts/mutate.sh src/mangatl/pipeline/detect_stage.py \
    's|        image_bytes = (ctx|        ctx.project.write_regions(ctx.page.ordinal, ())\n        image_bytes = (ctx|' \
    -- uv run pytest -q tests/core/test_detect_stage.py tests/core/test_detect_page.py

>       assert fixture.project.read_regions(_TARGET_ORDINAL) == before
E       assert () == (RawRegion(po...rged_from=()))
E         Right contains 3 more items, ...
FAILED tests/core/test_detect_stage.py::test_a_detector_that_raises_leaves_the_pages_stored_regions_untouched
1 failed, 22 passed in 1.41s

=== mutate: command exited 1; restored (verified byte-for-byte against ...bak) ===
```

**Exactly one test, and it is the AC-6 test.** The story claimed in
`## Notes`, before any of this was built, that *"a stage that cleared the page
first would pass every other criterion in this file"* — that is now measured on
the shipped stage rather than argued. RED's count was `1 failed, 21 passed`
against 22 tests; the suite is 23 now, so 22 pass. The prediction that matters —
**one** failure, and which one — is exact.

**M5 — `session.run(page)` instead of `session.run(canvas)`.** Predicted: AC-7,
both parametrisations. A second module, so the table is checked in both.

```
$ bash scripts/mutate.sh src/mangatl/detect/page.py 's|raw = session.run(canvas)|raw = session.run(page)|' \
    -- uv run pytest -q tests/core/test_detect_stage.py tests/core/test_detect_page.py

E       assert (320, 240, 3) == (1, 3, 1024, 1024)
FAILED tests/core/test_detect_page.py::test_the_session_is_handed_the_letterboxed_canvas_exactly_once[(320, 240)]
FAILED tests/core/test_detect_page.py::test_the_session_is_handed_the_letterboxed_canvas_exactly_once[(240, 320)]
2 failed, 21 passed in 1.70s

=== mutate: command exited 1; restored (verified byte-for-byte against ...bak) ===
```

Green again afterwards, and no backup survived either run — `mutate.sh` exits 90
on a failed restore and leaves the `.bak` behind, and there is none:

```
$ uv run pytest -q tests/core/test_detect_stage.py tests/core/test_detect_page.py
23 passed in 1.25s
$ ls .claude/state/mutations/*.bak
ls: cannot access '.claude/state/mutations/*.bak': No such file or directory
```

Two mutations, two exact matches, in two different modules. The table is
evidence now rather than a claim. DV-1, DV-2 and DV-3 remain GATES' own and are
not discharged by this.

### GREEN confirmed RED's controls, and corrected the generality of one number

RED's suite failed at import, so every control value in the handoff was an
unverified claim. GREEN re-measured all of them against the shipped modules and
**every one reproduced exactly, on both page sizes** — including the two the
dispatch named as mattering most: AC-8's baseline of **2** and the far-column
control's gap of **42 px**, both computed from the same `RawDetectorOutput` the
chain consumed.

One reported nuance, recorded because it is a place a later reader could
over-generalise a measurement rather than a defect in anything: RED logged the
no-crop hostile control as *"extent `y0` = 69, 15 px out"*. That figure is
**landscape-only**. On the transposed portrait page the same defect appears in
**x** instead — extent `(84, 54, 119, 126)`, x0 10 px out — because the padding
sits on the other axis. The control holds on both orientations (10 px is still
far past the 1 px slack) and no frozen test asserts the number, so nothing
changes; the handoff's figure is simply narrower than it reads.

### The PR's first CI run, read for timings rather than for its tick

PR #21, run 35166437577. **`gates` pass in 20m46s, `boundaries` pass in 5s.**
Read because a pass within 10 % of a limit is a pending failure, and because
MT-009 left its own numbers here for exactly this comparison.

**Job steps, against MT-009's last green run:**

| Step | MT-035 | MT-009 | Δ |
|---|---|---|---|
| Harness self-test | 13m25s | 13m10s | +15 s — noise; this branch adds no harness test |
| Show configured gates | 64 s | 65 s | — |
| Audit the gate manifest | 66 s | 65 s | — |
| Run gates | 4m45s | 3m22s | **+1m23s** |

**The gates themselves, on CI hardware:**

```
PASS   format (12s, observed 52)          PASS   coverage-core (41s, observed 310, floor 287)
PASS   lint (1s, observed 5, floor 5)     WARN   integration (1s, ran but produced no
PASS   typecheck (9s, observed 30)               evidence of work, optional)
PASS   unit (42s, observed 315, floor 292) PASS  build (64s)
PASS   coverage (41s, observed 315)       KNOWN  mutation (mutmut not installed; stack.md §6)

All required gates passed (9 ran, 0 unconfigured, 1 known).
```

**Nothing is near a limit, and that is a measurement rather than a shrug**: no
workflow in `.github/workflows/` declares `timeout-minutes`, so the ceiling is
GitHub's 360-minute default and the whole job sits at **5.8 %** of it. There is
no hook timeout in this project either — `pytest-timeout` is not a dependency
and no `timeout` setting exists in `pyproject.toml`. So the 30-second-hook
failure mode the loop warns about has nothing to attach to here **yet**; the
first story that adds a per-test timeout has to size it against `coverage`'s
instrumented numbers below, not `unit`'s.

**The cost this story added, recorded so the trend is visible:** `unit` went
34 s → **42 s** and `coverage` 24 s → **41 s** against the figures MT-035's own
`## Context` quoted from the MT-009 PR. The suite grew 292 → 315 (+23 tests),
and `coverage` grew faster than `unit` because these tests do real work under
instrumentation — 1024-square letterboxing, PNG encode and decode per region.
**`coverage` is now the slowest gate in the suite and has roughly doubled in one
story.** Still 41 s against no limit, so it is not a problem today; it is the
number the next story should check before adding another instrumented suite.

**The `integration` WARN is read, not skipped** (the loop's rule for an optional
gate). It reports *"ran but produced no evidence of work: expected
/[1-9][0-9]* passed/, optional"* in 1 s — which is **exactly what `## Context`
predicted before the story started**: on CI `STORY_REQUIRES` is empty, so the
gate stays optional and skips for want of a CUDA provider and the gitignored
weights. It is not a regression and it needs no waiver. The criterion behind it,
AC-10, is covered where it can actually run: **`integration` PASS with 12 real
GPU tests** in the recorded local run, which `check-boundaries.sh` confirmed
with *"story-required gate 'integration' passed in the recorded run"*. No gate
reached REVIEW marked `pending CI`, so nothing is owed at DONE.

### Closed

PR #21 merged 2026-09-17T01:15:44Z by `ryanczhang7`, merge commit `813f8d0`.
Both CI runs green: the story commit at `gates` **20m46s** / `boundaries`
**5s**, and the docs-only timings commit at `gates` **21m27s** / `boundaries`
**5s** — a 41 s spread on identical code, which is runner noise and is recorded
because the loop's standing warning is about a docs-only re-run falling off a
margin. There was no margin to fall off here: 5.8 % of the only ceiling that
exists.

No gate reached REVIEW marked `pending CI`, so nothing was owed at DONE.

**What this story makes true, in the epic's own words:** *"Given a page scan,
the **pipeline** produces regions with masks, with furigana folded into their
parent utterance rather than standing as phantom bubbles, in right-to-left
top-to-bottom reading order"* — all of that sentence except its final clause,
which is MT-010's and is tracked as PO-1 above.
