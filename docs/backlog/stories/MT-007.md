---
id: MT-007
title: Text regions are detected with masks
slug: text-regions-are-detected-with-masks
epic: EPIC-03
type: feature
status: todo
phase: PLANNED
branch: story/MT-007-text-regions-are-detected-with-masks
depends_on: [MT-002, MT-006, MT-030]
required_gates: [integration]
---

## Context

Brief feature 2, and the story the brief's S2 metric attaches to. It also sets
the boundary that enforces the brief's firmest non-goal: **art-integrated sound
effects are never touched**, and that is a property of what this detector is
allowed to emit, not a filter applied downstream.

Read `docs/wiki/audits/MT-002-model-runtime.md` before starting. Its
`## Decided` section is **settled** — the runtime and the model file identity
are not to be re-litigated. Its `## Evidence` section is **not**: any latency,
VRAM figure or output-plausibility claim this story depends on is to be
re-verified, not assumed.

`required_gates: [integration]` is set because one criterion (AC-7) genuinely
can only run against the real model. Every other criterion runs in `unit`
against a fake session — see the adapter split in `architecture.md` §2, which
exists precisely so this list has one entry rather than seven.

## Acceptance criteria

- **AC-1** — Given a fake session returning a known probability map, when
  detection post-processing runs, then it produces one region per connected
  component above the threshold, each with a polygon and a 1-bit mask of the
  same dimensions as the page.
- **AC-2** — Given a probability map with a component of area below
  `MIN_REGION_AREA_PX`, when post-processing runs, then that component produces
  no region.
- **AC-3** — Given a probability map with two components whose masks overlap,
  when post-processing runs, then two regions are produced and each region's
  mask contains only its own pixels.
- **AC-4** — Given a page, when detection runs, then every returned region's
  polygon lies entirely within the page bounds and every mask pixel outside the
  polygon's bounding box is zero.
- **AC-5** — Given a region, when its mask is stored and read back through
  MT-005's store, then it is bit-identical.
- **AC-6** — Given a fixture page carrying art-integrated sound effects outside
  any bubble, when detection runs with the shipped configuration, then **no
  region overlaps any pixel of the annotated SFX area**.
  *Control:* the same page with the SFX region annotated as a bubble must
  produce a region there — otherwise the assertion is passing because the
  detector finds nothing at all.
- **AC-7** — *(integration)* Given the real detector model and a real page
  fixture, when the session loads and runs, then it returns a probability map of
  the expected shape and dtype, and post-processing yields at least one region.

## Contract

```
src/mangatl/detect/session.py
    class DetectorSession(Protocol):
        def run(self, page: NDArray[np.float32]) -> NDArray[np.float32]: ...
    def load_detector(model_path: Path, providers: Sequence[str]) -> DetectorSession

src/mangatl/detect/postprocess.py
    MASK_THRESHOLD: float        # settled in GREEN from fixture measurement
    MIN_REGION_AREA_PX: int
    def regions_from_probability(prob: NDArray[np.float32],
                                 page_size: tuple[int, int]) -> list[RawRegion]

src/mangatl/domain/region.py
    @dataclass(frozen=True) RawRegion:
        polygon: tuple[tuple[int, int], ...]   # page pixel coords, closed
        mask: bytes                            # PNG-encoded 1-bit, page-sized
        confidence: float                      # mean probability inside the mask
        kind: Literal["bubble", "box"]
```

**Semantics behind each number:**

- **`MASK_THRESHOLD`** is the probability above which a pixel is text. Raising
  it loses faint text; lowering it bleeds into screentone. It is **not settled
  here** — GREEN measures it on the fixture set and records the measurement and
  the two failure directions in `## Notes`. It is a number a later story may
  read out; it is a number this story must derive.
- **`MIN_REGION_AREA_PX`** exists to discard speckle, not to discard small
  bubbles. A single-kana bubble is legitimate and must survive; the floor is
  sized below the smallest annotated region in the fixture set, and the fixture
  that pins it is named in `## Notes`.
- **`confidence` is the mean probability inside the mask**, not the max. The max
  is 1.0 for almost every region and therefore carries no information.
- **`polygon` is in page pixel coordinates**, not model-input coordinates. The
  rescale from the model's input size back to the page is where an off-by-a-
  ratio bug lives, and AC-4 is what catches it.

**Oracle partition:**

| AC | Kind | Instruction to RED |
|---|---|---|
| AC-1..AC-5 | **Mechanical** | Fake session, synthetic probability maps you construct by hand. Pin exactly; these are arithmetic. |
| AC-6 | **Oracle-free** | The SFX exclusion has no settled metric. Invent one (pixel overlap against an annotated mask is the obvious candidate) and **demand the control named in the criterion fires hard**. An assertion that passes against a detector returning zero regions is the failure this AC exists to prevent. |
| AC-7 | **Mechanical** | Shape, dtype, non-empty. A smoke test, nothing more. |

**Fixtures.** This story introduces `fixtures/pages/` with, at minimum: a page
with clean bubbles, a page with SFX outside bubbles, a page with overlapping
bubbles, a page with one single-kana bubble. Each needs a hand-made annotation
file. **These fixtures are development material and must never be the benchmark
chapter** (EPIC-07, O1).

## Deferred verifications

**Is AC-6's metric capable of failing?**

- *Falsifiable condition:* with post-processing forced to return an empty region
  list, AC-6 **must** fail (its control clause), and AC-1 **must** fail. With
  `MASK_THRESHOLD` set to 0.0 — every pixel is text — AC-6 **must** fail,
  because the single page-sized region overlaps the SFX area.
- *Why RED cannot run it:* there is no post-processing to mutate.
- **Owner: GATES.** Three mutations via `bash scripts/mutate.sh`, one of them a
  wrong *value* (`MASK_THRESHOLD`) rather than a removed behaviour.
<!-- Result: pasted by GATES - what was mutated, what failed, and that the file
     was restored - or the word WAIVED with the reason. Nothing else discharges this. -->

## Model guidance

- **RED and GREEN models:** to be recorded at dispatch, by name. Defaults are
  `claude-opus-5` (Claude Opus 5).
- **Oracle partition is mixed and that is the important part of the brief**
  (table above): AC-1..AC-5 and AC-7 are to be pinned exactly; **AC-6 is the one
  place RED must invent a metric, and it is the one place a negative control is
  mandatory.** Put the partition in the dispatch prompt verbatim.
- **Success condition, falsifiable:** RED's AC-6 test fails by a wide margin
  against a deliberately over-eager detector (threshold 0.0) and passes against
  the fixture, with both numbers recorded in the handoff. If the separation is
  narrow enough that sampling noise decides it, the metric is wrong and the
  criterion goes back to the PO.
- **Verdict:** *to be recorded.*

## Out of scope

Reading order (MT-009). Furigana merging (MT-008) — this story emits ruby as its
own region and MT-008 folds it in. OCR. Any change to the model itself. Panel
segmentation.

## Test plan

## Handoff: RED -> GREEN

## Gate results

## Notes

### Inherited from MT-002 (PO-1) — this story owns three things MT-001 assigned to MT-002

Recorded here on 2026-09-12 by lead-po, during MT-002, because the next agent to
pick this story starts with an empty context and will not otherwise know.

MT-001's bootstrap left three comments in the tree saying MT-002 would do these.
MT-002 is a document-only spike whose phase path (PLANNED → REVIEW → DONE) does
not permit a `test` write at all, so it could not. They are this story's:

1. **Write the first test under `tests/integration/`.** AC-7 already is that
   test. `tests/integration/README.md` explains why it must live there and not
   in `tests/core/` beside the fakes.
2. **Remove the `waiver | integration` line** from
   `.claude/harness/project.conf` once AC-7 collects. While the directory is
   empty the gate exits 5 and the waiver is what stops it WARNing forever; the
   moment a test lands, the waiver would hide a real failure. `gates.sh` says so
   itself when a waived gate passes.
3. **Add the inference dependency to `pyproject.toml`**, in the form
   `docs/wiki/audits/MT-002-model-runtime.md` §`## Decided` gives. This is the
   first story that imports it — `stack.md` forbids pinning a dependency nothing
   imports, which is why MT-002 recorded the lines rather than adding them.
   Adding it should make the `discovery | providers` line in `project.conf`
   start passing; check `bash scripts/doctor.sh` after `uv sync`.

Expect the `build` gate to get materially slower with (3) in place — PyInstaller
starts freezing the inference runtime. MT-001 measured 34 s with PySide6 alone.
If it crosses anything, that is a real finding for `## Gate probes`, not a
reason to weaken the gate.

### Open licence question inherited from MT-002 — settle before this story ships

> **NARROWED BY MT-030, 2026-09-12.** Read
> `docs/wiki/audits/MT-030-detector-licence.md` — it is the current statement of
> this question and supersedes the paragraph below where the two disagree.
> Corrected here, in this story's own body, rather than only in the audits,
> because this note is what a reader of MT-007 hits first.
>
> **Two things resolved.** *(1)* The Manga109-s objection is **void** — Manga109-s
> is the 87-volume subset published expressly for commercial use, and its terms
> permit commercial use of models trained on it, subject to attribution (MT-024
> owns that). The academic-only restriction belongs to the full Manga109, and
> the incumbent's README names the subset. Terms quoted verbatim in MT-030's
> audit, independently re-fetched. *(2)* **The `## Contract` model identity does
> not change.** MT-030 measured nine candidates and found no permissively
> licensed detector with an ONNX export that clears its pinned floor, so the
> incumbent stands at the hash MT-002 recorded. Write AC-7's fixture against it.
>
> **What is still open, and is still the user's:** whether to ship GPL-3.0
> knowingly or commission a detector. That does not block this story's tests —
> it blocks *shipping*, at MT-024.
>
> **One thing MT-030 found that this story should read before writing AC-6.**
> The incumbent's box head covers **0.000** of all six annotated SFX areas on
> `012.jpg` while its mask covers 27–59% — so the split AC-6 is built on is real
> and now has numbers. But **no permissive candidate has a box head at all**
> (confirmed against every downloaded graph's outputs). If the detector ever
> changes, AC-6 loses its mechanism and MT-019 loses `seg ∩ accepted boxes`.

MT-002 escalated it to the user and the **GPL-3.0 half is not resolved**. Full
detail in `docs/wiki/audits/MT-030-detector-licence.md`; MT-002's own
§ *Licence risk* carries a superseded-by block pointing at the same place.

`dmMaze/comic-text-detector` is **GPL-3.0** [measured — the repo's `LICENSE`].
The ONNX re-upload `mayocream/comic-text-detector-onnx` that MT-002 measured
declares `apache-2.0`, but that is a third party's metadata on a derivative of a
GPL-3.0 project and does not obviously relicense anything. Upstream also states
the model was trained on ~13k images, **1/3 from Manga109-s** — which MT-030
established is the *commercial-use* subset, not the academic-only one. ~~whose
terms restrict use to academic research~~ was wrong and is retracted.

This app is *distributed*, so the GPL-3.0 question is real. The technical result
is unaffected either way: MT-002 measured that the ONNX runtime path works, and
that holds for whatever detector is chosen.

Treat the LaMa licence (MT-019) the same way: MT-002 verified *repository
metadata only* and explicitly flagged the `big-lama` checkpoint provenance as
untraced.
