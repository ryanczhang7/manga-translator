---
id: MT-043
title: The manifest audit stops counting optional gates as required
slug: the-manifest-audit-stops-counting-option
epic: EPIC-01
type: fix
status: todo
phase: PLANNED
branch: story/MT-043-the-manifest-audit-stops-counting-option
depends_on: []      # nothing blocks this. It SHOULD land before MT-040 - see ## Notes PO-5, which also says why that ordering is stated rather than enforced
required_gates: []  # deliberately empty; no gate in this repository judges the harness. See ## Notes PO-4
---

## Context

**Filed 2026-09-21 by the Lead PO**, out of a defect the orchestrator found and
reproduced while closing [[MT-039]]. Same lineage of corrections to the
machinery [[MT-001]] built as [[MT-031]]..[[MT-034]], [[MT-037]] and
[[MT-039]]..[[MT-042]].

`bash scripts/gates.sh --audit` ends every run in this repository with:

```
WARN mutation     no evidence line; a vacuous pass would go unnoticed
...
1 required gate(s) have no evidence line. Add one per gate:
  evidence | <id> | <regex proving the tool did work>
Use `evidence | <id> | -` only where no such output exists, and say why in the story.

Manifest audit passed.
```
`[MEASURED — 2026-09-21, orchestrator, full --audit run captured to a file, 210 s]`

**`mutation` is `optional`, and it is the only gate in the manifest with no
evidence line.** All six required gates have one:

| gate | required? | has `evidence`? |
|---|---|---|
| `format` | optional | yes |
| `lint` | **required** | yes |
| `typecheck` | **required** | yes |
| `unit` | **required** | yes |
| `coverage` | **required** | yes |
| `coverage-core` | **required** | yes |
| `integration` | optional | yes |
| `build` | **required** | yes |
| `mutation` | optional | **no** |

`[project.conf:284-325 and 599; read 2026-09-21]`

So the sentence is false on its face: zero required gates have no evidence
line, and the audit says one does.

### The cause: two paths through one counter, guarded differently

`noevidence` is one variable (`scripts/gates.sh:277`), incremented from two
places and printed by two summaries. The normal run path guards the increment
by `req`; the `--audit` path does not.

**Normal run — `scripts/gates.sh:525-527`:**

```bash
elif [ "$exp" = "<none>" ] && [ "$BOOTSTRAPPED" = "yes" ] && [ "$req" = "required" ]; then
  results="$results\nPASS         $id (${dur}s) -- no evidence line: a vacuous pass would go unnoticed"
  noevidence=$((noevidence+1))
```

**`--audit` — `scripts/gates.sh:436-439`:**

```bash
if [ "$exp" = "<none>" ]; then
  printf 'WARN %-12s no evidence line; a vacuous pass would go unnoticed\n' "$id"
  noevidence=$((noevidence+1)); continue
fi
```

— **no `req` test at all.** Then `scripts/gates.sh:640-644`:

```bash
if [ "$noevidence" -gt 0 ]; then
  printf '\n%d required gate(s) have no evidence line. Add one per gate:\n' "$noevidence"
```

over the unguarded count. The run path's own summary
(`scripts/gates.sh:758-759`) uses the same words — *"%d required gate(s) ran
without an evidence line"* — and there they are true, because there the counter
was guarded. One variable, one wording, two population rules. That asymmetry is
the defect.

**The per-gate `WARN` line is accurate and useful and is not the problem.** It
names the gate, so a reader can see for themselves that it is optional. It is
also the *only* place an optional gate's missing evidence line is surfaced at
all: on a normal run, `gates.sh:525`'s `req` guard means an optional gate with
no evidence line prints a bare `PASS` with no annotation. The `WARN` stays.

### Reproduced in one second, in a fixture

`make_project_fixture` + `write_conf` from `.claude/tests/_lib.sh`, one required
gate with an evidence line and one optional gate without:

```
gate     | unit     | required | . | printf 'Tests  47 passed (47)\n'
evidence | unit     | Tests +[1-9][0-9]* passed
gate     | mutation | optional | . | printf 'mutants: 3\n'
waiver   | mutation | mutmut is not installed; stack.md s6
```

```
ok   unit         evidence: Tests +[1-9][0-9]* passed
WARN mutation     no evidence line; a vacuous pass would go unnoticed
...
1 required gate(s) have no evidence line. Add one per gate:
...
Manifest audit passed.
=== exit: 0 ===
```
`[MEASURED — 2026-09-21, Lead PO, fixture run, ~1 s]`

**This is the whole reproduction, and it costs a second.** No test in this story
needs to shell out to a real `--audit` (86 s on CI, 210 s here). See PO-6.

### Why this is worth a story rather than a comment

**1. It cries wolf.** An audit whose *standing* output claims a required gate is
unguarded, when none is, is an audit people learn to skim. This lineage exists
because vacuous passes hide in output nobody reads — `project.conf`'s header
(*"exit 0 is not proof of work — it is the absence of a complaint, and a tool
with nothing to do does not complain"*) and [[MT-037]], which removed the last
way a gate here could report PASS having done nothing.

**2. It masks the exact thing `--audit` exists to catch — and this is the
load-bearing reason.** `.github/workflows/gates.yml:58-62` states the step's
job:

> ```
> # Catches a gate whose command was emptied, whose cwd was moved away, or
> # whose evidence line was dropped - none of which the gate run itself can
> # report, because a gate that no longer does anything still exits 0.
> - name: Audit the gate manifest
>   run: bash scripts/gates.sh --audit
> ```

*"whose evidence line was dropped"* is signalled by **this sentence and nothing
else**: the count is the signal. And the count is already non-zero for a benign
reason. Reproduced — same fixture, `evidence | unit | ...` deleted so that a
**required** gate has genuinely lost its line:

```
2 required gate(s) have no evidence line. Add one per gate:
  evidence | <id> | <regex proving the tool did work>
Use `evidence | <id> | -` only where no such output exists, and say why in the story.

Manifest audit passed.
```
`[MEASURED — 2026-09-21, Lead PO, fixture run]`

1 → 2, and **the sentence reads identically**. Note also `Manifest audit
passed.` and exit 0: this summary never fails the run, so the number is the
entire signal, and it is a number a reader has been trained to expect to be 1.

## Acceptance criteria

*Oracle partition: all four are **mechanical**. Every one is an exact string in
`gates.sh`'s output against a fixture manifest whose contents the test writes.
Pin them exactly; invent nothing. There is no metric here and therefore no
negative-control-on-a-statistic — but AC-3 and AC-4 are negative controls on the
change itself, and they are the two that stop this fix from silencing a real
signal.*

- **AC-1** — Given a manifest in which every **required** gate has an `evidence`
  line and at least one **optional** gate has a command but none, when
  `bash scripts/gates.sh --audit` runs, then the output does **not** contain the
  string `required gate(s) have no evidence line`, and the audit still exits 0.

- **AC-2** — Given that same manifest, when `--audit` runs, then the per-gate
  line for the optional gate is still printed, unchanged. The authoritative form
  is the existing `printf` at `gates.sh:437` —
  `printf 'WARN %-12s no evidence line; a vacuous pass would go unnoticed\n' "$id"`
  — which for a gate named `mutation` renders as
  `WARN mutation     no evidence line; a vacuous pass would go unnoticed`
  (`%-12s`, so `mutation` is followed by five spaces; do not hand-count padding,
  assert the substring `WARN mutation` and the substring
  `no evidence line; a vacuous pass would go unnoticed` on one line, as the
  existing assertions in `gates.test.sh` do).
  *This AC is why the fix is not "delete the WARN": the one place the condition
  is reported must survive.*

- **AC-3** — Given a manifest in which a **required** gate has a command and no
  `evidence` line, when `--audit` runs, then the output contains
  `1 required gate(s) have no evidence line.` — and given a manifest in which
  **one required and one optional** gate both lack one, it contains
  `1 required gate(s) have no evidence line.`, not `2`.
  *This is the masking defect stated as a test: the count must move only for
  required gates. It is the negative control on AC-1 — a fix that simply removed
  the summary block would pass AC-1 and fail this.*

- **AC-4** — Given a manifest with `BOOTSTRAPPED=no` and a **required** gate
  that has a command and no `evidence` line, when `--audit` runs, then the
  output still contains `1 required gate(s) have no evidence line.`
  *Unchanged behaviour, pinned so the fix cannot drift into it. The run path's
  guard is `BOOTSTRAPPED=yes && req=required`; only the `req` half is being
  copied. Measured 2026-09-21: today this case counts. See `## Contract`,
  "Why `req` and not `BOOTSTRAPPED`".*

## Contract

### The change

**File:** `scripts/gates.sh`. **Lines:** the `if [ "$exp" = "<none>" ]` block at
`436-439` in the `--audit` branch.

**Direction chosen: guard the `--audit` counter by `req`, matching the run
path.** The increment becomes conditional on `[ "$req" = "required" ]`; the
`printf` of the per-gate `WARN` and the `continue` do not move. In shape:

```bash
if [ "$exp" = "<none>" ]; then
  printf 'WARN %-12s no evidence line; a vacuous pass would go unnoticed\n' "$id"
  [ "$req" = "required" ] && noevidence=$((noevidence+1))
  continue
fi
```

GREEN owns the exact form (a `&&` one-liner under `set -uo pipefail` where the
last command's status can leak is a real hazard here — `gates.sh` does not use
`set -e`, but the block ends in `continue`, so the status is discarded; an
`if`/`fi` is equally acceptable and may read better).

**The summary text at `scripts/gates.sh:640-644` does not change.** Under this
direction the existing wording becomes *true*, so there is no new string to pin
and nothing downstream has to learn a new sentence:

```
%d required gate(s) have no evidence line. Add one per gate:
  evidence | <id> | <regex proving the tool did work>
Use `evidence | <id> | -` only where no such output exists, and say why in the story.
```

### Why this direction and not the other

The dispatch named two defensible fixes. They are not equivalent.

**Rejected: keep counting every gate and reword the summary to stop claiming
"required".** It fixes the sentence and leaves the defect, which is that
`noevidence` means *"required gates missing evidence"* in one path of this
script and *"any gate missing evidence"* in the other. Rewording commits to that
divergence permanently: the two summary blocks (`:640` and `:758`) would then
have to say different things about the same variable, and the next reader
comparing the paths finds the asymmetry still there with a comment over it.
It also does not fix the masking in AC-3 — a reworded sentence still reads
identically at 1 and at 2. To fix the masking under this direction you would
have to print **two** counts, which is more output on every audit for a benign
condition: the cry-wolf failure mode one notch quieter.

**Chosen: guard by `req`.** One decision, taken once, in the path that did not
take it; the two paths then agree; the existing wording becomes true; the
masking goes away because the count moves only for the thing the count claims to
be about. Diff is one line of `gates.sh`.

**What is lost, named honestly.** An optional gate with no evidence line stops
being *aggregated*. It is not silenced — AC-2 keeps the per-gate `WARN`, which
is one line among roughly ten in `--audit`'s output and names the gate. The
summary's job is to make a count actionable, and a count that mixes two
severities is less actionable than either.

### Why `req` and not `BOOTSTRAPPED`

The run path guards by both. Only `req` is copied, deliberately.

`req` is a property of the **manifest** — this gate is or is not binding — and
the audit's job is to judge the manifest. `BOOTSTRAPPED` is a property of the
project's **stage**, and `--audit` is precisely the tool a bootstrap story uses
to check a manifest it is still filling in; telling it nothing about missing
evidence lines would be a regression. The run path's `BOOTSTRAPPED` guard exists
because a non-bootstrapped run's summary is already full of `UNCONFIGURED`
noise; the audit has no such problem, and the `-z "$cmd"` branch above
(`gates.sh:425-432`) already handles the unconfigured case with its own
`BOOTSTRAPPED` test before this block is reached. AC-4 pins the behaviour so the
fix cannot drift into it.

### Baseline measurements this story may read out rather than re-derive

| fact | value | source |
|---|---|---|
| `mutation` is the only gate with no `evidence` line | — | `project.conf:284-325`, read 2026-09-21 |
| `mutation` is `optional` | — | `project.conf:304` |
| all six required gates have an `evidence` line | — | `project.conf:318-325` |
| full `--audit`, this machine | 210 s | orchestrator, 2026-09-21 |
| full `--audit`, CI | 86 s | orchestrator, 2026-09-21 |
| fixture `--audit` | ~1 s | Lead PO, 2026-09-21 |
| `floor \| gates \| 161` is a **floor**, not an equality | — | `.claude/tests/floors.conf`, MT-039 AC-3 |

### The regression test

**Suite:** `.claude/tests/gates.test.sh` (998 lines today). Add a `describe`
block; do not create a new suite.

**Use the existing fixture helpers.** `make_project_fixture` and `write_conf`
from `.claude/tests/_lib.sh`, and the local `gates()` wrapper at
`gates.test.sh:13`, exactly as the twenty-odd existing `--audit` assertions do
(`gates.test.sh:83, 222, 333, 359, 472, 936, 975`). Every gate command is a
`printf`. **No test in this story shells out to a real `--audit`** — that is
86 s on CI and 210 s here, per run, and `gates.test.sh` already runs `gates.sh`
eleven times.

**The floor needs no edit.** `.claude/tests/floors.conf` records
`floor | gates | 161`, and MT-039 AC-3 made it a floor rather than an equality
precisely so that a story which **adds** assertions need not touch that file. A
story that *removed* assertions would have to. This one only adds, so
`floors.conf` is not in this story's diff. If RED finds it must edit that file,
that is a signal something is wrong — stop and say so.

### Oracle partition

All four criteria are **mechanical**: exact strings from `gates.sh`, against
manifests the test writes. Pin them exactly. Nothing here is oracle-free and
nothing needs calibrating.

### Callers of a changed signature

**None.** No function signature changes. `noevidence` is a local counter in
`scripts/gates.sh` and appears nowhere else:

```
$ grep -rn 'noevidence' --include='*.sh' .
scripts/gates.sh:277,438,490,499,502,527,531,640,758,791
```
`[MEASURED — 2026-09-21, Lead PO]`

`gates.sh:791` writes it into `.claude/state/last-gate-run`, from the **normal
run** path only (the `--audit` branch exits at `:647` or `:649`, well before
it), so the stamp is unaffected by this change. GREEN should confirm that rather than assume it.

## Model guidance

**This dispatch (PLANNED, Lead PO).** `.claude/agents/lead-po.md` declares
`model: opus`; the session model is **Opus 5 (`claude-opus-5`)**; **no override
was passed**. Resolved model: **`claude-opus-5`**.

**RED and GREEN:** no deviation proposed. Stay on each agent's declared `model:`
and record what it resolved to, by name, here — never the word "default"
(`rules.md`, "The model each agent runs on"). This story is four mechanical
string assertions and a one-line change; it is not a place where a model
comparison would teach anything, and running one here would add another
uncontrolled data point to a question that already has exactly one honest
measurement in this repository — and that one pointed at the **brief**, not the
model (`story-authoring`, "Brief RED by oracle": a partitioned RED brief on the
weaker model produced 69x separation against 10x). No success condition is
stated because no experiment is being run; there is therefore no verdict to
record, and that is the intended outcome rather than an omission.

**How to brief RED:** hand it `## Contract` whole. The four criteria are
mechanical, so precision beats invention — the strings are already written
down. The one thing RED must not do is reach for a real `--audit`.

## Out of scope

- **Adding an `evidence` line to `mutation`.** See PO-1 — this is the trap.
- **Making `--audit` *fail* (exit non-zero) when a required gate has no evidence
  line.** It does not today: measured 2026-09-21, a fixture with a required gate
  missing its line printed the count and then `Manifest audit passed.`, exit 0.
  That may well be wrong, but it is a different defect — about the audit's
  *severity*, not its *accuracy* — and MT-043's claim is that the audit stops
  lying, not that it gets stricter. Changing both at once would make it
  impossible to tell which change any resulting CI failure came from. File it
  separately if it is wanted.
- Any other `noevidence` call site: `:490`, `:499`, `:502`, `:527` are the
  normal run path and are correct.
- `trim()`, the parse loop, or anything else MT-040 owns. PO-5.
- Adding a `selftest` gate. PO-4.

## Test plan

<!-- Filled by the Test Developer during RED. -->

## Handoff: RED -> GREEN

<!-- Filled by the Test Developer at the end of RED. -->

## Gate results

<!-- Written by scripts/gates.sh itself. -->

## Notes

**PO-1 — Should `mutation` simply get an `evidence` line? Not in this story, and
this is the cheapest-looking option, which is why it is written down.**

It would make the symptom disappear. Measured 2026-09-21, same fixture, with
`evidence | mutation | -` added:

```
ok   mutation     (liveness declared unassertable)
...
Manifest audit passed.
```

The `WARN` and the summary both vanish. One line of `project.conf`, no code, no
test. And the defect is untouched: the next optional gate added without an
evidence line re-creates it exactly, and until then `noevidence` still means two
different things in two paths of one script.

It is also, today, a **false declaration**. `evidence | <id> | -` means *"no
such output exists"* — `project.conf`'s own instruction is *"Use `evidence |
<id> | -` only where no such output exists, and say why in the story."* `mutmut`
produces perfectly assertable output; it is not installed (`waiver | mutation |
mutmut is not installed and not configured until the suite is real; stack.md
§6`, `project.conf:599`). Writing `-` would record a falsehood in order to
silence a miscount. The honest state of `mutation` right now is: waived,
uninstalled, no evidence line yet — and that state should produce one accurate
`WARN` line and no more, which is exactly what this story's fix leaves.

**When `mutation` becomes real** — when `stack.md` §6's `uv run mutmut run` is
actually installed and the suite is worth mutating — it must arrive with a
genuine `evidence` regex, watched to fail, in that story's `## Gate probes`. Not
this one.

**PO-2 — The phase lock will enforce nothing in this story.** Every path it
touches classifies `harness`, which every phase permits:

```
$ bash -c '. .claude/hooks/lib.sh; for p in scripts/gates.sh \
    .claude/tests/gates.test.sh .claude/harness/project.conf; do
    printf "%-32s %s\n" "$p" "$(classify "$p")"; done'
scripts/gates.sh                 harness
.claude/tests/gates.test.sh      harness
.claude/harness/project.conf     harness
```
`[MEASURED — 2026-09-21, Lead PO]`

So RED can write `scripts/gates.sh` and GREEN can write
`.claude/tests/gates.test.sh` and nothing will stop either. **The discipline is
honoured by the agents, not enforced by the hook.** Same trap as MT-039..MT-042;
stated here because a fresh context will not have read those.

**PO-3 — Consequently, `## Handoff` must carry the failure output verbatim.**
There is no lock to make RED's failure real, and the fix is one line. RED runs
`bash scripts/selftest.sh gates` (the single suite, not the 43-minute full run)
and pastes what it shows. A test written against machinery that *already exists*
— which `gates.sh --audit` does — passes on arrival if it is written wrong, and
`rules.md`'s non-negotiable applies: these four assertions must be *watched* to
fail. They will, because the behaviour they assert does not exist yet; AC-2 and
AC-4 are the exceptions, since they pin behaviour that is already correct, and
each of those must be earned by a mutation through `bash scripts/mutate.sh`,
with the output pasted. Do not skip that for AC-2 or AC-4 on the grounds that
they are "just" regression pins — they are the two that would pass while
asserting nothing.

**PO-4 — `required_gates: []` is a decision, not an oversight.** No gate in
`project.conf` reads `scripts/**` or `.claude/tests/**`; the eight gates judge
the product. The judge of this story is CI's **`Harness self-test`** step
(`.github/workflows/gates.yml`), which runs `scripts/selftest.sh`, which runs
`gates.test.sh`, which is where this story's regression test lives. **Do not add
a `selftest` gate** — [[MT-039]] declined exactly that with arithmetic, and
nothing about MT-043 changes the sums. `story-authoring`'s rule ("name, before
RED, the required gate that would fail if this story's artifact broke") is
answered as: none, by design, and the CI step is the answer instead.

**PO-5 — Ordering against [[MT-040]]: MT-043 should land FIRST.**

MT-040 rewrites `gates.sh`'s manifest parse loop (`trim()` at `:89` and its
twelve call sites) and is **held to byte-identical output and unchanged
verdicts** as acceptance criteria — that is how a pure-performance story proves
it changed nothing. MT-043 **deliberately changes `--audit`'s output**. They
touch one file.

Three reasons for MT-043 first:

1. **MT-040's baseline would go stale.** MT-040 captures `--list` and `--audit`
   output, applies its change, and diffs. If MT-043 merges afterwards, MT-040's
   recorded baseline stops matching the script, and anyone re-running that
   comparison sees a diff MT-040 said would not exist. This lineage keeps filing
   stories about exactly that kind of quiet contradiction; adding one to it
   while closing one would be poor.
2. **Correctness before performance**, the ordinary order. MT-043 is one line
   plus test assertions; MT-040 is a restructure. Landing the small correction
   first means MT-040 never has to explain a wrong line inside its own evidence.
3. **The conflict surface is small either way, so the cost of choosing is
   low.** MT-043 touches `gates.sh:436-439` (the emit block in the `--audit`
   branch) and reads `:640-644`. MT-040 touches `:89` and the twelve `trim()`
   call sites in the parse loops. Different hunks. Rebasing MT-040 over MT-043
   is cheap; the reverse is cheap too — what is *not* cheap is a stale baseline.

**This ordering is stated, not enforced.** `depends_on` runs the other way in
this harness: it names what must be DONE before *this* story starts, so MT-043
cannot express "before MT-040" in its own frontmatter. Enforcing it means adding
`MT-043` to **MT-040's** `depends_on` — a one-line change to
`docs/backlog/stories/MT-040.md`, which is still at PLANNED and therefore legal
to edit. The Lead PO did **not** make that change: this dispatch scoped the
write to MT-043 and EPIC-01, and silently editing a third story's frontmatter is
not the Lead PO's call to take unasked. It is handed to the orchestrator as a
decision. If it is declined, the ordering is folklore and MT-040 should re-take
its baseline after MT-043 merges.

**PO-6 — Cost, because it is this lineage's subject.** `--audit` is **86 s** on
CI and **210 s** on this machine. Any test that shells out to a full `--audit`
run pays that every time the self-test runs, and the self-test is already 63% of
CI's `gates` job (MT-039/MT-040 dossier). The entire defect reproduces in a
fixture in about a second, shown above. **Prefer the fixture.** If RED believes
a real `--audit` is needed for any criterion, that is a `## Contract` amendment
with a reason, not a default.

**PO-7 — Why this is `type: fix` and not `chore`.** Unlike MT-031..MT-034 and
MT-039..MT-042, which change how the harness behaves or what it costs, this is a
defect in shipped machinery: the audit prints a false statement on every run. So
the criteria reproduce the bug (AC-1, AC-3) and the test that captures it becomes
the permanent regression test — `story-authoring`, Types: *"A bug is never fixed
without a test that reproduces it first. That test is the whole value of the
fix."* Same shape as [[MT-037]], the other `fix` in this epic.
