---
id: MT-037
title: A gate that skipped its work stops reporting PASS
slug: a-gate-that-skipped-its-work-stops-repor
epic: EPIC-01
type: fix
status: in-review
phase: REVIEW
branch: story/MT-037-a-gate-that-skipped-its-work-stops-repor
depends_on: []      # nothing blocks this; see ## Notes PO-4 for MT-011's landing order
required_gates: []  # deliberately empty; see ## Notes PO-5 for why escalating `integration` here would be self-defeating
---

## Context

`integration` reports `PASS` on a machine where it ran one of its twenty-six
tests. Reproduced by the orchestrator at PLANNED, in this very worktree, against
the manifest as it stands (PO-1 below, pasted in full):

```
=== gate: integration (optional) ===
uv run pytest -q tests/integration -m "gpu or network"
sssssssssssssssss.ssssssss                                               [100%]
1 passed, 25 skipped in 2.71s

--- gate summary ---
PASS         integration (9s, observed 1)
```

The page scans and model weights the integration suite needs live under
`spikes/**`, which `.gitignore` covers, so every one of those tests calls
`pytest.skip` when they are absent. The gate's evidence line is
`evidence | integration | [1-9][0-9]* passed`, and **`1 passed` satisfies it**.
There is no `floor` line. So the gate reports `PASS` and is, on that machine,
precisely the vacuous gate the `evidence` mechanism was added to catch — see the
`project.conf` header ("exit 0 is not proof of work — it is the absence of a
complaint, and a tool with nothing to do does not complain") and the
`quality-gates` skill's "The vacuous pass".

**This is a defect in the gate, not a gap in a feature — hence `type: fix`.** A
mechanism whose stated job is distinguishing a gate that worked from one that
only exited 0 currently cannot distinguish them for this gate. AC-1 reproduces
it and its test becomes the permanent regression test.

### Why it is consequential now, and where the harm actually is

`integration` has had this property since MT-001. **MT-036 is the first story to
put it in `required_gates`**, making it binding — the first story, therefore,
whose *binding* gate could report PASS without exercising the artifact it was
made binding for. MT-011 already carries `required_gates: [integration]` and is
next.

The orchestrator's probes (PO-2, PO-3) moved where the harm is:

- **It is not principally a CI problem.** On CI there is no active story at all:
  `gates.sh` reads the story from `.claude/state/current-story.env`, which
  `.gitignore` excludes from every commit, so `required_gates` never escalates
  anything on a runner. CI's `integration` line has never been evidence for any
  story, and never could be.
- **It is a problem on the machine that records the evidence.** A git worktree
  does not carry gitignored files. This harness dispatches its work into
  `.claude/worktrees/**`. So an agent running `bash scripts/gates.sh` for a story
  that escalates `integration` — from a worktree, which is the normal case —
  gets `1 passed, 25 skipped`, records `PASS integration (observed 1)` into
  `## Gate results`, and `check-boundaries.sh` then reads that line and agrees
  that *"story-required gate 'integration' passed in the recorded run"*. Every
  mechanism in the chain behaves exactly as designed and the chain certifies
  nothing.

### What is already fine, and is not re-litigated here

**MT-036's own evidence stands.** Its `## Gate results` records
`PASS integration (7s, observed 26)` from a full local run in a checkout that
carried `spikes/`, stamped against tree `e777c915…`, and `check-boundaries.sh`
verified that stamp against the merged tree. This story does not re-verify
MT-036, re-measure anything MT-036 measured, or touch MT-036's file.

### The required gate that would fail if this story's artifact broke

**None of the eight.** Everything this story writes classifies as `harness`
(verified: `scripts/gates.sh`, `.claude/harness/project.conf` and
`.claude/tests/gates.test.sh` all classify `harness`), so no `covers` line
governs it and the `changes:` check never sees it. Its criteria are checked by
`bash scripts/selftest.sh`, which is a **CI step and not a gate** — it runs in
`.github/workflows/gates.yml` ("Harness self-test") and in `scripts/ci-local.sh`,
and it must stay green. `required_gates` cannot fix that, because no gate runs
the harness self-test; making one is named in `## Out of scope` as a follow-up.

## Acceptance criteria

<!-- AC-1 is the bug reproduction and is FALSE against the manifest as this
     story begins. AC-2 and AC-5 are its negative controls, and neither is
     optional: without AC-2 the fix could make `integration` always fail, and
     without AC-5 the new `skipped-when` line could excuse every shortfall. -->

- **AC-1** — Given `integration`'s configuration in
  `.claude/harness/project.conf`, when its command exits 0 having run 1 of its 26
  collected tests (log: `1 passed, 25 skipped in 0.46s`), then `gates.sh` reports
  **something other than `PASS`** for it. *Against the manifest at the start of
  this story this is false: it reports `PASS integration (9s, observed 1)`, pasted
  in `## Context`. That output is the defect; the test that pins this criterion is
  the permanent regression test.*
- **AC-2** — *(negative control for AC-1)* Given the same configuration, when the
  command exits 0 on a machine that has the spike data (log: `26 passed in 7.1s`),
  then `gates.sh` reports **`PASS`**, and the summary names both the observed
  count and the floor (`observed 26, floor 26`). *A fix that satisfies AC-1 by
  making `integration` never pass fails here.*
- **AC-3** — Given a story whose frontmatter escalates a gate
  (`required_gates: [<id>]`) and a run of that gate that exits 0, falls below its
  floor, and whose log matches the gate's declared skip pattern, then the gate is
  reported **`BLOCKED`**, the run **exits 3**, and the stamped
  `## Gate results` records `result: blocked` — so there is **no `PASS <id>` line**
  for `check-boundaries.sh`'s *"story-required gate passed in the recorded run"*
  check to read.
- **AC-4** — Given **no** active story, so the gate is optional (the condition on
  CI and on any checkout between stories), and the same short, skip-matching run,
  then the gate is reported **`KNOWN`** with its reason inline, the run **exits
  0**, and the recorded result is still `pass`. *It is specifically not `WARN`: a
  WARN on every PR for the life of the project is the outcome MT-007 and MT-008
  declined a floor to avoid, and `quality-gates` reserves WARN for something that
  changed.*
- **AC-5** — *(negative control for AC-3 and AC-4)* Given the same gate and floor,
  when the command exits 0 with a count below the floor whose log does **not**
  match the skip pattern (log: `3 passed in 0.4s` — a suite that genuinely
  shrank), then the gate is reported exactly as a floor shortfall is today:
  **`FAIL`** when required, **`WARN`** when optional. *The pattern classifies a
  shortfall; it must never excuse one.*
- **AC-6** — Given a `skipped-when` line that (a) names no configured gate, or
  (b) carries no pattern, or (c) names a gate that has no `floor`, when
  `bash scripts/gates.sh --audit` runs, then the audit **fails** and says which of
  the three it is. *Negative control: a well-formed `skipped-when` on a floored
  gate passes the audit and is printed with it.* Each of the three can never
  protect anything: (a) fires on nothing, (b) would match every log and turn every
  shortfall into a block, (c) has no measurement to classify.
- **AC-7** — Given a gate carrying a `skipped-when` line, when
  `bash scripts/gates.sh --list` runs, then the pattern is printed under that
  gate, beside its `evidence`, `floor` and `blocked-when` lines.

## Contract

<!-- Written by the Lead PO before RED. RED may amend a block in place with a
     reason; GREEN builds what the amended block says. -->

### C-0 — Oracle partition of the criteria

| AC | Partition | What that means for RED |
|---|---|---|
| AC-1, AC-2 | **mechanical** | Pin exactly. The inputs are fixed log strings and the outputs are fixed summary tokens. Do not invent tolerance. |
| AC-3, AC-4, AC-5 | **mechanical** | The verdict token (`BLOCKED`/`KNOWN`/`WARN`/`FAIL`), the exit code and the recorded `result:` line are all exact. |
| AC-6, AC-7 | **mechanical** | Exact audit/list strings. |
| — | **settled** | The floor value `26` and the observed-1 behaviour are read out of PO-1 and MT-036's stamped record. Do not re-derive them and do not re-run the GPU suite to confirm them. |

There is no oracle-free criterion in this story. Nothing here needs a metric
invented.

### C-1 — The new `project.conf` table kind

Exact format line, to be documented in the `project.conf` header in the style of
the existing kinds:

```
skipped-when | <gate id> | <extended regex>
```

Parsed exactly as `evidence` and `blocked-when` are — `cut -d'|' -f3-`, so a
regex containing `|` (alternation) survives the split — and stored in a table
alongside `EVIDENCE`, `FLOORS`, `BLOCKEDWHEN`.

**Semantics, in one sentence:** *when a gate exits 0, matches its evidence
regex, and comes in below its floor, this pattern says whether the shortfall is
the environment declining to supply the work (report it as a block) or the suite
having lost work (report it as a floor shortfall, unchanged).*

It is consulted **only** in that one place:

- never when `rc != 0` — that path is the existing fail/`blocked-when` logic and
  is untouched;
- never when `observed >= floor` — a passing gate stays a pass even if some of
  its tests skipped;
- never when there is no floor — see the audit rule below.

### C-2 — `scripts/gates.sh`

| Where | Change |
|---|---|
| table parser (~L87–130) | add `skipped-when` to the `case "$kind" in evidence\|waiver\|floor\|slow\|ci-factor\|covers\|blocked-when)` list and to the store `case`; one new accumulator variable. |
| outcome logic (~L470–480) | inside the existing `[ -n "$floor" ]` branch, after `observed -lt floor` is established: if the gate has a `skipped-when` pattern and `clean_log` matches it, set a **new internal outcome** (suggested: `environment`) instead of `noevidence`, with a `why` string. Otherwise unchanged. |
| the outcome `case` (~L487–531) | one new arm for that outcome: `required` (including story-escalated) → `BLOCKED …`, `blocked=$((blocked+1))`; `optional` → `KNOWN …`, `known=$((known+1))`. An optional gate that also carries a `waiver` keeps the existing waiver wording — the waiver is the broader declaration. |
| `--list` (~L317–333) | print `skipped-when: <pattern>` for the gate, beside the existing `blocked-when:` line. |
| `--audit` (~L537–575) | three refusals, each following the shape of the existing `blocked-when` and `covers` audits: names no configured gate; has no pattern; **names a gate with no `floor` line** (nothing measures the shortfall, so the line can never fire and sits in the manifest looking like protection — the same reasoning as the existing "a floor needs an evidence regex to measure"). |

**Exit codes are the existing ones and must not move:** a required block →
exit 3 via the existing `blocked` counter; `KNOWN` → exit 0 and `RESULT=pass`.
No new exit code and no new stamp field.

**Wording.** RED chooses the exact `why` string and the exact summary line and
**pins them in `## Handoff`**; GREEN matches what RED pinned, because tests are
frozen in GREEN. The string must contain the observed count, the floor, and the
word `skipped`, so that the log answers *why* without anyone opening the gate
log. Suggested, not binding:

```
KNOWN        integration (3s, did 1 units of work, below the floor of 26 in
             project.conf, and 25 were skipped rather than lost: the environment
             did not supply them)
```

### C-3 — `.claude/harness/project.conf`

Two live lines:

```
floor        | integration | 26
skipped-when | integration | [1-9][0-9]* skipped
```

**`skipped` only — not `deselected`.** The distinction is load-bearing and is
what AC-5 protects. A `skipped` count comes from the tests themselves deciding
at call time that their inputs are absent; a `deselected` count comes from the
gate's own marker expression, which is a change to the manifest and therefore a
regression, not an environment. `-m "network"` in place of `-m "gpu or network"`
must still fail. `## Gate probes` P-1 is exactly that probe.

Plus, in the Lead PO's slice and written by the Lead PO (`rules.md` path
ownership: `project.conf` is the Lead PO's, `scripts/gates.sh` is the Feature
Developer's):

1. A header comment block for `skipped-when`, in the style of the existing ones:
   the format line, the rationale, and the rules — *do not add a pattern to make
   a failing gate stop failing*, mirroring the `blocked-when` warning.
2. In that block: **a `skipped-when` block is discharged by supplying what the
   environment lacks — `scripts/doctor.sh`, `docs/wiki/environment.md` — which is
   step 2 of the BLOCKED playbook, not step 3.** The pending-CI path is wrong for
   this one: CI is the *weaker* machine here, and quoting a CI log would certify
   nothing.
3. A `--- Floors ---` entry that **engages with MT-007's and MT-008's stated
   reason for leaving `integration` floor-less** ("on CI, where it is optional and
   the gitignored weights are absent, it collects nothing, and a floor would turn
   that into a permanent WARN") rather than silently overwriting it: the reason
   was correct, the floor alone would have produced exactly that permanent WARN
   (PO-3), and `skipped-when` is what makes the floor safe to add — the CI report
   becomes `KNOWN`, which is a declared non-result, not noise.
4. The number `26` is **settled**: it is the collected count of
   `tests/integration -m "gpu or network"`, observed as `26 passed` in MT-036's
   stamped record and as `1 passed, 25 skipped` in PO-1. Do not re-derive it.

### C-4 — `docs/wiki/environment.md`

One short subsection: the integration suite's inputs live under `spikes/**`,
which `.gitignore` covers; **a git worktree therefore never carries them**, and
this harness dispatches into `.claude/worktrees/**`; run the integration gate
from a checkout that has `spikes/`, or expect `KNOWN`/`BLOCKED` and read it as
"this checkout cannot answer that question". Prose, no AC.

### C-5 — `scripts/check-boundaries.sh`: **no change**

Its existing check at ~L263 —

```
story-required gate '<g>' passed in the recorded run
```

— already greps for `^[[:space:]]*PASS[[:space:]]+<g>` in `## Gate results`.
Under this story a vacuous run records `BLOCKED integration` instead, so that
grep finds nothing and the existing check bites. AC-3 asserts the record shape
this depends on. **Do not edit `check-boundaries.sh`.** If RED believes it must
change, that is an amendment with a reason, not a quiet edit.

### C-6 — Test files

| File | Change |
|---|---|
| `.claude/tests/gates.test.sh` | a new `describe` block covering AC-1 through AC-7 against `make_project_fixture`, with `printf` gate commands as every case in that file already does. AC-3 needs `story "$FIX" T-1 GATES` with `required_gates: [integration]` and `set_phase "$FIX" GATES`; AC-4 needs `set_phase "$FIX" ""`. Both shapes already exist in the file (the `covers` block and the FULL-stamp block). |
| `.claude/tests/boundaries.test.sh` | **no change expected.** C-5 changes nothing there. |
| `.claude/tests/ci-local.test.sh` | **no change expected.** No CI step is added or removed; that suite derives its step list from `.github/workflows/**`. |

`bash scripts/selftest.sh` must be green at the end of every phase. It is the
only thing that runs these tests.

### C-7 — Callers of every changed signature

`gates.sh` exports nothing and is called as a script. Its callers, grep-listed
before dispatch, and why none needs a change:

- `.github/workflows/gates.yml` — `--list`, `--audit`, bare run. New verdicts
  print inside the existing summary; no new exit code.
- `scripts/ci-local.sh` — same steps.
- `.claude/hooks/gate-reminder.sh` — reads `.claude/state/last-gate-run`
  (`RESULT=`, `FULL=`); no new stamp field is introduced.
- `scripts/check-boundaries.sh` — reads `## Gate results` for `result:` and
  `PASS <gate>`; both already understand `pass` and `blocked` (C-5).
- `.claude/tests/gates.test.sh` — the suite under change.

### C-8 — Gate probes owed at GATES

This story changes a gate (`integration` gains a `floor` and a `skipped-when`),
so `## Gate probes` is required. Planned now; the output is pasted in GATES.

- **P-1 — the floor, and that a `deselected` shortfall is not excused.**
  Temporarily narrow the gate command to `-m "network"` in a checkout that *has*
  `spikes/`, run `bash scripts/gates.sh --gate integration`. Expected: a count
  well below 26 with `deselected` rather than `skipped` in the log, so
  `skipped-when` does not match and the gate reports a floor shortfall
  (`WARN`, or `FAIL` with a story escalating it) — **not** `KNOWN`. Revert the
  command.
- **P-2 — `skipped-when`, against the real suite.** In a checkout that has
  `spikes/`, rename it (`git mv` is wrong — it is untracked; a plain directory
  rename to `spikes.probe/`), run the gate, expect `KNOWN integration (…25
  skipped…)` and exit 0; with a story escalating it, expect `BLOCKED` and exit 3.
  Rename back and confirm the gate reports `PASS … observed 26, floor 26` again.
- **P-3 — the audit rules.** Add, one at a time, `skipped-when | integratoin |
  [1-9][0-9]* skipped` (typo), `skipped-when | integration |` (no pattern) and
  `skipped-when | build | [1-9][0-9]* skipped` (no floor); run
  `bash scripts/gates.sh --audit` and paste each refusal. Revert.

P-2 is the one that matters: it is the only probe that breaks the real thing the
new rule guards rather than a fixture.

## Model guidance

**Written before RED. `rules.md`: the Lead PO records the resolved model of
every dispatch, by name, never the word "default".**

- **RED — `test-developer`.** Its definition at `.claude/agents/test-developer.md`
  declares `model: opus`, which on this session resolves to **Opus 5
  (`claude-opus-5`)**. The dispatch prompt must end with: *"state in your report
  which model you actually ran on, and whether you were dispatched with an
  override."* **The orchestrator records the answer here before accepting the
  phase — resolved model: **Opus 5 (`claude-opus-5`)**, reported by the subagent
  in its own report. Dispatched with **no model override**, so the agent
  definition's `model: opus` governed.**
- **GREEN — `feature-developer`**, `model: opus` → Opus 5 (`claude-opus-5`),
  same instruction, same recording obligation.
- **Do not lower either.** `rules.md` is explicit that the failure mode of a
  weaker model in GREEN or GATES is the one this harness exists to prevent —
  reaching green by weakening a test — and this story's whole subject is a
  weakened liveness check. A model that "fixes" AC-1 by deleting the evidence
  line would be the joke writing itself.
- **How to brief RED.** Give it C-0's partition and say plainly that **every
  criterion here is mechanical**: fixed log strings in, fixed summary tokens out.
  There is no metric to invent and no threshold to calibrate. What RED must not
  do is test only the positive cases — AC-2 and AC-5 are the whole value of the
  suite, and a suite that pins AC-1/AC-3/AC-4 alone is satisfied by a `gates.sh`
  that fails `integration` unconditionally.
- **Success condition, falsifiable either way:** *at GREEN, mutating the
  `skipped-when` match in `scripts/gates.sh` so that it always matches (via
  `bash scripts/mutate.sh`) must turn **AC-5's** case red and leave AC-1's,
  AC-3's and AC-4's green.* If AC-5 stays green under that mutation, RED wrote a
  suite that cannot tell a classified shortfall from an excused one, and the
  model guidance failed on its own terms.
- **Verdict:** _(to be recorded when RED ends, against the condition above.)_

## Out of scope

Deliberately generous — every item below is a real temptation while inside this
change:

- **Committing the spike weights or page scans.** They are a non-redistributable
  scan of a commercial release and a GPL-3.0 model's weights (MT-007 C-9 / PO-2).
  `.gitignore` covering them is a licensing decision, not an oversight, and this
  story exists *because* of it, not to undo it.
- **Making `integration` `required` for the repo.** It loads real `.onnx` models
  onto a GPU and calls a paid API. Per-story escalation via `required_gates` is
  the designed answer and it stays.
- **Re-verifying or re-measuring anything MT-036 measured** — its mutation table,
  its `observed 26`, its tree stamp. All verified at MT-036 and not reopened.
- **Touching MT-011's file at all** — its acceptance criteria, its
  `required_gates`, its `depends_on`. See `## Notes` PO-4 for why no dependency
  edge is added in either direction.
- **Adding a `floor` to `format`.** MT-005, MT-006, MT-007 and MT-008 each
  deliberately declined one and each said why; nothing here changes that
  reasoning.
- **Adding a `selftest` gate to `project.conf`.** This story's criteria are
  checked by `bash scripts/selftest.sh`, which is a CI step rather than a gate
  (`## Context`). Making it a gate is a real and possibly correct idea, and it is
  a change to what every story's gate run costs and means — its own story, with
  its own probe. Noted, not done.
- **Changing the `BLOCKED` advisory text `gates.sh` prints at the end of a run.**
  That block describes the pending-CI playbook, which is the wrong path for a
  `skipped-when` block (C-3 item 2). Correcting the prose belongs with the
  playbook; here it is documented in the `project.conf` header instead.
- **Unifying the existing launch-failure `BLOCKED` path's optional branch with
  the new one.** Today an optional gate the environment refused to launch reports
  `WARN`; the new path reports `KNOWN`. That inconsistency is visible and
  arguable, and changing the `rc != 0` path is a behaviour change to a mechanism
  with its own eight-run field report behind it. Not here.
- **A `discovery` line for `spikes/**`.** Tempting, and wrong: `discovery` asks a
  *runner* what it can see, and collection of the integration suite succeeds at
  26 whether or not the data is present — the skip happens at call time. A
  `test -d spikes` line would be reading the filesystem while wearing the
  runner's name, which is the exact anti-pattern the `discovery` header warns
  about. The gate's own `KNOWN` line is the diagnosis.
- **Any change to `src/mangatl/**` or `tests/**`.** This story does not touch the
  product.

## Test plan

All of it lives in one new `describe` block, **`skipped-when: a gate that
skipped its work stops reporting PASS`**, at the end of
`.claude/tests/gates.test.sh`, run by `bash scripts/selftest.sh gates`. Every
gate command is a `printf` against `make_project_fixture`, as every case in that
file already is. 52 new assertions; nothing else in the file is touched except
one new shared helper in `.claude/tests/_lib.sh` (`assert_not_contains`, needed
because AC-1 is phrased as *something other than `PASS`* and `assert_contains`
cannot express that).

| # | Test (assertion name) | AC / contract | Level |
|---|---|---|---|
| 1 | the manifest gives integration a floor of 26 | AC-1, C-3 | manifest — reads the real `.claude/harness/project.conf` |
| 2 | and a skipped-when pattern that matches skips only | AC-1, C-3 | manifest — same, and pins `skipped` not `deselected` |
| 3 | a run that skipped 25 of its 26 tests is not a PASS | **AC-1** | integration — real manifest lines, PO-1's log, `printf` command |
| 4 | a machine that has the data still passes | **AC-2** | integration |
| 5 | naming both the count and the floor | **AC-2** | integration |
| 6 | and the run exits 0 | AC-2 | integration |
| 7 | a gate at its floor passes even though tests skipped | C-1 (never consulted at or above the floor) | integration |
| 8 | and is not turned into a declared non-result | C-1 | integration |
| 9 | an optional gate the environment starved reports KNOWN | **AC-4** | integration |
| 10 | and says why, inline | AC-4, C-2 wording | integration |
| 11 | and points at the log | AC-4 | integration |
| 12 | and is specifically not a WARN | **AC-4** | integration |
| 13 | and the run exits 0 | AC-4 | integration |
| 14 | and it is counted as a known non-result | AC-4 (`known` counter) | integration |
| 15 | and the stamp still records a pass | AC-4 (`RESULT=pass`) | integration |
| 16 | a waiver is still named beside the reason | C-2 (waiver is the broader declaration) | integration |
| 17 | a gate the story leans on is BLOCKED | **AC-3** | integration + story frontmatter |
| 18 | naming the story that escalated it | AC-3 | integration |
| 19 | and says why | AC-3, C-2 wording | integration |
| 20 | and points at the log | AC-3 | integration |
| 21 | and it is counted as a gate that could not run | AC-3 (`blocked` counter) | integration |
| 22 | and the run exits 3, not 0 and not 1 | **AC-3** | integration |
| 23 | and the story records result: blocked | **AC-3** | integration — reads the written `## Gate results` |
| 24 | so the record carries no PASS line for check-boundaries.sh to read | **AC-3 / C-5** | integration — runs `check-boundaries.sh`'s own grep against the record |
| 25 | required: a shortfall the pattern does not match still FAILs | **AC-5** | integration |
| 26 | with the floor wording it has always had | AC-5 | integration |
| 27 | and is not reported as a block | AC-5 | integration |
| 28 | and the run exits 1 | AC-5 | integration |
| 29 | optional: the same shortfall still WARNs | **AC-5** | integration |
| 30 | and is not excused as a declared non-result | **AC-5** | integration |
| 31 | and the run exits 0 | AC-5 | integration |
| 32 | a deselected shortfall is a manifest regression, not an environment | **AC-5 / C-3** | integration |
| 33 | so the skip pattern does not classify it | **AC-5 / C-3** | integration |
| 34 | a non-zero exit fails even when the log matches the skip pattern | **DV-2** | integration |
| 35 | and is not a block | DV-2 | integration |
| ~~36~~ | ~~and not a declared non-result~~ — **removed on the return to RED**: on a `required` fixture `KNOWN` is excluded by the fixture's own setting, so no single mutation can turn it red. The claim moves to 37a/37b. See `## Regressions` R-6. | — | — |
| 37 | and the run exits 1 | DV-2 | integration |
| 37a | optional: a non-zero exit warns even when the log matches the skip pattern | **DV-2** (optional twin, added on the return) | integration |
| 37b | and is not excused as a declared non-result on the rc != 0 path | **DV-2** (optional twin, added on the return) | integration |
| 38 | a pattern using alternation is not truncated at the first branch | C-1 (`cut -d'\|' -f3-`) | integration |
| 39 | and the line quotes the branch that matched | C-1, C-2 wording | integration |
| 40 | a skipped-when naming no configured gate fails the audit | **AC-6 (a)** | `--audit` |
| 41 | and the audit exits non-zero | AC-6 (a) | `--audit` |
| 42 | a skipped-when with no pattern fails the audit | **AC-6 (b)** | `--audit` |
| 43 | and the audit exits non-zero | AC-6 (b) | `--audit` |
| 44 | a skipped-when on a gate with no floor fails the audit | **AC-6 (c)** | `--audit` |
| 45 | and the audit exits non-zero | AC-6 (c) | `--audit` |
| 46 | a well-formed skipped-when passes the audit | AC-6 negative control | `--audit` |
| 47 | and the audit prints it with the gate | AC-6 negative control | `--audit` |
| 48 | and the audit exits 0 | AC-6 negative control | `--audit` |
| 49 | --list prints the skip pattern | **AC-7** | `--list` |
| 50 | beside the blocked-when line | AC-7 | `--list` |
| 51 | beside the floor | AC-7 | `--list` |
| 52 | and the evidence regex | AC-7 | `--list` |

**Why two tests for AC-1 rather than one.** Tests 1 and 2 read the **real**
`.claude/harness/project.conf`; test 3 lifts the real manifest's own
`evidence`/`floor`/`skipped-when` lines for `integration` into the fixture and
replaces only the command. A suite that only ever asks `gates.sh` about a conf
it wrote itself would go green while the real `integration` gate still reported
`PASS` on a machine that ran one of its 26 tests - and PO-1 says that machine is
every worktree this harness dispatches into. It is the same reasoning that makes
`make_fixture` copy the real `paths.conf` and `VERSION` instead of inventing
them. Test 3 reproduces PO-1 mechanically and is the permanent regression test.

## Handoff: RED -> GREEN

### The command

```
bash scripts/selftest.sh gates      # this block only
bash scripts/selftest.sh            # every harness suite; all others must stay green
```

Roughly 3 minutes per run on this machine (Windows, git-bash); the whole
self-test is roughly 12. `bash scripts/gates.sh --fast` does **not** run these:
they are a CI step, not a gate (see `## Context`).

### Verbatim failure output

`bash scripts/selftest.sh gates`, against `scripts/gates.sh` and
`.claude/harness/project.conf` exactly as this story begins. 24 of the 52 new
assertions fail. The headline and the expected/actual of each, verbatim:

```
  skipped-when: a gate that skipped its work stops reporting PASS
    FAIL the manifest gives integration a floor of 26
         expected: 26
         actual:   
    FAIL and a skipped-when pattern that matches skips only
         expected: [1-9][0-9]* skipped
         actual:   
    FAIL a run that skipped 25 of its 26 tests is not a PASS
         expected NOT to contain: PASS         integration
         actual:                   
    FAIL an optional gate the environment starved reports KNOWN
         expected to contain: KNOWN        integration
         actual:               
    FAIL and says why, inline
         expected to contain: did 1 units of work, below the floor of 26 in project.conf; the log says 25 skipped, so the work was skipped rather than lost: the environment did not supply it
         actual:               
    FAIL and points at the log
         expected to contain: -> .claude/state/gate-logs/integration.log
         actual:               
    FAIL and is specifically not a WARN
         expected NOT to contain: WARN         integration
         actual:                   
    FAIL and it is counted as a known non-result
         expected to contain: All required gates passed (1 ran, 0 unconfigured, 1 known)
         actual:               
    FAIL a waiver is still named beside the reason
         expected to contain: did 1 units of work, below the floor of 26 in project.conf; the log says 25 skipped, so the work was skipped rather than lost: the environment did not supply it; the spike data is not redistributable
         actual:               
    FAIL a gate the story leans on is BLOCKED
         expected to contain: BLOCKED      integration
         actual:               
    FAIL and says why
         expected to contain: did 1 units of work, below the floor of 26 in project.conf; the log says 25 skipped, so the work was skipped rather than lost: the environment did not supply it
         actual:               
    FAIL and it is counted as a gate that could not run
         expected to contain: 1 required gate(s) could not run
         actual:               
    FAIL and the run exits 3, not 0 and not 1
         expected: 3
         actual:   1
    FAIL and the story records result: blocked
         expected to contain: result: blocked
         actual:               ---
    FAIL a pattern using alternation is not truncated at the first branch
         expected to contain: KNOWN        integration
         actual:               
    FAIL and the line quotes the branch that matched
         expected to contain: the log says no data files were supplied
         actual:               
    FAIL a skipped-when naming no configured gate fails the audit
         expected to contain: a `skipped-when` line names no configured gate
         actual:               ok   integration  evidence: [1-9][0-9]* passed
    FAIL and the audit exits non-zero
         expected: 1
         actual:   0
    FAIL a skipped-when with no pattern fails the audit
         expected to contain: a `skipped-when` line has no pattern
         actual:               ok   integration  evidence: [1-9][0-9]* passed
    FAIL and the audit exits non-zero
         expected: 1
         actual:   0
    FAIL a skipped-when on a gate with no floor fails the audit
         expected to contain: a `skipped-when` line names a gate with no `floor` line
         actual:               ok   build        evidence: Bundled [1-9][0-9]* targets
    FAIL and the audit exits non-zero
         expected: 1
         actual:   0
    FAIL and the audit prints it with the gate
         expected to contain: skipped-when: [1-9][0-9]* skipped
         actual:               ok   integration  evidence: [1-9][0-9]* passed
    FAIL --list prints the skip pattern
         expected to contain: skipped-when: [1-9][0-9]* skipped
         actual:               integration  optional  .      printf "26 passed in 7.1s\n"

gates: 136 passed, 24 failed

1 of 1 harness suite(s) FAILED.
```

**The defect, reproduced in a fixture.** Test 3's `actual:` block in full - this
is PO-1, mechanically, and it is what AC-1 exists to make impossible:

```
    FAIL a run that skipped 25 of its 26 tests is not a PASS
         expected NOT to contain: PASS         integration
         actual:                   
         === gate: integration (optional) ===
         printf "1 passed, 25 skipped in 0.46s\n"
         1 passed, 25 skipped in 0.46s
         
         --- gate summary ---
         PASS         integration (0s, observed 1)
         
         (not recorded: no active story; use --story <id> to record it in one)
         
         All required gates passed (1 ran, 0 unconfigured, 0 known).
```

**AC-3's, in full** - the record `check-boundaries.sh` reads:

```
    FAIL and the story records result: blocked
         expected to contain: result: blocked
         actual:               ---
         ...
         ## Gate results
         
         <!-- gates.sh: written by bash scripts/gates.sh; do not edit or paste by hand -->
         
             run:    2026-09-17T15:41:08Z
             commit: 9a4a941 (working tree had uncommitted changes)
             tree:   c5b0e1c7c3e4a696e606789fddef9737bb02ac57
             result: fail (1 required gate(s) failed)
         
             FAIL         integration (required by story T-1) (0s, did 1 units of work, below the floor of 26 in project.conf) -> .claude/state/gate-logs/integration.log
```

### `gates.sh --fast` at the end of RED: green, and that is correct here

```
--- gate summary ---
PASS         format (4s, observed 71)
PASS         lint (3s, observed 5, floor 5)
PASS         typecheck (10s, observed 39, floor 28)
PASS         unit (17s, observed 421, floor 292)
PASS         coverage (16s, observed 421, floor 292)
PASS         coverage-core (15s, observed 416, floor 287)

--fast skipped: integration build mutation
```

`tdd-cycle` says to read a RED `--fast` for the *shape* of the failure, with the
test gates red. **This story is the documented exception** (`## Context`): every
file it touches classifies as `harness`, and its criteria are checked by
`bash scripts/selftest.sh`, which is a **CI step and not a gate**. No gate runs
the harness self-test, so no gate can be red for this story's tests, and adding
a `selftest` gate is explicitly `## Out of scope`. What the run *does* establish
is the thing `--fast` is for: the new test file is admissible - `format` and
`lint` are clean over it and nothing times out. The verdict that matters for
MT-037 is `bash scripts/selftest.sh`, and it is red in exactly one suite.

### The strings GREEN is frozen against

C-2 says RED picks the wording and GREEN matches it, because tests are frozen in
GREEN. These are the pinned strings, verbatim. Nothing else in the summary is
constrained.

**1. The `why` string.** Built from the observed count, the floor and the text
the skip pattern matched, in that order:

```sh
skipmatch="$(clean_log "$log" | grep -Eom1 -- "$skippat" | head -1)"
why="did $observed units of work, below the floor of $floor in project.conf; the log says $skipmatch, so the work was skipped rather than lost: the environment did not supply it"
```

For the 1-of-26 fixture that renders exactly:

```
did 1 units of work, below the floor of 26 in project.conf; the log says 25 skipped, so the work was skipped rather than lost: the environment did not supply it
```

Note the shape: the first clause is **byte-identical to the existing
`noevidence` floor wording** (`did $observed units of work, below the floor of
$floor in project.conf`), so AC-5's "with the floor wording it has always had"
and this share a prefix on purpose. The clause after the `;` is what is new.
Quoting `$skipmatch` is what makes the line answer *why* without opening the gate
log, and test 39 pins it against a pattern whose matching branch is not the first
one.

**2. The summary lines.** `$logrel` is `.claude/state/gate-logs/<id>.log`, as
elsewhere:

```sh
# required, including story-escalated:
BLOCKED      $id$escalated (${dur}s, $why) -> $logrel        # blocked=$((blocked+1))
# optional, carrying a waiver (the waiver is the broader declaration - C-2):
KNOWN        $id (${dur}s, $why; $waiver) -> $logrel         # known=$((known+1))
# optional:
KNOWN        $id (${dur}s, $why) -> $logrel                  # known=$((known+1))
```

Column widths are the existing ones: `BLOCKED` + 6 spaces, `KNOWN` + 8 spaces.
There is **no `exit $rc,`** clause - unlike the launch-failure `blocked` arm,
this outcome only ever happens on `rc = 0`.

**3. The counters, exit codes and stamp** - all existing, none new:

| Case | Summary tail | Exit | Stamp |
|---|---|---|---|
| required / story-escalated | `1 required gate(s) could not run` | **3** | `RESULT=blocked`, `## Gate results` → `result: blocked (…)` |
| optional | `All required gates passed (1 ran, 0 unconfigured, 1 known).` | **0** | `RESULT=pass` |

**4. The three `--audit` refusals.** Shape follows the existing `blocked-when`
and `covers` audits (`printf 'FAIL %-12s <message>\n' "$id"`). The tests assert
the message text only, so the `%-12s` id column is yours:

```
a `skipped-when` line names no configured gate
a `skipped-when` line has no pattern
a `skipped-when` line names a gate with no `floor` line: there is nothing to measure the shortfall it would classify
```

Only the prefix up to and including ``no `floor` line`` is pinned on the third;
the colon clause after it is a suggestion. Each refusal must make `--audit` exit
non-zero (the existing `fails` counter does this).

**5. The `--audit` positive print**, beside the existing `floor:` line:

```
     %-12s skipped-when: %s
```

pinned only as the substring `skipped-when: [1-9][0-9]* skipped` appearing in a
passing audit's output, together with `Manifest audit passed.` and exit 0.

**6. The `--list` line**, beside the existing `blocked-when:` line:

```
%-12s %-9s %-6s skipped-when: %s
```

pinned as the substring `skipped-when: [1-9][0-9]* skipped`. Tests 50-52 pin
that the existing `blocked-when:`, `floor:    ` and `evidence: ` lines are still
printed unchanged in the same block.

### What the tests do **not** constrain

- The internal outcome token. C-2 suggests `environment`; nothing asserts it.
- The accumulator variable's name, and how `SKIPPEDWHEN` is stored - only that
  the value survives `cut -d'|' -f3-` (test 38 fails if it does not).
- Where in `--list`/`--audit` output the new line sits relative to the others,
  beyond both appearing for the same gate.
- The `## Gate results` `result:` **text after** `blocked` - only that it starts
  `result: blocked`.
- Anything in `project.conf`'s header prose (C-3 items 1-3) or
  `docs/wiki/environment.md` (C-4). No test reads them.
- `scripts/check-boundaries.sh` - see the verification below; it needs no change
  and no test asks it to.

### Files touched in RED

| File | Change |
|---|---|
| `.claude/tests/gates.test.sh` | one new `describe` block, 52 assertions, appended before `summary "gates"` |
| `.claude/tests/_lib.sh` | new helper `assert_not_contains <what> <needle> <haystack>` |
| `docs/backlog/stories/MT-037.md` | `## Test plan`, `## Handoff`, `## Regressions`, a RED addendum under DV-2 |

Nothing else. `scripts/gates.sh` and `.claude/harness/project.conf` are
untouched (`git status` clean for both after every mutation; each `mutate.sh`
run reported `restored (verified byte-for-byte…)`).

### Assertions that passed on arrival

28 of the 52 did. `gates.sh` already has a floor mechanism, so every assertion
about what the floor mechanism *keeps* doing is green from the first run and
would stay green if it asserted nothing. Five reverted `mutate.sh` probes earn
21 of them; the remaining 7 cannot be earned in RED and are declared below
rather than claimed. The pasted red is in `## Regressions`.

| Probe | Mutation of `scripts/gates.sh` | Assertions it turned red |
|---|---|---|
| **M1** | `s/observed" -lt/observed" -le/` — the floor comparison, so a gate exactly at its floor is a shortfall | 4, 5 (AC-2), 7 (C-1) |
| **M2** | `s/observed" -lt/observed" -gt/` — a below-floor run stops being a shortfall at all | 20, 24 (AC-3), 25, 26, 28, 29, 32 (AC-5) |
| **M3** | `s/blockpat="/blockpat="[1-9][0-9]* skipped\|/` — makes the skip pattern reachable on the `rc != 0` path, which is exactly what DV-2 excludes | 34, 35, 37 (DV-2) |
| **M4** | `s/warns=\$((warns+1))/fails=\$((fails+1))/` — an optional shortfall counted as a failure | 13, 15 (AC-4), 31 (AC-5) |
| **M5** | `s/floor:    %s/floor:  %s/; s/blocked-when: %s/blockedwhen: %s/; s/evidence: %s/evidence  %s/` — the `--list` lines the new one must not displace | 50, 51, 52 (AC-7) |

**The 7 that cannot be earned in RED, declared rather than claimed.** Each is a
*negative* assertion about an outcome `gates.sh` cannot currently produce,
because the arm that would produce it is what GREEN is about to write. There is
nothing to mutate. They are not decoration - they are the assertions that stop
the new arm from firing where it must not - but their force begins at GREEN, and
**GATES's DV-3 mutation (make the `skipped-when` match always match) is the probe
that earns them.** GREEN should expect all 7 to stay green and should not read
that as evidence.

| # | Assertion | Why RED cannot make it fail |
|---|---|---|
| 6 | AC-2: and the run exits 0 | coupled to 4 and 5; no independent mutation makes a passing optional gate exit non-zero |
| 8 | C-1: and is not turned into a declared non-result | nothing can emit `KNOWN` for an at-floor gate yet |
| 27 | AC-5: and is not reported as a block | the `rc = 0` path cannot reach `BLOCKED` yet |
| 30 | AC-5: and is not excused as a declared non-result | nothing can emit `KNOWN` for an unclassified shortfall yet |
| 33 | AC-5/C-3: so the skip pattern does not classify it | the pattern is not read yet |
| ~~36~~ | DV-2: and not a declared non-result | same — **and it never could be: removed on the return to RED and replaced by the optional twin 37a/37b. See `## Regressions` R-6.** |
| 46, 48 | AC-6: a well-formed skipped-when passes the audit / exits 0 | the audit does not look at `skipped-when` lines yet, so it cannot wrongly refuse one |

### Negative controls: the numbers, measured

RED here is **not** the usual import-failure case - this is a bash suite against
a fixture, `gates.sh` already exists, so every one of the 52 assertions executed
on the first run and the 24 reds above are real reds. The controls are therefore
already exercised, not claims. What *is* worth recording is the arithmetic each
control depends on, measured outside `gates.sh` with the same two pipelines it
uses (`work_count` = `grep -oE -m1 "(<evidence>).*" | grep -oE '[0-9]+' | head -1`;
the classification = `grep -Eom1 -- "<skipped-when>"`):

| Control (test #) | Fixture log | Floor | `observed`, measured | skip pattern match, measured | Verdict the suite demands |
|---|---|---|---|---|---|
| AC-1/AC-4 (3, 9) | `1 passed, 25 skipped in 0.46s` | 26 | **1** | `25 skipped` | not `PASS`; `KNOWN`, exit 0 |
| AC-3 (17) | same, story-escalated | 26 | **1** | `25 skipped` | `BLOCKED`, exit 3 |
| AC-2 (4) | `26 passed in 7.1s` | 26 | **26** | *(none)* | `PASS`, `observed 26, floor 26` |
| C-1 (7) | `26 passed, 3 skipped in 7.1s` | 26 | **26** | `3 skipped` | `PASS` — at the floor, pattern not consulted |
| AC-5 (25, 29) | `3 passed in 0.4s` | 26 | **3** | *(none)* | `FAIL` required / `WARN` optional |
| AC-5/C-3 (32) | `3 passed, 23 deselected in 0.4s` | 26 | **3** | *(none)* | `WARN` — a deselection is a regression, not an environment |
| C-1 alternation (38) | `1 passed, 0 skipped in 0.4s` + `no data files were supplied` | 26 | **1** | `no data files were supplied` | `KNOWN` — and `WARN` if the pattern is truncated at `\|` |
| DV-2 (34) | `1 passed, 25 skipped in 0.46s` + `INTERNALERROR`, `exit 2` | 26 | n/a (`rc != 0`) | `25 skipped` | `FAIL`, exit 1 |

The two rows that carry the story: `3 passed, 23 deselected` measures **no**
match for `[1-9][0-9]* skipped`, which is what makes AC-5's `deselected` case a
real discriminator rather than a restatement; and `1 passed, 0 skipped` measures
no match for the *first* branch of the alternation and a match for the second,
which is what makes test 38 fail if the parser uses `-f3` instead of `-f3-`.

**GREEN's job:** confirm each `observed` and each match against the shipped
implementation, not merely that the tests pass. They were measured against the
pipelines as they stand today.

### Two of the Contract's mechanisms, checked

- **C-2's line numbers are slightly off, the mechanism is right.** The
  `[ -n "$floor" ]` branch is at `scripts/gates.sh` **L480-488**, not ~470-480,
  and `observed -lt floor` is L484. Everything else in C-2 holds. The
  `blocked-when` consultation is inside `if [ "$rc" -ne 0 ]` at L460-471, as
  stated.
- **C-5 verified: `check-boundaries.sh` needs no change.** L263-269 is
  `for g in $(frontmatter_list "$sfile" required_gates)` /
  `grep -qE "^[[:space:]]*PASS[[:space:]]+$g( |\(|$)"` against `## Gate results`,
  reported as *"story-required gate '$g' passed in the recorded run"*. A
  `BLOCKED integration` record does not match it. Test 24 runs that exact regex
  against the record `gates.sh` actually wrote, so the dependency is pinned from
  this side rather than assumed. **No amendment needed and none requested.**

### Anything that should change the implementation approach

- **Do the `skipped-when` lookup where `blocked-when`'s is done**, i.e. with
  `table_lookup "$SKIPPEDWHEN" "$id"`, and guard it with `[ -n "$skippat" ]`
  before the `grep -Eq`. An empty pattern passed to `grep -Eq` matches every
  line, which is precisely why AC-6 (b) refuses an empty one at audit time - but
  the audit is a separate invocation and a run does not perform it. Test 42 pins
  the audit; nothing pins the run against an empty pattern, so belt and braces.
- **AC-6 (c) needs `GATE_IDS` *and* `FLOORS`**, both of which are already fully
  populated by the time the audit block runs (the parse loop completes before
  any gate runs). The existing `blocked-when` audit loop at L554-565 is the
  template; add the floor check as a second refusal inside the same loop, after
  the "names no configured gate" `continue`.
- **The `--audit` positive print lives at L427-430**, in the per-gate `AUDIT`
  block, and must go after the `floor:` line — but note that block `continue`s
  before the gate runs, and that the floor-broken check above it already
  `continue`s on a floor with no evidence regex. A `skipped-when` on a gate with
  a *broken* floor therefore never reaches your new print; that is fine and no
  test depends on it.
- **The 1-of-26 optional case currently prints the "1 optional gate(s) WARNed"
  advisory.** Once it becomes `KNOWN` that advisory correctly disappears; test 12
  (`is specifically not a WARN`) matches on `WARN         integration`, so the
  advisory's own wording is not in scope, but do not be surprised by the diff.

### Addendum, return to RED from GATES, 2026-09-17: the DV-2 optional twin

This is the only change to the tests since the handoff above; everything else in
this section still holds as written.

- **Command, unchanged:** `bash scripts/selftest.sh gates` (whole harness:
  `bash scripts/selftest.sh`).
- **File touched:** `.claude/tests/gates.test.sh` only — one assertion removed
  (old L889, test 36), two added (37a, 37b), plus the comment above the DV-2
  block explaining which fixture carries which half of the claim.
- **New counts:** `gates: 161 passed, 0 failed` (was 160). All 12 suites green.
- **`scripts/gates.sh` is untouched** and byte-identical to the tree GATES
  stamped; the only mutation run in this phase went through `scripts/mutate.sh`
  and restored with `cmp`.
- **What the twin pins, for GREEN/GATES:** with `rc != 0` the `skipped-when` arm
  is never consulted, *including on an optional gate* — where the wrong answer is
  `KNOWN`, not `BLOCKED`. Output shape depended on:
  `WARN         integration` present, `KNOWN        integration` absent, for a
  gate whose command exits 2 and whose log contains `25 skipped`.
- **It was earned, not assumed:** the rc-guard mutation and its red output are in
  `## Regressions` R-6.
- **Deliberately not added:** an exit-code assertion on the twin. On an optional
  fixture the exit code is 0 for `WARN` and for `KNOWN` alike, so such an
  assertion could not discriminate — the same defect as the one being removed.
  R-6 records the same finding for existing test 6.

## Regressions

Not a return from GREEN - this is RED's first pass. The section is here because
`gates.sh` already exists, so 28 of the 52 new assertions passed on their first
execution, and `rules.md` is explicit that an assertion never observed to fail is
not a test. Five reverted `scripts/mutate.sh` probes earn 21 of them; the other 7
are declared in `## Handoff` as unearnable in RED, with GATES's DV-3 mutation
named as the probe that earns them.

Every probe below restored the file and said so. Baseline for comparison:
`gates: 136 passed, 24 failed`.

### M1 - the floor comparison, so a gate exactly at its floor is a shortfall

Earns tests 4, 5 (AC-2) and 7 (C-1). A fix that satisfied AC-1 by making
`integration` never pass is the cheapest wrong fix available, and this is the
assertion set that refuses it.

```
$ bash scripts/mutate.sh scripts/gates.sh 's/observed" -lt/observed" -le/' \
    -- bash scripts/selftest.sh gates

=== mutate: scripts/gates.sh (1 line(s) changed by s/observed" -lt/observed" -le/) ===
  484 -       elif [ "$observed" -lt "$floor" ]; then
  484 +       elif [ "$observed" -le "$floor" ]; then

    FAIL a machine that has the data still passes
         expected to contain: PASS         integration
         actual:               
         === gate: integration (optional) ===
         printf "26 passed in 7.1s\n"
         26 passed in 7.1s
         
         --- gate summary ---
         WARN         integration (0s, did 26 units of work, below the floor of 26 in project.conf, optional)
    FAIL naming both the count and the floor
         expected to contain: observed 26, floor 26
         actual:               
         --- gate summary ---
         WARN         integration (0s, did 26 units of work, below the floor of 26 in project.conf, optional)
    FAIL a gate at its floor passes even though tests skipped
         expected to contain: PASS         integration
         actual:               
         === gate: integration (optional) ===
         printf "26 passed, 3 skipped in 7.1s\n"
         26 passed, 3 skipped in 7.1s
         
         --- gate summary ---
         WARN         integration (1s, did 26 units of work, below the floor of 26 in project.conf, optional)

gates: 133 passed, 27 failed

=== mutate: command exited 1; restored (verified byte-for-byte against .../.claude/state/mutations/scripts_gates.sh.20260917T155303Z.3422679.bak) ===
  484:       elif [ "$observed" -lt "$floor" ]; then
```

### M2 - a below-floor run stops being a shortfall at all

Earns tests 25, 26, 28, 29, 32 (AC-5) and 20, 24 (AC-3). This is the mutation
that matters: AC-5 is the negative control that stops the new `skipped-when` line
from excusing every shortfall, and it was green on arrival because
`FAIL`/`WARN` is what `gates.sh` already does.

```
$ bash scripts/mutate.sh scripts/gates.sh 's/observed" -lt/observed" -gt/' \
    -- bash scripts/selftest.sh gates

=== mutate: scripts/gates.sh (1 line(s) changed by s/observed" -lt/observed" -gt/) ===
  484 -       elif [ "$observed" -lt "$floor" ]; then
  484 +       elif [ "$observed" -gt "$floor" ]; then

    FAIL so the record carries no PASS line for check-boundaries.sh to read
         expected: 0
         actual:   1
    FAIL required: a shortfall the pattern does not match still FAILs
         expected to contain: FAIL         integration
         actual:               
         === gate: integration (required (required by story T-1)) ===
         printf "3 passed in 0.4s\n"
         3 passed in 0.4s
         
         --- gate summary ---
         PASS         integration (0s, observed 3, floor 26)
         
         recorded in docs/backlog/stories/T-1.md (## Gate results)
         
         All required gates passed (1 ran, 0 unconfigured, 0 known).
    FAIL with the floor wording it has always had
         expected to contain: did 3 units of work, below the floor of 26 in project.conf
         actual:               
         --- gate summary ---
         PASS         integration (0s, observed 3, floor 26)
    FAIL and the run exits 1
         expected: 1
         actual:   0
    FAIL optional: the same shortfall still WARNs
         expected to contain: WARN         integration
         actual:               
         === gate: integration (optional) ===
         printf "3 passed in 0.4s\n"
         3 passed in 0.4s
         
         --- gate summary ---
         PASS         integration (0s, observed 3, floor 26)
    FAIL a deselected shortfall is a manifest regression, not an environment
         expected to contain: WARN         integration
         actual:               
         === gate: integration (optional) ===
         printf "3 passed, 23 deselected in 0.4s\n"
         3 passed, 23 deselected in 0.4s
         
         --- gate summary ---
         PASS         integration (0s, observed 3, floor 26)

=== mutate: command exited 1; restored (verified byte-for-byte against .../.claude/state/mutations/scripts_gates.sh.20260917T160128Z.3450948.bak) ===
  484:       elif [ "$observed" -lt "$floor" ]; then
```

Note the second failure in that list. Test 24 runs `check-boundaries.sh`'s own
grep (`^[[:space:]]*PASS[[:space:]]+integration( |\(|$)`) against the
`## Gate results` block `gates.sh` wrote, and under this mutation the record
grows exactly the `PASS integration` line C-5 depends on not being there. That
is the whole chain in one assertion: vacuous gate → recorded PASS →
*"story-required gate 'integration' passed in the recorded run"*.

### M3 - the skip pattern made reachable on the `rc != 0` path

Earns tests 34, 35, 37 (DV-2). C-1 says `skipped-when` is consulted only for a
gate that exited 0; this breaks that and the DV-2 case catches it.

```
$ bash scripts/mutate.sh scripts/gates.sh 's/blockpat="/blockpat="[1-9][0-9]* skipped|/' \
    -- bash scripts/selftest.sh gates

=== mutate: scripts/gates.sh (2 line(s) changed by s/blockpat="/blockpat="[1-9][0-9]* skipped|/) ===
  465 -     blockpat="$BLOCKED_DEFAULT"
  465 +     blockpat="[1-9][0-9]* skipped|$BLOCKED_DEFAULT"
  467 -     [ -n "$extra" ] && blockpat="$blockpat|$extra"
  467 +     [ -n "$extra" ] && blockpat="[1-9][0-9]* skipped|$blockpat|$extra"

    FAIL a non-zero exit fails even when the log matches the skip pattern
         expected to contain: FAIL         integration
         actual:               
         === gate: integration (required) ===
         printf "1 passed, 25 skipped in 0.46s\nINTERNALERROR: the run aborted\n"; exit 2
         1 passed, 25 skipped in 0.46s
         INTERNALERROR: the run aborted
         
         --- gate summary ---
         BLOCKED      integration (0s, exit 2, could not launch: 25 skipped) -> .claude/state/gate-logs/integration.log
    FAIL and is not a block
         expected NOT to contain: BLOCKED      integration
         actual:                   
         --- gate summary ---
         BLOCKED      integration (0s, exit 2, could not launch: 25 skipped) -> .claude/state/gate-logs/integration.log
    FAIL and the run exits 1
         expected: 1
         actual:   3

=== mutate: command exited 1; restored (verified byte-for-byte against .../.claude/state/mutations/scripts_gates.sh.20260917T160911Z.3476619.bak) ===
  465:     blockpat="$BLOCKED_DEFAULT"
  467:     [ -n "$extra" ] && blockpat="$blockpat|$extra"
```

### M4 - an optional shortfall counted as a failure

Earns tests 13, 15 (AC-4) and 31 (AC-5). AC-4's claim is not only the verdict
token but that the recorded result is still `pass`; this is what makes that
clause assert something.

```
$ bash scripts/mutate.sh scripts/gates.sh 's/warns=\$((warns+1))/fails=\$((fails+1))/' \
    -- bash scripts/selftest.sh gates

=== mutate: scripts/gates.sh (3 line(s) changed by s/warns=\$((warns+1))/fails=\$((fails+1))/) ===
  510 -         results="$results\nWARN         $id (${dur}s, $why, optional)"; warns=$((warns+1))
  510 +         results="$results\nWARN         $id (${dur}s, $why, optional)"; fails=$((fails+1))
  518 -         results="$results\nWARN         $id (${dur}s, exit $rc, optional) -> $logrel"; warns=$((warns+1))
  518 +         results="$results\nWARN         $id (${dur}s, exit $rc, optional) -> $logrel"; fails=$((fails+1))
  529 -         results="$results\nWARN         $id (${dur}s, $why, optional) -> $logrel"; warns=$((warns+1))
  529 +         results="$results\nWARN         $id (${dur}s, $why, optional) -> $logrel"; fails=$((fails+1))

    FAIL and the run exits 0
         expected: 0
         actual:   1
    FAIL and the stamp still records a pass
         expected to contain: RESULT=pass
         WHEN=
         actual:               RESULT=fail
         WHEN=2026-09-17T16:15:53Z
         FULL=yes
         BLOCKED=0

=== mutate: command exited 1; restored (verified byte-for-byte against .../.claude/state/mutations/scripts_gates.sh.20260917T161256Z.3490218.bak) ===
  510:         results="$results\nWARN         $id (${dur}s, $why, optional)"; warns=$((warns+1))
  518:         results="$results\nWARN         $id (${dur}s, exit $rc, optional) -> $logrel"; warns=$((warns+1))
  529:         results="$results\nWARN         $id (${dur}s, $why, optional) -> $logrel"; warns=$((warns+1))
```

### M5 - the `--list` lines the new one must not displace

Earns tests 50, 51, 52 (AC-7). AC-7 says the pattern is printed *beside* the
existing `evidence`, `floor` and `blocked-when` lines; without these three, a
`--list` that printed only `skipped-when:` would satisfy the criterion. One
compound expression rather than three runs, because the three printfs are
independent and each red names itself.

```
$ bash scripts/mutate.sh scripts/gates.sh \
    's/floor:    %s/floor:  %s/; s/blocked-when: %s/blockedwhen: %s/; s/evidence: %s/evidence  %s/' \
    -- bash scripts/selftest.sh gates

=== mutate: scripts/gates.sh (4 line(s) changed by ...) ===
  320 -     printf '%-12s %-9s %-6s evidence: %s\n' "" "" "" "$exp"
  320 +     printf '%-12s %-9s %-6s evidence  %s\n' "" "" "" "$exp"
  321 -     [ -n "$floor" ]  && printf '%-12s %-9s %-6s floor:    %s\n' "" "" "" "$floor"
  321 +     [ -n "$floor" ]  && printf '%-12s %-9s %-6s floor:  %s\n' "" "" "" "$floor"
  328 -       [ "$bid" = "$id" ] && printf '%-12s %-9s %-6s blocked-when: %s\n' "" "" "" "$bpat"
  328 +       [ "$bid" = "$id" ] && printf '%-12s %-9s %-6s blockedwhen: %s\n' "" "" "" "$bpat"
  427 -       printf 'ok   %-12s evidence: %s\n' "$id" "$exp"
  427 +       printf 'ok   %-12s evidence  %s\n' "$id" "$exp"

    FAIL beside the blocked-when line
         expected to contain: blocked-when: no CUDA-capable device
         actual:               integration  optional  .      printf "26 passed in 7.1s\n"
                                       evidence  [1-9][0-9]* passed
                                       floor:  26
                                       blockedwhen: no CUDA-capable device
    FAIL beside the floor
         expected to contain: floor:    26
         actual:               integration  optional  .      printf "26 passed in 7.1s\n"
                                       evidence  [1-9][0-9]* passed
                                       floor:  26
                                       blockedwhen: no CUDA-capable device
    FAIL and the evidence regex
         expected to contain: evidence: [1-9][0-9]* passed
         actual:               integration  optional  .      printf "26 passed in 7.1s\n"
                                       evidence  [1-9][0-9]* passed
                                       floor:  26
                                       blockedwhen: no CUDA-capable device

=== mutate: command exited 1; restored (verified byte-for-byte against .../.claude/state/mutations/scripts_gates.sh.20260917T161646Z.3503887.bak) ===
```

After all five, `git status --porcelain scripts/gates.sh` is empty.

### R-6 — a return to RED from GATES: test 36 cannot fail

*Recorded by the Lead PO orchestrator, 2026-09-17, on the return. Phase set back
to RED before this was written.*

**The defective assertion**, `.claude/tests/gates.test.sh` L889, in the DV-2
block:

```
int_conf required 'printf "1 passed, 25 skipped in 0.46s\nINTERNALERROR: the run aborted\n"; exit 2'
out="$(gates)"; rc=$?
assert_contains     "a non-zero exit fails even when the log matches the skip pattern" "FAIL         integration" "$out"
assert_not_contains "and is not a block"            "BLOCKED      integration" "$out"
assert_not_contains "and not a declared non-result" "KNOWN        integration" "$out"   # <- this one
```

**What is wrong with it.** The fixture is `required`. In the `environment` arm a
required gate reports `BLOCKED`, never `KNOWN` - so `KNOWN` is excluded by the
fixture's own `required` setting, independently of the `rc != 0` guard the
assertion exists to pin. It asserts something true and cannot observe it being
false.

**How it was found.** GREEN declined to claim it (see `### GATES's answer` under
`## Deferred verifications`): its targeted rc-guard mutation
`477s/$rc" -ne 0/$rc" -eq 1/` turned tests 34, 35 and 37 red and left 36 green.

**Independently reproduced by the orchestrator, with a different expression, and
the result is stronger than the report.** Rather than re-running GREEN's
mutation, I attacked the other half of the conjunction - the required/optional
split in the new arm:

```
$ bash scripts/mutate.sh scripts/gates.sh '555s/BLOCKED      /KNOWN        /' \
    -- bash scripts/selftest.sh gates

    FAIL a gate the story leans on is BLOCKED
gates: 159 passed, 1 failed
=== mutate: command exited 1; restored (verified byte-for-byte against
    .../scripts_gates.sh.20260917T221814Z.293757.bak) ===
```

Test 36 stayed **green while the run's output literally contained
`KNOWN        integration`** for a required gate. So neither single-point
mutation reaches it: it needs the rc-guard broken *and* the required branch
emitting `KNOWN`. An assertion no single mutation can turn red is vacuous by
this repository's own definition - which is precisely the defect this story
exists to fix, one level up.

**What it should assert instead.** DV-2's claim is *the new arm is unreachable
on the `rc != 0` path*. On a **required** fixture the tempting wrong answer is
`BLOCKED`, and test 35 already pins that. The `KNOWN` half of the claim needs an
**optional** fixture, where `KNOWN` is what a broken rc-guard would actually
produce. So: keep 34/35/37 as they are, and add an optional twin of the DV-2
fixture - same command, same log, `int_conf optional` - asserting `WARN` and
**not** `KNOWN`. That assertion is reachable by the single mutation GREEN
already ran, and must be earned with it.

**Scope of this return.** The defective assertion and its twin, nothing else.
`scripts/gates.sh` is correct and is frozen again by the lock; source is not to
be touched. The gate record stamped at tree `25233f21` is superseded by this
return and will be re-run at GATES.

#### RED's answer on the return, 2026-09-17

**What changed, and it is only this.** In `.claude/tests/gates.test.sh`:

- L889 — `assert_not_contains "and not a declared non-result"` on the
  **required** DV-2 fixture (test 36) — is **removed**.
- An **optional twin** of the DV-2 fixture is added immediately after the
  required one: same command, same log, `int_conf optional`. It asserts
  `WARN         integration` and **not** `KNOWN        integration` — tests
  **37a** and **37b** in `## Test plan`.

Nothing else in the file; nothing outside it. `scripts/gates.sh` is
byte-identical to what GATES stamped, before and after the probe below:
`sha256sum scripts/gates.sh` =
`3a34002991d65814c69a26928d9f13a6151b4ba6729de30a29b511a73649988c`.

**Why L889 is removed rather than kept beside the twin.** The orchestrator's
instruction left this open, so here is the argument.

On a **required** fixture, DV-2's claim — *the `skipped-when` arm is unreachable
when `rc != 0`* — has exactly one observable consequence, and test 35 (`and is
not a block`) already carries it: a broken rc guard sends that log into the
`environment` arm, and for a required gate that arm prints `BLOCKED`. `KNOWN` is
not the wrong answer the required fixture can give; it is an answer the fixture's
own `required` setting forbids in *every* arm that can print it (`noevidence`,
`fail`, `environment`, `blocked` all reach `KNOWN` only through the
`[ -n "$waiver" ]` branch, and this fixture has no waiver). Reaching it needs two
simultaneous faults — the rc guard broken *and* the required/waiver dispatch
broken — which is why neither GREEN's mutation nor the orchestrator's reached it.
So L889 is not merely unearnable: on this fixture it is **redundant with test
35** for the half of the claim that can be observed, and inert for the half that
cannot.

Removing it is therefore not "deleting a case to reach green": nothing was red,
the suite's verdict is unchanged, no assertion is weakened, and the claim it was
meant to carry moves to a fixture that can actually observe it being false —
where it is now pinned by a single-point mutation. Keeping it alongside the twin
would leave a line in the file that looks like a negative control, reads like one
in review, and cannot fail; that is precisely the vacuity this story exists to
remove one level up, and a suite about vacuous PASSes is the wrong place to keep
an example of one.

**The probe that earns the twin.** "Watch it fail" cannot apply — `gates.sh`
already exists and the twin was green on its first execution — so it is earned
with GREEN's own rc-guard mutation, the one that left test 36 green. Line 477 is
still the rc guard (`if [ "$rc" -ne 0 ]; then`). One mutation, one run, one
revert:

```
$ bash scripts/mutate.sh scripts/gates.sh '477s/\$rc" -ne 0/$rc" -eq 1/' \
    -- bash scripts/selftest.sh gates

=== mutate: scripts/gates.sh (1 line(s) changed by 477s/\$rc" -ne 0/$rc" -eq 1/) ===
  477 -   if [ "$rc" -ne 0 ]; then
  477 +   if [ "$rc" -eq 1 ]; then

  skipped-when: a gate that skipped its work stops reporting PASS
    FAIL a non-zero exit fails even when the log matches the skip pattern
         expected to contain: FAIL         integration
         actual:
         === gate: integration (required) ===
         ...
         BLOCKED      integration (0s, did 1 units of work, below the floor of 26 in project.conf; the log says 25 skipped, so the work was skipped rather than lost: the environment did not supply it) -> .claude/state/gate-logs/integration.log
    FAIL and is not a block
         expected NOT to contain: BLOCKED      integration
    FAIL and the run exits 1
         expected: 1
         actual:   3
    FAIL optional: a non-zero exit warns even when the log matches the skip pattern
         expected to contain: WARN         integration
         actual:
         === gate: integration (optional) ===
         printf "1 passed, 25 skipped in 0.46s\nINTERNALERROR: the run aborted\n"; exit 2
         1 passed, 25 skipped in 0.46s
         INTERNALERROR: the run aborted

         --- gate summary ---
         KNOWN        integration (0s, did 1 units of work, below the floor of 26 in project.conf; the log says 25 skipped, so the work was skipped rather than lost: the environment did not supply it) -> .claude/state/gate-logs/integration.log

         All required gates passed (1 ran, 0 unconfigured, 1 known).
    FAIL and is not excused as a declared non-result on the rc != 0 path
         expected NOT to contain: KNOWN        integration
         actual:
         === gate: integration (optional) ===
         ...
         KNOWN        integration (0s, did 1 units of work, below the floor of 26 in project.conf; the log says 25 skipped, so the work was skipped rather than lost: the environment did not supply it) -> .claude/state/gate-logs/integration.log

gates: 146 passed, 15 failed

1 of 1 harness suite(s) FAILED.

=== mutate: command exited 1; restored (verified byte-for-byte against
    .../.claude/state/mutations/scripts_gates.sh.20260917T232900Z.338696.bak) ===
  477:   if [ "$rc" -ne 0 ]; then
```

The line that matters is the last one of the four: **`KNOWN        integration`
in the output, and the assertion red because of it.** That is the discrimination
test 36 could not make on a required fixture, made on an optional one by the
single mutation DV-2 is about. (Tests 34, 35 and 37 go red under the same
mutation, as GREEN reported; the required half of the DV-2 block is unchanged.)

**Counts.** Baseline before the change: `gates: 160 passed, 0 failed`. After
(one assertion removed, two added): `gates: 161 passed, 0 failed`. Under the
mutation above: `146 passed, 15 failed` — GREEN measured 13 reds against the old
file, and the two extra are the twin.

**Test 6, checked while here, and NOT changed.** The orchestrator asked whether
test 6 (`AC-2: and the run exits 0`) is vacuous for the same structural reason.
It is not the same reason, and the difference is the one that decides whether to
act:

- Test 36 could be turned red by **no** single-point mutation: it needed two
  independent faults, so nothing about it was observable at all.
- Test 6 *is* reachable, but only through the exit-code plumbing every gate
  shares. `fails` and `blocked` — the only two counters that move the exit code
  (`gates.sh` L863–867) — are incremented exclusively inside `[ "$req" =
  "required" ]` branches, so on test 6's **optional** fixture every outcome
  (`PASS`, `WARN`, `KNOWN`) exits 0 alike. A mutation of the pass/floor logic it
  is nominally about therefore cannot move it; only a mutation of the exit
  arithmetic can, and that turns a large fraction of the suite red at once —
  GREEN's `477s/$rc" -ne 0/$rc" -ne 99/` attempt is the shape of it, `120 passed,
  40 failed`.

So test 6 is **coupled, not unreachable**: it holds a true and cheap invariant
(an optional gate never breaks the build) that no *targeted* mutation isolates.
That is what RED's own table said — "coupled to 4 and 5" — and what GATES
confirmed. It is a weaker assertion than it looks, not a vacuous one, and fixing
it would mean a new fixture asserting an AC-2 claim AC-2 does not make. Left
alone deliberately, and reported rather than expanded into, per the return's
scope.

## Deferred verifications

<!-- Written by the Lead PO at PLANNED, per the /advance-story protocol: the
     verifications RED cannot perform, each as a falsifiable condition with an
     owning phase. RED is expected to DECLINE these in its handoff, not claim
     them. check-boundaries.sh refuses a PR whose entries name no phase, or that
     reaches REVIEW with neither a pasted result nor an explicit WAIVED. -->

- **DV-1 — the floor and `skipped-when` against the REAL `integration` gate.**
  Every criterion here is checked against `make_project_fixture` with `printf`
  gate commands (C-6), which is the right test bed and is *not* the thing the
  rule guards. C-8's P-1, P-2 and P-3 break the real gate. **RED cannot run
  these:** P-2 needs a checkout that actually has `spikes/`, and this worktree
  does not (PO-1) — which is the finding. **Owner: GATES.** Falsifiable: P-2
  must show `KNOWN integration` with `25 skipped` in the line and exit 0, then
  `PASS … observed 26, floor 26` after the directory is renamed back.
- **DV-2 — that the new outcome arm cannot be reached when `rc != 0`.** C-1 says
  `skipped-when` is consulted only on a gate that exited 0 and came in below its
  floor. RED can assert that with a fixture whose command exits non-zero *and*
  whose log matches the skip pattern — expected: `FAIL`, not `BLOCKED`/`KNOWN`.
  If RED writes that case, DV-2 is discharged in RED and says so. **Owner: RED**,
  falling through to **GATES** if RED declines it.
- **DV-3 — the model-guidance success condition.** Mutating the `skipped-when`
  match in `scripts/gates.sh` so that it always matches, via
  `bash scripts/mutate.sh`, must turn **AC-5's** case red and leave AC-1's,
  AC-3's and AC-4's green. There is nothing to mutate until GREEN has written
  it. **Owner: GATES.** A suite that stays green under that mutation cannot tell
  a classified shortfall from an excused one, and the story is not done.

### RED's answer to each, 2026-09-17

- **DV-1 — DECLINED, in those words.** RED cannot run it. P-2 needs a checkout
  carrying `spikes/`; this worktree does not have one (`ls -d spikes` → no such
  file), which is PO-1's finding and the reason the story exists. P-1 and P-3
  edit `.claude/harness/project.conf`, which is frozen to RED by the phase lock
  and by `rules.md` path ownership. Left to **GATES**, unchanged.
- **DV-2 — DISCHARGED in RED, with one caveat stated plainly.** The case is
  written: `## Test plan` tests 34-37, a fixture whose command is
  `printf "1 passed, 25 skipped in 0.46s\nINTERNALERROR: the run aborted\n"; exit 2`
  on a `required` gate with `floor | integration | 26` and
  `skipped-when | integration | [1-9][0-9]* skipped`. It demands
  `FAIL         integration` and exit 1, and refuses both `BLOCKED` and `KNOWN`.
  It was earned by probe **M3** in `## Regressions`, which makes the skip pattern
  reachable on the `rc != 0` path and turns it red with
  `BLOCKED      integration (0s, exit 2, could not launch: 25 skipped)`.
  **The caveat:** in RED the new arm does not exist, so the case currently passes
  because `gates.sh` has no `skipped-when` behaviour at all. Its force begins the
  moment GREEN writes the arm. **GREEN must confirm it is still
  `FAIL`/exit 1 after the arm exists** — that is one line of the same run and it
  is what turns this from a written case into a discharged verification.

  M3's red, pasted here so this entry carries its own result rather than a
  pointer to one:

  ```
  $ bash scripts/mutate.sh scripts/gates.sh 's/blockpat="/blockpat="[1-9][0-9]* skipped|/' \
      -- bash scripts/selftest.sh gates
    465 -     blockpat="$BLOCKED_DEFAULT"
    465 +     blockpat="[1-9][0-9]* skipped|$BLOCKED_DEFAULT"

      FAIL a non-zero exit fails even when the log matches the skip pattern
           expected to contain: FAIL         integration
           actual:
           --- gate summary ---
           BLOCKED      integration (0s, exit 2, could not launch: 25 skipped) -> .claude/state/gate-logs/integration.log
      FAIL and the run exits 1
           expected: 1
           actual:   3

  === mutate: command exited 1; restored (verified byte-for-byte against ...) ===
    465:     blockpat="$BLOCKED_DEFAULT"
  ```
- **DV-3 — DECLINED, in those words.** There is nothing to mutate: the
  `skipped-when` match is what GREEN is about to write. RED's `## Handoff` names
  the seven assertions whose red depends on it, so GATES can check the mutation
  hits exactly those and leaves AC-1's, AC-3's and AC-4's green. **Owner: GATES**,
  unchanged.

### GREEN's answer, 2026-09-17

- **DV-1 — still DECLINED, and still correct.** This worktree has no `spikes/`
  (`ls -d spikes` → no such file), so P-1, P-2 and P-3 remain GATES's. What GREEN
  *can* report is the half DV-1 does not need the data for, run against the real
  manifest rather than a fixture — PO-1's own command, after the fix:

  ```
  $ bash scripts/gates.sh --gate integration
  === gate: integration (optional) ===
  uv run pytest -q tests/integration -m "gpu or network"
  sssssssssssssssss.ssssssss                                               [100%]
  1 passed, 25 skipped in 1.69s

  --- gate summary ---
  KNOWN        integration (6s, did 1 units of work, below the floor of 26 in project.conf; the log says 25 skipped, so the work was skipped rather than lost: the environment did not supply it) -> .claude/state/gate-logs/integration.log

  (not recorded in the story: a partial run is not evidence of anything)

  All required gates passed (1 ran, 0 unconfigured, 1 known).
  exit=0
  ```

  PO-1's `PASS integration (9s, observed 1)` is gone from the machine PO-1 ran
  on, which is AC-1 against the real manifest rather than against a fixture copy
  of it. It does **not** discharge DV-1: P-2's second half (`PASS … observed 26,
  floor 26` after the data is restored) still needs a checkout carrying
  `spikes/`, and that is GATES's.

- **DV-2 — CONFIRMED against the shipped arm, as GREEN was asked to.** RED's
  caveat was that tests 34–37 passed only because no `skipped-when` behaviour
  existed. The arm now exists, and the same fixture still reports `FAIL`/exit 1:

  ```
  gate         | integration | required | . | printf "1 passed, 25 skipped in 0.46s\nINTERNALERROR: the run aborted\n"; exit 2
  evidence     | integration | [1-9][0-9]* passed
  floor        | integration | 26
  skipped-when | integration | [1-9][0-9]* skipped

  === gate: integration (required) ===
  1 passed, 25 skipped in 0.46s
  INTERNALERROR: the run aborted

  --- gate summary ---
  FAIL         integration (0s, exit 2) -> .claude/state/gate-logs/integration.log

  1 required gate(s) failed.
  --- exit=1 ---
  ```

  The log matches `[1-9][0-9]* skipped` and the gate has a floor, and neither
  `BLOCKED` nor `KNOWN` appears: the new arm lives inside the `rc = 0` branch's
  `[ -n "$floor" ]` block and the `rc != 0` path never reaches it. **DV-2 is
  discharged.**

- **DV-3 — still GATES's**, unchanged. GREEN wrote the match; nothing here
  mutates it.

### GREEN: the negative controls, re-measured against the shipped code

RED measured its control table against the pipelines "as they stand today". Each
row re-measured here against `clean_log`/`work_count` as they stand *after* this
story's edits (extracted from `scripts/gates.sh` itself, not retyped), with the
live `evidence` regex `[1-9][0-9]* passed` and the live `skipped-when` pattern:

| Control (test #) | `observed` | skip-pattern match | Agrees with RED |
|---|---|---|---|
| AC-1/AC-4/AC-3 (3, 9, 17) `1 passed, 25 skipped in 0.46s` | 1 | `25 skipped` | yes |
| AC-2 (4) `26 passed in 7.1s` | 26 | *(none)* | yes |
| C-1 at floor (7) `26 passed, 3 skipped in 7.1s` | 26 | `3 skipped` | yes |
| AC-5 (25, 29) `3 passed in 0.4s` | 3 | *(none)* | yes |
| AC-5/C-3 (32) `3 passed, 23 deselected in 0.4s` | 3 | *(none)* | yes |
| C-1 alternation (38) `1 passed, 0 skipped` + `no data files were supplied` | 1 | `no data files were supplied` | yes |
| DV-2 (34) `1 passed, 25 skipped` + `INTERNALERROR` | 1 (unused; `rc != 0`) | `25 skipped` | yes |

No divergence in any row. The two that carry the story behave as RED said they
would: `deselected` measures **no** match, so AC-5's regression case is a real
discriminator; and the alternation's *second* branch is the one that matches,
which is what makes test 38 fail against a parser using `cut -f3` — and the
parser was already `-f3-` (C-1's claim verified at the source, `scripts/gates.sh`
L106, comment included, so no change was needed there).

### GATES's answer, 2026-09-17

- **DV-1 - DISCHARGED.** P-1, P-2 and P-3 were all run against the real
  `integration` gate and are pasted in `## Gate probes`. The falsifiable
  condition DV-1 named is met exactly: with the data renamed away the gate
  reports `KNOWN integration (1s, did 1 units of work, below the floor of 26 in
  project.conf; the log says 25 skipped, ...)` and exits 0, and with it renamed
  back it reports `PASS integration (8s, observed 26, floor 26)`. The
  story-escalated half is there too - `BLOCKED integration (required by story
  MT-037) ...`, exit 3. The environment RED could not have was supplied for
  GATES as a gitignored symlink to the main checkout's `spikes/`; it is not
  committed and `git status` never saw it.
- **DV-3 - DISCHARGED, and the success condition holds exactly.** The
  `skipped-when` match in `scripts/gates.sh` L509 was made unconditional through
  `mutate.sh` and `bash scripts/selftest.sh gates` run against it. Baseline is
  `gates: 160 passed, 0 failed`; under the mutation it is **153 passed, 7
  failed**, and all seven reds are AC-5's - the negative control that stops the
  new line from excusing every shortfall. AC-1's (test 3), AC-3's (17-24) and
  AC-4's (9-15) stayed green, as the guidance required.

  ```
  $ bash scripts/mutate.sh scripts/gates.sh \
      '509s#clean_log "$log" | grep -Eq -- "$skippat"#true#' \
      -- bash scripts/selftest.sh gates

  === mutate: scripts/gates.sh (1 line(s) changed) ===
    509 -         if [ -n "$skippat" ] && clean_log "$log" | grep -Eq -- "$skippat"; then
    509 +         if [ -n "$skippat" ] && true; then

    skipped-when: a gate that skipped its work stops reporting PASS
      FAIL required: a shortfall the pattern does not match still FAILs
           expected to contain: FAIL         integration
           actual:
           --- gate summary ---
           BLOCKED      integration (required by story T-1) (0s, did 3 units of work, below the floor of 26 in project.conf; the log says , so the work was skipped rather than lost: the environment did not supply it) -> .claude/state/gate-logs/integration.log
      FAIL and is not reported as a block
           expected NOT to contain: BLOCKED      integration
      FAIL and the run exits 1
           expected: 1
           actual:   3
      FAIL optional: the same shortfall still WARNs
           expected to contain: WARN         integration
           actual:
           --- gate summary ---
           KNOWN        integration (0s, did 3 units of work, below the floor of 26 in project.conf; the log says , so the work was skipped rather than lost: the environment did not supply it) -> .claude/state/gate-logs/integration.log
      FAIL and is not excused as a declared non-result
           expected NOT to contain: KNOWN        integration
      FAIL a deselected shortfall is a manifest regression, not an environment
           expected to contain: WARN         integration
           actual:
           --- gate summary ---
           KNOWN        integration (0s, ... the log says , so the work was skipped rather than lost ...)
      FAIL so the skip pattern does not classify it
           expected NOT to contain: KNOWN        integration

  gates: 153 passed, 7 failed

  === mutate: command exited 1; restored (verified byte-for-byte against
      .../scripts_gates.sh.20260917T214119Z.209375.bak) ===
    509:         if [ -n "$skippat" ] && clean_log "$log" | grep -Eq -- "$skippat"; then
  ```

  Reds: tests 25, 27, 28, 29, 30, 32, 33. Test 26 ("with the floor wording it has
  always had") correctly stays green - the mutation *extends* the floor wording
  rather than replacing it, which is the shared-prefix property C-2 asked for.

#### The seven assertions RED declared unearnable: where each one now stands

RED's `## Handoff` named DV-3's mutation as "the probe that earns them". It earns
three of them. Two more were earned by two supplementary mutations run here; two
remain unearned, and are reported rather than claimed.

| # | Assertion | Earned by | Status |
|---|---|---|---|
| 27 | AC-5: and is not reported as a block | DV-3 | **earned** |
| 30 | AC-5: and is not excused as a declared non-result | DV-3 | **earned** |
| 33 | AC-5/C-3: so the skip pattern does not classify it | DV-3 | **earned** |
| 8 | C-1: and is not turned into a declared non-result | supplement A | **earned** |
| 46, 48 | AC-6: a well-formed skipped-when passes the audit / exits 0 | supplement B | **earned** |
| 6 | AC-2: and the run exits 0 | - | unearned, coupled (as RED said) |
| 36 | DV-2: and not a declared non-result | - | unearned, and structurally so - see below |

**Supplement A** - the floor comparison, which RED's M1 probed *before* the
`KNOWN` arm existed and which now reaches it:

```
$ bash scripts/mutate.sh scripts/gates.sh 's/observed" -lt/observed" -le/' \
    -- bash scripts/selftest.sh gates
    FAIL a machine that has the data still passes
    FAIL naming both the count and the floor
    FAIL a gate at its floor passes even though tests skipped
    FAIL and is not turned into a declared non-result      <- test 8, newly earned
gates: 156 passed, 4 failed
=== mutate: command exited 1; restored (verified byte-for-byte against ...) ===
```

With the comparison at `-le`, the at-floor fixture (`26 passed, 3 skipped`) becomes
a shortfall whose log matches the skip pattern, so it turns into `KNOWN` - which
is exactly what test 8 forbids. C-1's "never consulted at or above the floor" is
now pinned by a failing observation.

**Supplement B** - the audit's no-pattern guard inverted, so a well-formed line is
refused:

```
$ bash scripts/mutate.sh scripts/gates.sh '619s/\[ -n "\$kpat" \]/[ -z "$kpat" ]/' \
    -- bash scripts/selftest.sh gates
    FAIL a skipped-when with no pattern fails the audit
    FAIL and the audit exits non-zero
    FAIL a skipped-when on a gate with no floor fails the audit
    FAIL a well-formed skipped-when passes the audit       <- test 46, newly earned
    FAIL and the audit exits 0                             <- test 48, newly earned
gates: 155 passed, 5 failed
=== mutate: command exited 1; restored (verified byte-for-byte against ...) ===
```

**Test 36 cannot be earned, and the reason is worth recording.** Two mutations
were tried to reach it. `477s/$rc" -ne 0/$rc" -ne 99/` sends *every* gate down
the failure path (`gates: 120 passed, 40 failed`) and leaves 34-37 green, so it
measures nothing about DV-2. The targeted one does reach the new arm:

```
$ bash scripts/mutate.sh scripts/gates.sh '477s/\$rc" -ne 0/$rc" -eq 1/' \
    -- bash scripts/selftest.sh gates
    FAIL a non-zero exit fails even when the log matches the skip pattern
    FAIL and is not a block
    FAIL and the run exits 1
gates: 147 passed, 13 failed
=== mutate: command exited 1; restored (verified byte-for-byte against ...) ===
```

The rc=2 fixture now falls into the floor branch and its log matches the skip
pattern - tests 34, 35 and 37 go red - but test 36 stays green, because that
fixture is a **required** gate, and a required `environment` outcome is `BLOCKED`,
never `KNOWN`. Test 36 asserts the absence of a token the fixture's own
`required` setting already excludes. It is not wrong and it costs nothing, but it
cannot discriminate as written; an optional twin of the DV-2 fixture is what
would earn it. Recorded here rather than dressed up as earned. (Test 35 carries
the discriminating half of the same claim and *is* earned, by RED's M3 and again
here.)

All five mutations in this section restored their file and said so; `sha256sum
scripts/gates.sh` is `3a34002991d65814c69a26928d9f13a6151b4ba6729de30a29b511a73649988c`
before and after every one of them.

## Gate results

<!-- gates.sh: written by bash scripts/gates.sh; do not edit or paste by hand -->

    run:    2026-09-18T05:12:49Z
    commit: 5a51681
    tree:   c524736449264faab1b80c78d19bafc9cc88f09b
    result: pass (9 ran, 0 unconfigured, 2 known)

    PASS         format (4s, observed 71)
    PASS         lint (3s, observed 5, floor 5)
    PASS         typecheck (2s, observed 39, floor 28)
    PASS         unit (19s, observed 421, floor 292)
    PASS         coverage (20s, observed 421, floor 292)
    PASS         coverage-core (17s, observed 416, floor 287)
    KNOWN        integration (1s, did 1 units of work, below the floor of 26 in project.conf; the log says 25 skipped, so the work was skipped rather than lost: the environment did not supply it) -> .claude/state/gate-logs/integration.log
    PASS         build (53s)
    KNOWN        mutation (1s, exit 2; mutmut is not installed and not configured until the suite is real; stack.md §6) -> .claude/state/gate-logs/mutation.log

## Gate probes

<!-- REQUIRED: this story changes a gate. The PLAN is in ## Contract C-8; the
     pasted output is written here in GATES. check-boundaries.sh refuses a PR
     whose ## Gate probes describes a failure without showing one. -->

Run in this worktree on 2026-09-17 by the Feature Developer at GATES, against
`scripts/gates.sh` and `.claude/harness/project.conf` exactly as this story
leaves them (`sha256 project.conf 2c09f6da...`, `sha256 gates.sh 3a340029...`,
re-checked identical after every probe). The orchestrator supplied the missing
environment for the duration of GATES: `spikes/` exists here as a **symlink to
the main checkout's copy**, which `.gitignore:81` covers, so it is invisible to
`git status` and cannot reach a commit. Every probe below was applied and
reverted through `bash scripts/mutate.sh`, which restored each file and verified
the restore byte-for-byte; the `mv` of the symlink in P-2 is undone in the same
script, under a trap.

All 26 integration tests are `@pytest.mark.gpu`; none is `network` (MT-011,
which brings the API, is still PLANNED). So these probes cost GPU time and no
money.

### P-1 - a shortfall the log calls `deselected` is NOT excused by `skipped-when`

**C-8's mechanism no longer produces the shape P-1 exists to test, and this is
reported rather than quietly followed.** C-8 says to narrow the gate command to
`-m "network"`. Measured: `-m "network"` collects **zero** tests
(`26 deselected`), pytest exits **5**, and `gates.sh` therefore never reaches the
floor logic at all - it takes the `rc != 0` path, which is the *existing*
mechanism and neither the floor nor `skipped-when`:

```
$ bash scripts/mutate.sh .claude/harness/project.conf \
    '302s#-m "gpu or network"#-m "network"#' -- bash scripts/gates.sh --gate integration
  302 + gate | integration   | optional | . | uv run pytest -q tests/integration -m "network"

=== gate: integration (optional) ===
uv run pytest -q tests/integration -m "network"

26 deselected in 0.71s

--- gate summary ---
WARN         integration (2s, exit 5, optional) -> .claude/state/gate-logs/integration.log

=== mutate: command exited 0; restored (verified byte-for-byte against ...) ===
  302: gate | integration   | optional | . | uv run pytest -q tests/integration -m "gpu or network"
```

That is a WARN for the right reason but by the *wrong mechanism*: it proves
nothing about the floor, nothing about `skipped-when`, and the manifest did the
same thing before MT-037. (It is not even the `noevidence` shortfall one would
predict from `0 passed`: pytest exits 5 on "no tests collected", so the run never
gets as far as the evidence regex.)

**Substitution, and the reason.** Narrow by keyword instead, keeping a non-zero
pass count with the remainder deselected - which is the shape P-1 is actually
about. `--collect-only` first: `5/26 tests collected (21 deselected)`.

```
$ bash scripts/mutate.sh .claude/harness/project.conf \
    '302s#-m "gpu or network"#-m "gpu or network" -k "detector_session"#' \
    -- bash scripts/gates.sh --gate integration

=== mutate: .claude/harness/project.conf (1 line(s) changed) ===
  302 - gate | integration   | optional | . | uv run pytest -q tests/integration -m "gpu or network"
  302 + gate | integration   | optional | . | uv run pytest -q tests/integration -m "gpu or network" -k "detector_session"

=== gate: integration (optional) ===
uv run pytest -q tests/integration -m "gpu or network" -k "detector_session"
.....                                                                    [100%]
5 passed, 21 deselected in 2.82s

--- gate summary ---
WARN         integration (6s, did 5 units of work, below the floor of 26 in project.conf, optional)

1 optional gate(s) WARNed. A WARN means something changed since the last run:
read it. ...

=== mutate: command exited 0; restored (verified byte-for-byte against ...) ===
  302: gate | integration   | optional | . | uv run pytest -q tests/integration -m "gpu or network"
```

**Exactly what C-3 demands.** `observed 5` is below the floor of 26; the log says
`deselected`, not `skipped`; the `skipped-when` pattern does not match; and the
gate reports the plain floor shortfall it has always reported - `WARN` because
the gate is optional here, and **not** `KNOWN`, **not** `BLOCKED`. The manifest
selecting fewer tests stays a regression.

### P-2 - `skipped-when` against the real 26-test suite

The probe that breaks the real thing the new rule guards. Four runs, in order.

**1. Data present - the gate does its work.**

```
$ bash scripts/gates.sh --gate integration
=== gate: integration (optional) ===
uv run pytest -q tests/integration -m "gpu or network"
..........................                                               [100%]
26 passed in 11.51s

--- gate summary ---
PASS         integration (13s, observed 26, floor 26)

All required gates passed (1 ran, 0 unconfigured, 0 known).
--- exit=0 ---
```

**2. Data renamed away, no story escalating the gate - `KNOWN`, exit 0 (AC-4).**
`mv spikes spikes.probe` renames the *symlink*, never the main checkout's data:

```
$ mv spikes spikes.probe && bash scripts/gates.sh --gate integration
ls: cannot access 'spikes': No such file or directory
lrwxrwxrwx 1 ryanc 197609 47 Sep 17 15:56 spikes.probe -> /c/Users/ryanc/Projects/manga-translator/spikes

=== gate: integration (optional) ===
uv run pytest -q tests/integration -m "gpu or network"
sssssssssssssssss.ssssssss                                               [100%]
1 passed, 25 skipped in 0.43s

--- gate summary ---
KNOWN        integration (1s, did 1 units of work, below the floor of 26 in project.conf; the log says 25 skipped, so the work was skipped rather than lost: the environment did not supply it) -> .claude/state/gate-logs/integration.log
...
All required gates passed (1 ran, 0 unconfigured, 1 known).
--- exit=0 ---
```

This is PO-1's exact log - `1 passed, 25 skipped` - against the real manifest,
and PO-1's `PASS integration (9s, observed 1)` is gone. AC-1 and AC-4 on the real
gate rather than on a fixture copy of it.

**3. The same starved run, with the story escalating the gate - `BLOCKED`, exit 3
(AC-3).** `required_gates` was flipped to `[integration]` through `mutate.sh`,
which restored the frontmatter afterwards and verified it; a `--gate` run is
partial, so nothing was written to `## Gate results`:

```
$ bash scripts/mutate.sh docs/backlog/stories/MT-037.md \
    '11s/required_gates: \[\]/required_gates: [integration]/' \
    -- bash scripts/gates.sh --gate integration
  11 + required_gates: [integration]  # deliberately empty; see ## Notes PO-5 ...

=== gate: integration (required (required by story MT-037)) ===
uv run pytest -q tests/integration -m "gpu or network"
sssssssssssssssss.ssssssss                                               [100%]
1 passed, 25 skipped in 0.49s

--- gate summary ---
BLOCKED      integration (required by story MT-037) (2s, did 1 units of work, below the floor of 26 in project.conf; the log says 25 skipped, so the work was skipped rather than lost: the environment did not supply it) -> .claude/state/gate-logs/integration.log
...
1 required gate(s) could not run: the environment refused to launch them.
This is not a failure and not a pass. ...

=== mutate: command exited 3; restored (verified byte-for-byte against ...) ===
  11: required_gates: []  # deliberately empty; see ## Notes PO-5 ...
--- exit=3 ---
```

**4. Data back - the gate passes again.**

```
$ mv spikes.probe spikes && bash scripts/gates.sh --gate integration
lrwxrwxrwx 1 ryanc 197609 47 Sep 17 15:56 spikes -> /c/Users/ryanc/Projects/manga-translator/spikes

=== gate: integration (optional) ===
..........................                                               [100%]
26 passed in 6.79s

--- gate summary ---
PASS         integration (8s, observed 26, floor 26)
--- exit=0 ---
```

`git status --porcelain` after the sequence: the same six modified files and
nothing else; `required_gates: []` restored; both file hashes unchanged.

**One side effect of the probe, recorded because it is not obvious.** While the
data was renamed to `spikes.probe/`, that path stopped matching `.gitignore:81`
(`spikes/`), so the `changes:` check saw 107 newly untracked source paths and
printed `WARN changes: spikes.probe/... is exercised by no gate with a covers
line` for each, plus `107 changed source path(s) match no covers line`. That is
an artefact of the rename, not of this story: it vanished the moment the symlink
was renamed back, and it changed neither verdict nor either exit code. A future
run of this probe should prefer a rename target `.gitignore` already covers.

### P-3 - the three audit refusals (AC-6)

One at a time, appended to `.claude/harness/project.conf` through `mutate.sh`,
with `bash scripts/gates.sh --audit` run against each, then reverted and
verified.

**(a) names no configured gate** - `skipped-when | integratoin | [1-9][0-9]* skipped`:

```
ok   integration  evidence: [1-9][0-9]* passed
                  floor:  26
                  skipped-when: [1-9][0-9]* skipped
                  slow:   loads real .onnx models onto the GPU and calls a paid API
...
FAIL integratoin  a `skipped-when` line names no configured gate

1 manifest problem(s).
=== mutate: command exited 1; restored (verified byte-for-byte against ...) ===
```

The `ok integration ... skipped-when: [1-9][0-9]* skipped` block above the
refusal is AC-6's positive control and AC-7's audit print, on the real manifest.

**(b) no pattern** - `skipped-when | integration |`:

```
FAIL integration  a `skipped-when` line has no pattern
1 manifest problem(s).
=== mutate: command exited 1; restored (verified byte-for-byte against ...) ===
```

**(c) a gate with no floor** - `skipped-when | build | [1-9][0-9]* skipped`:

```
FAIL build        a `skipped-when` line names a gate with no `floor` line: there is nothing to measure the shortfall it would classify
1 manifest problem(s).
=== mutate: command exited 1; restored (verified byte-for-byte against ...) ===
```

Each refusal makes `--audit` exit 1. The pre-existing `WARN mutation  no evidence
line` and its advisory appear in all three runs and in a clean one alike; they
are not this story's and are unchanged by it.

## Notes

### PO-1 — orchestrator probe, PLANNED, 2026-09-17: the vacuous PASS, on the real repo

Not a fixture. `bash scripts/gates.sh --gate integration`, run by the
orchestrator in this worktree
(`.claude/worktrees/quizzical-sanderson-f9d214`) against the manifest exactly as
it stands:

```
=== gate: integration (optional) ===
uv run pytest -q tests/integration -m "gpu or network"
sssssssssssssssss.ssssssss                                               [100%]
1 passed, 25 skipped in 2.71s

--- gate summary ---
PASS         integration (9s, observed 1)

All required gates passed (1 ran, 0 unconfigured, 0 known).
```

**And the reason it reproduced here is the finding underneath the finding.**
`spikes/` does not exist in this worktree — `ls -d spikes` → *No such file or
directory* — while `ls -d ../../../spikes` in the main checkout resolves. A git
worktree shares `.git` and not the ignored working files. This harness dispatches
its work into `.claude/worktrees/**`. So the vacuous PASS is not a CI curiosity:
it is the **default condition of every agent working in this repository the way
the harness runs agents**, and MT-036's `observed 26` was obtained from a
checkout that happened to carry the data.

### PO-2 — orchestrator probe, PLANNED: `required_gates` does nothing on CI

Fixture (`make_project_fixture`), same tree, same `project.conf` carrying
`floor | integration | 26`, story `T-1` with `required_gates: [integration]`.
The only variable is whether `.claude/state/current-story.env` exists.

```
### D: story escalates integration, floor 26, short run, story ACTIVE (a dev machine)
exit=1
--- gate summary ---
FAIL         integration (required by story T-1) (0s, did 1 units of work, below the floor of 26 in project.conf) -> .claude/state/gate-logs/integration.log

recorded in docs/backlog/stories/T-1.md (## Gate results)

### E: the same tree with NO current-story.env (what CI has)
exit=0
--- gate summary ---
WARN         integration (0s, did 1 units of work, below the floor of 26 in project.conf, optional)
```

`.gitignore` line 2 is `.claude/state/*`, and `git ls-files .claude/state/`
returns only `.gitkeep` and `README.md`. So **E is what CI always sees**: no
active story, no escalation, `integration` optional. Two consequences that shape
this story: the `covers` changed-paths check also never runs on CI, and CI's
`integration` line has never been evidence for any story.

### PO-3 — orchestrator probe, PLANNED: what each of the four options actually does

Same fixture. `A` is today's manifest; `B` is option 1; `C` is option 1 on a
capable machine; `F` is option 3 taken literally.

```
### A: today's integration line against a 1-passed-25-skipped log
PASS         integration (0s, observed 1)

### B: same log, with a floor of 26
WARN         integration (0s, did 1 units of work, below the floor of 26 in project.conf, optional)

### C: full local run, 26 passed, floor 26
PASS         integration (0s, observed 26, floor 26)

### F: can blocked-when fire on a gate that exits 0?
(conf: blocked-when | integration | [1-9][0-9]* skipped, gate REQUIRED)
exit=0
PASS         integration (1s, observed 1)
```

### PO-4 — the decision: what won, and what each loser loses

The user asked for a probe rather than taste. All four options were run or read
against `gates.sh` before this was written.

**What wins: a narrow form of option 2, measured by option 1 and reported in
option 3's vocabulary.** A new `project.conf` kind, `skipped-when`, that
classifies a below-floor shortfall as environmental, plus the `floor | integration
| 26` that makes the shortfall measurable at all. One-line reason: *"N passed" is
not evidence of work when M were skipped* — option 2's sentence — and the only
way to say it without weakening anything is to measure the shortfall (option 1)
and then report an environmental one as a declared non-result (option 3's
`BLOCKED`/`KNOWN`) instead of noise.

- **Option 1, a bare `floor | integration | 26`. Rejected on its own; adopted as
  the measurement.** Probe D shows it closes the real hole: with a story
  escalating the gate, the short run FAILs instead of passing. Probe E and B show
  what it costs: CI never escalates, so on CI the shortfall is a `WARN` on every
  PR forever — exactly the outcome MT-007 and MT-008 wrote into `project.conf` as
  the reason `integration` has no floor, and exactly what `quality-gates` forbids
  ("WARN must mean something changed"). Their reasoning was right and is not
  overruled; it is *discharged*, because `skipped-when` turns that WARN into a
  `KNOWN`.
- **Option 2 as a regex change. Rejected as stated; adopted as the idea.**
  Evidence lines are matched with `grep -Eq`. "N passed **and** no skips" needs a
  negative lookahead POSIX ERE does not have, and the alternative — pushing the
  assertion into the gate command as `&& test $(…)` — moves the liveness check out
  of the mechanism built and audited for it into a shell one-liner nothing
  inspects. The `evidence` header's own rule ("assert volume of work observed")
  points at a *count*, which is a floor.
- **Option 3, extending `blocked-when`. Rejected as a reuse; adopted as the
  verdict vocabulary.** Probe F is decisive and is not a matter of taste:
  `blocked-when` is consulted **only** inside `if [ "$rc" -ne 0 ]`, and
  `1 passed, 25 skipped` exits **0**. The line never fires; the gate still reports
  `PASS … observed 1`. Making it fire on a zero exit would also contradict its own
  documented meaning — *"These patterns describe a process that never STARTED"* —
  and a pytest run that started, collected 26 tests and skipped 25 of them plainly
  started. What option 3 is right about is the *verdict*: `BLOCKED` when a story
  is leaning on the gate, and a declared non-result when it is not.
- **Option 4, leave it. Rejected — but half of it is true, and it is the half
  nobody was worried about.** Probe E confirms CI never escalates, so "the local
  stamped run is the evidence and CI was never meant to be" is correct about CI.
  The hole is *inside that local stamped run*: PO-1 shows a worktree — the normal
  place agents work — producing `PASS integration (observed 1)`, which
  `check-boundaries.sh` then reads as *"story-required gate 'integration' passed in
  the recorded run"*. Option 4 nominates as the backstop the exact mechanism that
  is fooled.

### PO-5 — two frontmatter decisions, and their reasons

**`depends_on: []`, and no edge to or from MT-011.** A `depends_on` edge is a hard
block: `phase.sh set` refuses to move a story past PLANNED while a dependency is
not DONE. MT-011 is fully buildable without this story — nothing in it needs
`skipped-when` to exist — so the relationship is *landing order*, not dependency,
and encoding an ordering preference as a hard block is how a backlog acquires
edges nobody can later justify. **Recommendation, recorded here and not written
into MT-011 (which is out of scope): land MT-037 before MT-011 starts.** It is
small, and MT-011's AC-8 is the next binding `integration` claim in the backlog.
If MT-011 starts first it is not harmed — it simply inherits the old behaviour and
must be gated from a checkout carrying `spikes/`.

**`required_gates: []`, deliberately.** Escalating `integration` here would be
self-defeating rather than rigorous: this story is developed in a worktree, PO-1
shows a worktree has no `spikes/`, so an escalated `integration` would report
`BLOCKED` and exit 3 at every GATES run — and the BLOCKED playbook's remedy
(quote the PR's CI log) cannot apply, because CI has no `spikes/` either. Nothing
would be learned. The better outcome is the one that arrives for free: with
`required_gates` empty, the full run at GATES will exercise the new rule against
the real gate and record **`KNOWN integration`** in this story's own
`## Gate results` — the fix demonstrating itself on the real manifest, which is
better evidence than a self-referential `PASS`. The real-suite `PASS … observed
26, floor 26` case is covered by probe P-2 (C-8), run from a checkout that has
the data.

### PO-6 — sizing

One RED→GREEN cycle. The surface is one new table kind in `scripts/gates.sh`
(parser arm, one outcome branch, one `case` arm, one `--list` line, three
`--audit` refusals), two live lines plus header prose in `project.conf`, one
`describe` block in `.claude/tests/gates.test.sh`, and a paragraph in
`environment.md`. Comparable to MT-032, which changed `gates.sh` more than this
does. `.claude/tests/` and `bash scripts/selftest.sh` are the test bed, not
`tests/core`.

### PO-6 — Orchestrator verification of RED's claims, 2026-09-17

*Independent reproduction before accepting the phase, with an expression I wrote
rather than RED's.*

**The counts reproduce.** `bash scripts/selftest.sh gates` → `gates: 136 passed,
24 failed`, exactly as RED reported. Every failure is the absence of the
`skipped-when`/`floor` behaviour — the headline one is
`FAIL a run that skipped 25 of its 26 tests is not a PASS`, expecting NOT to
contain `PASS         integration` and getting `PASS integration (0s, observed
1)`. That is PO-1 reproduced mechanically off the real manifest's own
`integration` lines. No timeout, no config error, no unrelated breakage.

**The suite discriminates on the wording GREEN is frozen against.** RED's five
probes all mutated a *comparison* or a *counter*; none mutated the pinned prose,
which is the thing GREEN cannot reword. So:

```
$ bash scripts/mutate.sh scripts/gates.sh '486s/below the floor of/under the floor of/' \
    -- bash scripts/selftest.sh gates

    FAIL with the floor wording it has always had
gates: 134 passed, 26 failed
=== mutate: command exited 1; restored (verified byte-for-byte against
    .../scripts_gates.sh.20260917T171823Z.3653112.bak) ===
```

24 → 26: exactly two assertions newly red, one of them the AC-5 control that
pins today's floor wording. A suite that had merely *described* the wording
would not have moved. Restored and verified.

**Admissibility.** `bash scripts/gates.sh --fast` is **green** — `format`
(observed 71), `lint` (5, floor 5), `typecheck` (39, floor 28), `unit` (421,
floor 292), `coverage` (421), `coverage-core` (416). Correct for this story and
not a contradiction of a red RED: every file it touches classifies `harness`,
and its criteria are checked by `selftest.sh`, which is a CI step and not a gate.

**C-5 confirmed at the source, not taken from RED.** `check-boundaries.sh:264`
is `grep -qE "^[[:space:]]*PASS[[:space:]]+$g( |\(|$)"`. No change needed and
none made. **C-2's line numbers were off** — the `[ -n "$floor" ]` branch is at
L480–488, not ~470–480 — which is RED's correction and is accepted; the
mechanism is unaffected, so this is a note rather than an amendment.

**A pre-existing item, not this story's and not fixed here.**
`.claude/state/mutations/` holds `.claude_hooks_lib.sh.20260915T023340Z.191463.bak`
and its `.new`, dated 2026-09-14. Per `rules.md` a leftover `.bak` means a
restore failed and `mutate.sh` exited 90. `.claude/hooks/lib.sh` is clean in
`git status`, so nothing is corrupt — it is a stale pair from the MT-031/032
era. Flagged for whoever owns harness housekeeping; `.claude/state/**` is not
committed, so it cannot be cleaned up in a PR.
