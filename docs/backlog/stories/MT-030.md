---
id: MT-030
title: A permissively-licensed text detector, or a decision to ship GPL
slug: a-permissively-licensed-text-detector-or
epic: EPIC-03
type: spike
status: done
phase: DONE
branch: story/MT-030-a-permissively-licensed-text-detector-or
depends_on: [MT-002]
required_gates: []  # gate ids that are optional for the repo but binding for THIS story
---

## Context

**Filed 2026-09-12 out of MT-002's first escalation, on the user's decision to
settle it in parallel rather than defer it to MT-007.**

MT-002 settled the *runtime* — ONNX Runtime on the CUDA execution provider, all
three models working on the RTX 5070. Read
`docs/wiki/audits/MT-002-model-runtime.md` `## Decided` and **do not
re-litigate the runtime**. This story is only about *which detector*, and its
`## Evidence` is re-verifiable as always.

What MT-002 found, and could not resolve:

- `dmMaze/comic-text-detector` is **GPL-3.0** [measured — the repo's `LICENSE`].
- The ONNX re-upload MT-002 actually measured,
  `mayocream/comic-text-detector-onnx`, declares **`apache-2.0`** [web] — a third
  party's metadata on a derivative of a GPL-3.0 project, which does not
  obviously relicense anything.
- Upstream states the model was trained on ~13k images, **1/3 from Manga109-s**
  [web], a dataset whose terms restrict use to academic research.
  **← This premise is FALSE and the spike proved it so. See F-1 and PO-4 V-1.**
  Manga109-**s** is the 87-volume subset published expressly for commercial use.
  Left in place as written because it is the premise the story was filed on and
  the spike's job was to test it; marked here so no reader carries it forward.

This application is **distributed** as a double-click installer (brief §2,
MT-024), which is what makes this live rather than academic.

**Why now rather than at MT-007.** If the answer is "find another detector",
that is research, and MT-007's `## Contract` model identity changes with it.
Four stories sit between here and MT-007, so this runs alongside them at no cost
to the critical path — and discovering it inside MT-007's RED would put research
on the critical path instead. MT-007 now `depends_on` this story.

**Which required gate would fail if this story's artifact broke:** none — the
artifact is a document. That is why this is a `spike` and not a `feature`, and
why its phase path is PLANNED → REVIEW → DONE.

## Acceptance criteria

A spike's criteria are about what is *known* at the end, not about behaviour.

- **AC-1** — `docs/wiki/audits/MT-030-detector-licence.md` exists and lists every
  candidate detector considered, and for each: its licence, whether a usable
  ONNX export exists and where, what its training data is stated to be, and
  whether that training data carries use restrictions. Candidates ruled out are
  listed with the reason, not silently dropped.
- **AC-2** — for each candidate that is both permissively licensed **and** has an
  ONNX export, the document records a measured forward pass on the **same
  fixture pages MT-002 used** (`spikes/MT-002/pages/011–015.jpg`), on this
  machine, reporting region count, where the regions fell, and the execution
  provider actually selected — `session.get_providers()`, never
  `get_available_providers()`; MT-002 E2 says why.
- **AC-3** — the document quotes, rather than paraphrases, (a) the incumbent's
  actual licence text where it bears on distributing a derived work, and (b) the
  Manga109-s terms as published. A summary of a licence is not a licence.
- **AC-4** — the document ends with a `## Decided` section naming the detector
  MT-007 should use and its file identity (URL + SHA-256), **or** stating that
  no permissively-licensed alternative was found and that the decision is
  therefore between shipping GPL-3.0 and commissioning a detector — with the
  evidence for that, and without pretending the choice is technical.
- **AC-5** — anything reported as a *measurement* is labelled with what it was
  measured on; anything reported as a *claim from the web* is labelled as such.
  Same rule as MT-002 AC-5, same reason.

## Contract

**Output paths:**

- `docs/wiki/audits/MT-030-detector-licence.md` — the decision document.
- `docs/wiki/architecture.md` — a new decision record **only if** the detector
  changes, or if "ship GPL knowingly" is chosen. A licence position that is not
  written into the architecture is not a position.
- `spikes/MT-030/` — throwaway probe code and any downloaded candidates.
  `.gitignore` covers `spikes/`.

**The `## Decided` / `## Evidence` split is load-bearing**, exactly as in MT-002.
`## Decided` carries the detector choice and file identity and nothing else;
every number lives in `## Evidence` and is re-verified by MT-007, not assumed.

**Baselines to read out of MT-002 rather than re-derive** — these are the bar a
replacement has to clear, and they are `## Evidence`, so **re-verify them before
comparing against them**:

- recall **0.949**, IoU **0.585** against the detector author's own reference
  mask (MT-002 E6). Precision is low there because MT-002's post-processing was
  unrefined, not because the model is.
- **9/9** correct end-to-end transcriptions on page `014.jpg` via detector
  boxes, including white-on-black text (MT-002 E5/E6).
- The box head and the segmentation mask **disagree on sound effects**: boxes
  skip the パチ, the mask covers them (MT-002). Any replacement must be checked
  for the same split, because MT-007 AC-6 and MT-019 both depend on it.

**Oracle partition: oracle-free**, as MT-002 was. There is no settled answer to
read out. So the demand falls on the negative side: a claim that a candidate is
"comparable" must state what would have counted as **worse**, before the
comparison — a region-count floor, a recall figure, a named failure on the same
fixture — or it is an impression.

### Pinned by the orchestrator at dispatch, 2026-09-12

**The spike may amend any block below in place, with a reason, while the story
is still in PLANNED. Nothing here may be relaxed after the first candidate has
been measured** — that is the whole point of pinning the floor before the
comparison, and this story's `## Contract` already says an unpinned
"comparable" is an impression.

**P1 — a candidate that does not emit a per-pixel probability map or mask is
disqualified structurally, before any measurement.** This is not a preference.
`docs/wiki/architecture.md` line 124 stores `region.mask_blob` as a
PNG-compressed 1-bit mask, D4 says the downstream consumers "need masks and
polygons", and MT-007's `## Contract` types the detector as
`run(page) -> NDArray[np.float32]` consumed by
`regions_from_probability(prob, page_size)`. A box-only detector — most YOLO
derivatives — cannot serve MT-019's inpainting path at all. Record such
candidates in AC-1's table with "boxes only" as the reason and do **not** spend
the timebox benchmarking them.

**P2 — the disqualifying thresholds, fixed before the first candidate runs.**
A replacement is **worse than the incumbent** if any of these holds. Measure
them; do not re-scope them afterwards.

| # | Condition | Incumbent's measured value (MT-002, to be re-verified first) |
|---|---|---|
| W1 | Recall against `dmMaze/comic-text-detector`'s own published reference mask on `AisazuNihaIrarenai-003.jpg` is **below 0.85** | 0.949 (E6) |
| W2 | On `014.jpg`, it fails to cover any of the **9 regions** E5 confirmed as real text by reading them back through manga-ocr — including the white-on-black one | 9/9 (E5/E6) |
| W3 | ~~Region count on any of `011–015.jpg` falls outside **2–40**~~ — **VOID. Withdrawn by PO-5 on the spike's escalation E-1: ill-posed, disqualified the incumbent against itself, excluded no candidate W1 and E5 did not.** | ~~6–9 per page (E6)~~ |
| W4 | A single region covers **more than 40%** of the page | not observed (E6) |
| W5 | It cannot be made to run on `CUDAExecutionProvider` at all, confirmed by `session.get_providers()` | CUDA, 25.8 ms/page (E4) |

A candidate that clears all five is **not thereby "comparable"** — it is *not
disqualified*. Any stronger word in the document needs its own number.

**P3 — the SFX split is a required measurement, not an observation.** MT-002
found the box head silent on the art-integrated パチ on `012.jpg` while the
`seg` mask covers them, and **MT-007 AC-6 and MT-019 both depend on which
output is used**. Every candidate that reaches AC-2 is measured on `012.jpg`
for the same split, and the result is recorded whichever way it falls. A
candidate with a *single* segmentation output and no box head does not have the
split available — say so explicitly, because it means MT-007 AC-6 would need a
different mechanism, and that is a cost the user is entitled to see before
choosing.

**P4 — the candidate list is a floor, not a ceiling.** These are checked and
recorded in AC-1's table whatever the outcome; adding others is expected, and
each addition is listed with how it was found.

- `dmMaze/comic-text-detector` (incumbent) and the `mayocream/...-onnx`
  re-upload — the thing being replaced.
- `ragavsachdeva/magi` / Magiv2 — manga-native, licence to be read rather than
  assumed.
- PaddleOCR text detection (DB / PP-OCR family) — Apache-2.0, ONNX exports
  exist; **general** text, not manga-trained.
- docTR (Mindee) DBNet / LinkNet — Apache-2.0, ONNX exports exist.
- CRAFT (ClovaAI) and the EasyOCR packaging of it.
- MMOCR (OpenMMLab) DBNet++.
- Any Ultralytics-YOLO-derived comic bubble detector — **note the AGPL-3.0
  problem is the same class of problem as the incumbent's GPL-3.0, not an
  escape from it**, and most are box-only anyway (P1).

**P5 — a licence is read from the artefact, not from the model card.** The
incumbent's whole problem is that a third party's HF metadata says
`apache-2.0` over a GPL-3.0 derivative. For every candidate, the licence
recorded in AC-1's table is the one in the **upstream source repository's
`LICENSE` file**, fetched and quoted; where a re-upload's metadata disagrees,
both are recorded and the disagreement is named. AC-3's "quote, do not
paraphrase" applies to whichever text bears on distribution.

**P6 — where the detector identity is written down today**, so a change made
here does not leave the tree contradicting itself. Swept with `grep` across
`docs/`, `src/`, `tests/` and `.claude/` before dispatch; the spike is to
confirm the list against the tree:

| File | What it says |
|---|---|
| `docs/wiki/stack.md` line 130 | the Models table row naming `mayocream/comic-text-detector-onnx`, including the licence-risk sentence |
| `docs/wiki/audits/MT-002-model-runtime.md` | `## Decided` model identity table, and `## Licence risk` |
| `docs/backlog/stories/MT-007.md` | `## Context` restates the GPL-3.0 finding |
| `docs/wiki/architecture.md` | **no decision record exists yet** — this is the gap the Contract's second output path closes |

There are **no code callers**: `grep -rn "detect\|detector" --include=*.py src/
tests/` returns nothing, because `src/mangatl/detect/` is an empty package until
MT-007. So the caller sweep `/advance-story` requires of the orchestrator is
documentation-only, and it is the table above.

`stack.md` and `architecture.md` are this story's to amend. **MT-007 and the
MT-002 audit are not**: MT-007 is another story's file and the audit is a
closed story's evidence. If the detector changes, say so in this story's
`## Notes` as a numbered follow-up for whoever advances MT-007, rather than
editing either.

**P7 — `session.get_providers()`, never `get_available_providers()`.** AC-2
already says this; it is repeated here because MT-002 E1 measured
`get_available_providers()` advertising `CUDAExecutionProvider` in two
configurations where CUDA did not load at all, and because a candidate
benchmarked accidentally on CPU would be rejected on a number that is not about
the model. The `ort.preload_dlls(cuda=True, cudnn=True, msvc=True)` call is
required on this machine (MT-002 PO-3) and is process-global.

**P8 — this instruction set is checkable, not sacred.** Every mechanism named
above (that `preload_dlls` is required, that a given candidate has an ONNX
export, that the reference mask is where MT-002 says it is) is something the
orchestrator asserts and may have got wrong. If a cheap check contradicts one,
report that rather than working around it — MT-002's own dispatch named a
mechanism that turned out to be half wrong, and the spike catching it is what
made the audit worth reading.

## Deferred verifications

**DV-1 — the replacement's SFX split, asserted by a test. Owner: MT-007, in its
RED phase. WAIVED for MT-030.**

*Falsifiable condition:* whichever detector `## Decided` names, MT-007 AC-6's
assertion must fail when the detector's output is swapped for the one this
spike measured as covering the SFX. If AC-6 passes against both outputs it is
not testing the split.

*Why waived here:* MT-030 is document-only and writes no test; `tests/` is not
writable in any phase a `spike` has (PLANNED → REVIEW → DONE), which is the same
constraint MT-002 PO-1 recorded. The measurement itself is **not** deferred — P3
makes it part of AC-2 and it runs in this story.

**DV-2 — that the licence position survives contact with the packaging step.
Owner: MT-024. WAIVED for MT-030.**

*Falsifiable condition:* if `## Decided` is "ship GPL-3.0 knowingly", then
MT-024's installer must carry the licence text and the written-offer/source
obligations that follow, and their absence must fail MT-024. If `## Decided`
names a permissive replacement, the condition is void.

*Why waived here:* there is no installer yet, and the obligation depends on the
decision this story is being run to reach. Recorded so it is not discovered at
MT-024.

**RESOLVED 2026-09-13, after this story closed.** The user decided: **ship
GPL-3.0 knowingly.** So DV-2's first branch is the live one — its condition is
no longer conditional, and it is now five named obligations in MT-024's own
body plus a decision record at `architecture.md` **D13**. The waiver stands as
written (MT-030 still writes no installer and no test); what has changed is that
the condition it defers is now **specified** rather than **pending a decision**.
MT-024 inherits it as O-1 through O-5.

*One correction this decision forced, recorded here because MT-030's own
framing invited it:* DV-2 says "the licence text and the written-offer/source
obligations". That is right, and the surrounding conversation repeatedly
compressed the whole question to "attribution". **GPL-3.0 is not an attribution
licence.** Attribution is the *Manga109-s* obligation (O-4); GPL-3.0's are
copyleft (O-1 to O-3) and are not discharged by a credits line. D13 and MT-024
now separate the two kinds explicitly.

## Model guidance

- **Spike model: `claude-opus-5` (Claude Opus 5). RESOLVED AT DISPATCH,
  2026-09-12:** dispatched to the **`lead-po`** subagent, whose
  `.claude/agents/lead-po.md` frontmatter declares `model: opus`. No `model`
  override was passed on the dispatch and no session override was in effect, so
  `opus` resolved to **Claude Opus 5 (`claude-opus-5`)** — the same model the
  orchestrating session runs on. Recorded by name rather than as "the default",
  per `rules.md`.
- **Success condition, falsifiable:** a reader who was not present can, from the
  document alone, either (a) fetch the named replacement by URL and hash and
  write MT-007's contract against it, or (b) state the two remaining options
  with the licence text in front of them and decide. If MT-007 has to re-run any
  candidate to know what to depend on, the spike failed.
- **Verdict, recorded 2026-09-12 when the spike ended: MET, on the (b) branch.**
  `## Decided` does not name a replacement, so the success condition is (b): a
  reader can state the two remaining options with the licence text in front of
  them and decide. The audit carries GPL-3.0 §5(c) and §6(a)/(d) verbatim, the
  Manga109-s terms verbatim, and the full candidate table with a reason for every
  rejection. **MT-007 has to re-run nothing to know what it depends on** — the
  incumbent is unchanged and its file identity is restated in the audit's
  `## Decided`. The (a) branch is untestable here because no candidate qualified.
  *Caveat on the verdict:* the spike overturned a premise in its own dispatch
  (the Manga109-s reading), which is the outcome the brief was written to make
  possible rather than a failure of it.
- **Orchestrator's addendum on the brief itself, 2026-09-12.** Two of my five
  pinned thresholds did not measure what they were meant to — W1 is gameable by
  lowering an operating point, W3 was ill-posed and disqualified the incumbent
  against itself. **The spike reported both under the pins as written and moved
  neither**, and answered the one in its way by adding a threshold-independent
  comparison rather than by re-scoping the threshold. P8 said a named mechanism
  is checkable rather than sacred; this is that instruction being used correctly,
  and it is worth more than the two pins were. W3 is withdrawn by PO-5; W1 stands
  as written with its limitation documented. The verdict does not rest on either.

## Out of scope

Anything about the runtime — settled by MT-002. Any production code. OCR and
inpainting model choice. Detection *thresholds*, which are MT-007's and are
test-first. Training or fine-tuning a detector: if the conclusion is that one
must be commissioned, that conclusion is the deliverable, not the model.

## Notes

### Outcome, 2026-09-12 — and what the next agent must pick up

`docs/wiki/audits/MT-030-detector-licence.md` is written. **`## Decided` names no
replacement.** Nine candidates checked, four measured on this machine; none that
is permissively licensed with an ONNX export clears P2's floor. Per PO-3 the
`architecture.md` decision record is **deliberately absent** and the question is
with the user. That is the story's own definition of a success, not an
incomplete story.

**F-1 — the Manga109-s premise in this story's `## Context` is wrong, and the
tree has been corrected.** Manga109-**s** is the 87-volume subset published for
commercial use; its terms expressly permit commercial use of models trained on
it, subject to clear attribution. The academic-only restriction belongs to the
full Manga109. **GPL-3.0 is the entire blocker.** `stack.md` line 130 has been
corrected in place. **`MT-002.md`, the MT-002 audit and `MT-007.md` still carry
the old framing and were deliberately not edited** — a closed story's evidence
and another story's file. Whoever advances MT-007 should read the MT-030 audit
before its `## Context`.

**F-2 — two pinned thresholds do not measure what they were meant to. Neither was
moved.** Escalations E-1 and E-2 in the audit.
- **W3 disqualifies the incumbent against itself.** Its "6–9 per page" is MT-002
  E6's *Boxes* column; the *Mask components* column in the same table is 57–136,
  and components of the thresholded map is the only count a mask-only candidate
  has. Measured: the incumbent scores 56–136 and fires W3 on all five pages,
  while PaddleOCR *clears* it at 12–35 despite being far worse at the job. Needs
  the orchestrator to settle whether W3 counts boxes or counts components after a
  stated morphological close — the latter imports a post-processing constant that
  is MT-007's to derive, not this spike's. **No candidate's verdict turns on it:
  every one is already disqualified by W1.**
- **W1 is recall-only and gameable.** `onnxtr-db-resnet50` reaches recall 1.0000
  by masking 81% of the page. Rather than move the pin, the spike added a
  threshold-independent comparison (audit E5) that reaches the same verdict by a
  wider margin.

**F-3 — the SFX split is unavailable in every alternative, and this is a real
cost.** Measured for the first time with numbers: the incumbent's box head covers
**0.000** of all six annotated SFX areas on `012.jpg` while its mask covers
27–59%. **No permissive candidate has a box head at all**; each masks the SFX.
So if the detector ever changes, **MT-007 AC-6 loses the mechanism it is
designed around** and MT-019 loses the `seg ∩ accepted boxes` construction.
Follow-up for whoever advances MT-007.

**F-4 — the P6 table is missing one entry.** `docs/backlog/stories/MT-002.md`
line 311 also carries the detector SHA-256. Closed story's file; not edited.
Confirmed there are **no code callers** — `src/mangatl/detect/` is an empty
package.

**F-5 — for MT-007, two numbers worth having.** The incumbent's IoU against the
author's reference peaks at **0.714 at threshold ≈0.81**, not the 0.585 MT-002
measured at the upstream default of 0.3. That is a hint, **not a value to
adopt**: the reference is a *refined* mask and MT-007 implements refinement, so
the right operating threshold for a raw map feeding a refinement step is a
different quantity. And MT-002's E1/E6 numbers **all reproduced exactly** from an
independently written probe, so the baseline is sound.

**F-6 — if the user picks "commission a detector".** The licence-clean
manga-native lineage is `juvian/Manga-Text-Segmentation` (**MIT**, label masks
CC-BY-4.0 on Zenodo), which is the source of the *incumbent's own* mask labels.
It has no ONNX export and its 2025 successor's weights carry no licence at all.
That is the starting point, and it is unmeasured.

**Timebox.** If no permissively-licensed detector with a usable ONNX export is
found within the spike, the honest outcome is a document that says so, lists
what was checked, and puts the two remaining options to the user with the
evidence. That is a success. Spending the spike trying to make a marginal
candidate work is not.

**This story does not give legal advice and must not pretend to.** Its job is to
put the actual licence text, the actual dataset terms and the actual technical
alternatives in front of the user in one place. Whether GPL-3.0 obligations are
acceptable for this product is the user's decision, and it may be one they want
counsel on. An agent's reading of a licence goes in the document labelled as
exactly that.

### PO-1 — no gate can verify this artifact, and `required_gates: []` is the correct answer rather than an omission

**Decided 2026-09-12 by lead-po, before dispatch.** `/advance-story` requires
naming the gate that would fail if this story's artifact broke, and checking it
is `required`. Read out of `bash scripts/gates.sh --list` on this branch, the
`covers:` lines are:

```
unit          covers: src/mangatl/**
coverage      covers: src/mangatl/**
coverage-core covers: src/mangatl/domain/** typeset/** bench/**
integration   covers: src/mangatl/detect/** ocr/** clean/**    [optional, waived]
build         covers: packaging/**
```

**No gate reads `docs/**`.** The artifact is a document, so there is nothing for
a gate to exercise, and the story's own `## Context` already says so. This is
exactly MT-002's situation and it resolved the same way. The check that matters
here is the one `/advance-story` warns about — an `optional` gate being the only
thing that touches the artifact — and it does not apply, because *no* gate
touches it, optional or otherwise.

**The consequence, stated so it is not mistaken for a pass:** `gates.sh` will
report `All required gates passed` on this branch having exercised **nothing this
story produced**. That sentence is true and uninformative. The verification of
this story is AC-1 through AC-5 read against the document, plus the orchestrator's
independent reproduction recorded below. `check-boundaries.sh` agrees — it emits
`spike story; gate record not required` for `REVIEW:spike`.

**The gate that *will* verify the outcome is `integration`, at MT-007**, whose
`required_gates: [integration]` promotes it for that story. If this spike changes
the detector, MT-007's AC-7 is what proves the replacement loads and runs. That is
the right place for it: the artifact there is code.

**The fast gates were run anyway, and the first run reported five required gates
BLOCKED. That was the environment, and it is already documented.** Recorded here
so the record is honest rather than silent:

```
BLOCKED      lint (0s, exit 127, could not launch: command not found)
BLOCKED      typecheck / unit / coverage / coverage-core  (same)
scripts/gates.sh: line 410: uv: command not found
```

Per `/advance-story`'s BLOCKED triage this is **path 1, not path 2**: a missing
tool rather than a policy refusal, so it is fixable here and needs no *pending
CI* line. `docs/wiki/environment.md` §5 already records the cause and the exact
remedy — `winget` installed `uv.exe` into the package directory rather than
dropping a shim in `WinGet\Links`, so a shell started before that runs with a
stale `PATH`. With §5's export line applied, the same command re-run:

```
PASS  format (0s, observed 16)          PASS  unit (1s, observed 12, floor 12)
PASS  lint (0s, observed 1, floor 1)    PASS  coverage (1s, observed 12, floor 12)
PASS  typecheck (1s, observed 14)       PASS  coverage-core (2s, observed 7, floor 7)
All required gates passed (6 ran, 0 unconfigured, 0 known).
```

**This is not a gate record and is not evidence of anything** — a `--fast` run is
a subset and `gates.sh` deliberately does not write it into the story. It is a
sanity check that this branch breaks nothing, which is all a docs-only change
could do. `check-boundaries.sh` remains the check that matters here, and it
passes with `spike story; gate record not required`. Nothing to fix in
`environment.md`: it already says this.

### PO-7 — MT-002's audit gets a forward pointer, because a later agent is told to read it first

**Decided 2026-09-12 by lead-po, at REVIEW, after
[`manga-translator-8e`](#) — the session driving MT-003 — raised it.**

F-1 left MT-002's audit deliberately unedited on the grounds that it is a closed
story's evidence. That is right about the *evidence* and wrong about the reader:
MT-007's `## Context` instructs whoever picks it up to **read MT-002's audit
first**, and its `## Licence risk` section states the Manga109-s objection as
fact. A later agent would hit the stale framing before ever reaching MT-030.

So MT-002's audit now carries a short **superseded-by block at the head of
`## Licence risk` only** — a pointer, not a rewrite. Every measurement in that
document is untouched. The distinction is the one that matters: what MT-002 got
wrong was not a number it measured but an interpretation of a dataset's terms,
and interpretations are exactly what a later reader inherits without checking.

**Also confirmed, because the same message raised it:** MT-002's `## Decided`
model identity is **not** superseded. No replacement was found, so the incumbent
`mayocream/comic-text-detector-onnx` at
`1a86ace74961413cbd650002e7bb4dcec4980ffa21b2f19b86933372071d718f` stands, and
MT-030's own `## Decided` restates it. There is no stale identity to follow.

### PO-8 — `MT-007.md` is corrected too, which reverses F-1's call on that file

**Decided 2026-09-12 by lead-po, at REVIEW, after `manga-translator-8e` pointed
out that PO-7 had fixed the audit and left the worse instance standing.**
Verified against the file before acting rather than taken on report:
`docs/backlog/stories/MT-007.md` line 184, `### Open licence question inherited
from MT-002`, ends —

> Upstream also states the model was trained on ~13k images, **1/3 from
> Manga109-s**, whose terms restrict use to academic research.

**F-1 and PO-7 were wrong to leave this.** The reasoning was "another story's
file", and that reasoning was doing real work for the *MT-002 audit* — a closed
story's evidence, where a rewrite would falsify the record. It does none here:

1. **MT-007 is in `PLANNED`.** Nothing in it is frozen; acceptance criteria
   freeze on leaving PLANNED and this is not one anyway.
2. **It is the instance a reader acts on.** This paragraph is in the story body,
   above the line telling the reader to go and read the audits. PO-7 corrected
   the document a reader reaches *second* and left the one they reach *first*.
3. **It is not evidence.** It is an inherited *interpretation*, and the
   interpretation is what MT-030 voided. Nothing measured is touched.
4. **MT-030's outcome is what invalidated it**, so the correction belongs with
   the evidence, in this PR, not filed as a chore for someone without the
   quoted terms in hand.

The note now carries a narrowing block: the Manga109-s half retracted, the
`## Contract` model identity confirmed **unchanged** (so AC-7's fixture is
written against the incumbent), the GPL-3.0 half left open and named as MT-024's
blocker rather than this story's, and F-3's SFX numbers surfaced where AC-6 is
written. `MT-002.md` is still not edited — it is closed, and its stale line is a
hash in a manifest rather than a claim anyone acts on.

**The general lesson, since I got this wrong once already in this story:**
"another story's file" is a rule about not rewriting *evidence* and not
front-running someone else's *decisions*. It is not a rule about leaving a
statement that is now known to be false in the place a reader will act on it.

### PO-9 — MT-024 carries the shipping obligations in its own body, as a note and not as a criterion

**Decided 2026-09-12 by lead-po, at REVIEW, on the same session's follow-up: if
attribution is a shipping requirement, MT-024's story should say so the way
MT-007's now does.** Checked before acting — `MT-024.md` mentions licensing
exactly once, a `licence` field in `packaging/models.json` (line ~76). **That is
metadata about the weights, not a notice shipped to the user.** MT-030's DV-2
names MT-024 as owner, but DV-2 lives in *this* story, and MT-024's reader never
opens it. Same failure shape as PO-8, found one file further along.

So MT-024 now carries both obligations in its body: Manga109-s attribution
(live, decidable now, quoted verbatim) and GPL-3.0 (open, and the user's).

**Where this stops, and why the line is here rather than one step further.**
PO-8 said the rule protects evidence and *other people's decisions*. Correcting
a false sentence touches neither. **Writing an acceptance criterion into another
story does touch the second** — it is that story's PO deciding how to discharge
an obligation, and MT-024 is in PLANNED with its criteria not yet frozen
precisely so its own planning can make that call. So the note says explicitly
that it is not an AC, that MT-024's PO writes it, and that **Obligation 2's AC
must not be written until the user decides**, because a criterion frozen against
a guess at the answer is worse than an absence.

The distinction I am drawing: **a fact another story is missing, I supply; a
decision another story owns, I flag.**

### PO-2 — EPIC-03's done-when: what this story owes it, and the gap it does not close

**Decided 2026-09-12 by lead-po, before dispatch.** `/advance-story` requires
checking the epic's done-when against these criteria and reporting any gap to the
user before work runs.

`docs/backlog/epics/EPIC-03.md` done-when: *"Given a page scan, the pipeline
produces regions with masks, with furigana folded in … in right-to-left
top-to-bottom reading order, each carrying transcribed vertical Japanese. On the
fixture pages, every text-bearing bubble is found and no ruby column appears as a
region of its own."*

**MT-030 delivers none of that, and is not supposed to.** Every clause is owned by
MT-007 (regions with masks, every bubble found), MT-008 (furigana), MT-009
(order) and MT-010 (transcription). MT-030 is the first story in the epic and its
whole output is a document. **There is no gap for this story to close** — nothing
sits between the last story (MT-002, EPIC-01, DONE) and this one that the epic
promises and no story delivers.

**One thing this check did surface, and it is a real dependency rather than a
gap.** The epic's done-when says *"on the fixture pages, every text-bearing bubble
is found"*. The only fixture pages that exist are
`spikes/MT-002/pages/011–015.jpg`, which the MT-002 audit records as
**non-redistributable and never committed** — user-supplied scans from a
commercial release with scanlation watermarks burned in. So EPIC-03 cannot
currently be shown done by any means CI can re-run, whichever detector this spike
names. That is MT-007's and MT-023's problem, not MT-030's, and it is written here
because this check is where it became visible. **Reported to the user with this
dispatch; not absorbed silently.**

### PO-3 — the two possible outcomes have different blast radii, and the user decides the second one, not the spike

**Decided 2026-09-12 by lead-po, before dispatch.**

- If the spike finds a permissively-licensed detector that clears P2's floor, its
  `## Decided` names it and the follow-on work is mechanical: amend `stack.md`
  line 130, add the `architecture.md` decision record, and file a note for
  whoever advances MT-007. That is inside this story's Contract.
- If it does not, AC-4 says the document states the choice is between shipping
  GPL-3.0 knowingly and commissioning a detector. **That choice is the user's and
  the spike may not make it.** The `architecture.md` decision record in that case
  is written only *after* the user has decided, so this story may reach REVIEW
  with the architecture record deliberately absent and a question outstanding.
  That is a success under the story's own timebox note, not an incomplete story.

The spike is instructed to stop and report rather than pick, and the orchestrator
will put it to the user.

### PO-4 — orchestrator's independent verification of the spike's three load-bearing claims

`/advance-story` requires that a subagent's report be verified against a real
command run, and that **a claim the contract itself is wrong be reproduced
independently — different inputs, without reusing the subagent's own code.** This
spike made that claim twice (F-1 against the story's `## Context`, F-2 against two
of my own pins), which is exactly the shape of claim an agent makes when it wants
to stop failing, and also the shape of the most valuable escalations. Independent
reproduction is the only thing that separates them. All three were reproduced.

**V-1 — the Manga109-s premise (F-1). CONFIRMED.** Re-fetched from a *different*
entry URL with my own extraction code, outside the repository:

```
$ urlopen('http://www.manga109.org/en/download_s.html')
final URL: https://manga109.github.io/manga109-project-website/en/index.html 31003 bytes
saved 10781 chars

line 238:  academic purposes by non-commercial organizations   <- Manga109 (full set)
line 250:  87 books are available for commercial use           <- Manga109-s
line 253:  Using the Manga109-s dataset for experiments for machine learning or image processing.
line 258:  Using results ... obtained from machine learning experiments ... for commercial use.
```

And the other half of the claim — that the incumbent used the *subset* — checked
against upstream rather than against the audit:

```
$ raw.githubusercontent.com/dmMaze/comic-text-detector/master/README.md
All models were trained on around 13 thousand anime & comic style images,
1/3 from Manga109-s, 1/3 from DCM, and 1/3 are synthetic data ...
```

**Manga109-s, not Manga109.** Both halves hold. The objection recorded in this
story's `## Context`, in `stack.md` and in the MT-002 audit **is void**, and
`stack.md` is corrected. **GPL-3.0 is the whole question.**

*One defect found in the spike's own work by this check, and fixed:* the audit
quoted **four** of the five published conditions under a heading reading
"Conditions, verbatim". The fifth (a 20%-of-a-volume limit on republishing whole
pages) is irrelevant to shipping a model but was silently absent, and AC-3 exists
precisely to stop a list of terms being trimmed. Added to the audit with the
re-fetch recorded beside it.

**V-2 — the verdict's central measurement (E5). REPRODUCED, and wider.** The
whole document rests on "no permissive candidate is close", so I re-derived it
with a probe written from the ONNX graph signatures, sharing no lines with
`spikes/MT-030/*.py` or `spikes/MT-002/*.py`, living outside the repo, and
differing deliberately in four ways (PIL not cv2; 41-point sweep not 140;
reference binarised `>0` not `>=128`; metric computed at the reference's own
resolution by upsampling the model's map rather than downsampling the reference):

```
reference mask 1654x1170, 100344 px set
incumbent comic-text-detector      peak IoU 0.7534 @ th 0.772 (recall there 0.8604)
CRAFT mlt-25k @2048                peak IoU 0.4471 @ th 0.574 (recall there 0.7669)
                                     recall>=0.85 reachable at th 0.549 -> recall 0.8661, page masked 10.5%
PP-OCRv5 server det @1600          peak IoU 0.3819 @ th 0.054 (recall there 0.7634)
                                     recall never reaches 0.85 at any swept threshold

VERDICT: incumbent peak IoU 0.7534 vs best permissive 0.4471  -> ratio 1.68x
```

Spike: 1.55×. Orchestrator: **1.68×**. Reference pixel count 100,344 on both, and
matching MT-002 E6. The script aborts unless `session.get_providers()[0]` is
`CUDAExecutionProvider` (P7); all three selected CUDA.

*And one refinement it forces:* CRAFT **can** reach recall 0.8661 while masking
10.5% of the page, so **CRAFT is not disqualified by W1 in any
threshold-independent sense** — it fires W1 only at the operating point the spike
chose. E-2 made this objection using `onnxtr-db-resnet50`; it applies with more
force to CRAFT, which is the best permissive candidate and so the one where it
matters. What rules CRAFT out is E5. The verdict is unchanged; its justification
is narrower than the summary table implies, and the audit now says so.

**V-3 — "no permissive candidate has a box head" (F-3). CONFIRMED against the
artefacts.** This claim decides whether MT-007 AC-6 survives a detector change, so
I enumerated every downloaded graph's outputs myself:

```
comic-text-detector-onnx      [('blk', [1, 64512, 7]), ('seg', [1, 1, 1024, 1024]), ('det', [1, 2, 1024, 1024])]
craft-mlt-25k                 [('output', [_, h/2, w/2, 2]), ('relu_18', [_, 32, h/2, w/2])]
onnxtr-db-resnet50            [('logits', [_, 1, 1024, 1024])]
PP-OCRv5_server_det_onnx      [('fetch_name_0', [_, 1, _, _])]
ogkalu-comic-bubble           [('labels', ...), ('boxes', ...), ('scores', ...)]
```

The incumbent is the only graph with both a detection head and a segmentation
map. `ogkalu`'s P1 disqualification is confirmed **against the artefact**, not its
model card. F-3's cost to MT-007 AC-6 is real.

**What I did not re-derive:** E1's baseline reproduction (the spike had already
reproduced MT-002's numbers exactly, and my own run independently returned the
same 100,344-pixel reference), E2's nine-region W2 oracle, E6's SFX coverage
percentages, and every licence other than the incumbent's. Those are `## Evidence`
and MT-007 re-verifies what it depends on.

### PO-5 — W3 is withdrawn, not repaired

**Decided 2026-09-12 by lead-po, on the spike's escalation E-1, which I accept.**
The pin was mine and it was ill-posed. Verified directly against MT-002 E6 rather
than against the spike's reproduction of it: that table carries two count columns,
*Boxes* `9, 6, 8, 9, 6` and *Mask components* `136, 86, 83, 111, 57`. My W3 said
"region count", quoted the first, and meant a quantity a mask-only candidate could
produce. The two readings disagree by an order of magnitude, so as written W3
**disqualifies the incumbent against itself on all five pages** while *clearing*
PaddleOCR at 12–35 — a threshold that ranks a weaker detector above the one it was
calibrated from.

Neither repair is available here. Counting boxes makes W3 unmeasurable for exactly
the mask-only candidates P1 selects for. Counting components after a morphological
close requires choosing that constant, and a bubble-level count does not exist
before `refine_mask` — MT-007's to derive, test-first, and named *Out of scope* in
this story. Choosing one to rescue my own threshold is the narrowing `rules.md`
forbids.

**So W3 is void, and the honest statement is that four of five pinned conditions
discriminated and the fifth was ill-posed.** It costs nothing: PaddleOCR is the
only candidate W3 would have cleared and it is out on peak IoU 0.38 against 0.75.
**The spike was right not to move it**, and reporting it under the pin as written
rather than quietly repairing it is the behaviour the harness is built to get.

**Follow-up for MT-007:** a region-count sanity check is worth having, *after*
refinement, against a bubble count, with the morphology constant derived from a
fixture. Not a licence question, and not this spike's.

### PO-6 — a phase-guard defect observed while verifying, filed rather than worked around

While editing my own throwaway probe **outside the repository**, in the
session scratchpad, this was refused:

```
$ sed -i 's|        if a.ndim == 4 and min(...)|...|' "$SC"

BLOCKED by the harness phase lock.
  story:    MT-030
  phase:    PLANNED
  path:     s|        if a.ndim == 4 and min
  category: source
```

The guard took the **`sed` expression** as the path argument and classified it
`source`. The real target was an absolute path in the session scratchpad, which is
not in the repository at all. Per law 5 I did not reach for another tool to get the
same `sed` through — I rewrote the file with `Edit`, which is a different edit, not
the same edit smuggled past the lock.

This is the case CLAUDE.md describes as *"a bug in the guard: add the case to
`.claude/tests/phase-guard.test.sh` and fix it there"*. It is **not fixed here** —
`.claude/**` is `harness` and writable in PLANNED, so the lock would have allowed
it, but a guard fix belongs in its own chore story with its own test, not smuggled
into a licence spike. **Filed as a follow-up.** The failure mode is a false
*positive*, which is the safe direction; it costs a retry, not a violation.

### Closing record — CI timings from the first run, and the merge

**PR:** https://github.com/ryanczhang7/manga-translator/pull/4 — merged
2026-09-13T04:49:55Z as `b430224`, head `1302c63`.

Both required checks passed on the first run. Timings read out rather than
skipped, per `/advance-story`: a pass within 10% of a limit is a pending
failure, and pass/fail alone does not show that.

```
boundaries   pass       6s
gates        pass   10m43s
```

Composition of the 10m43s, from the job log
(`actions/runs/34738221544/job/103673335835`), computed from the step
timestamps:

| Step | Wall |
|---|---|
| `scripts/selftest.sh` | **6m36s** — the bulk, and it is the harness's own suite, not this project's |
| `gates.sh --list` | 50s |
| `gates.sh --audit` | 50s |
| `gates.sh` (the gates themselves) | **2m12s** |
| checkout + `setup-uv` + job setup/teardown | 15s |

**Headroom: no limit is anywhere near.** `.github/workflows/` sets no
`timeout-minutes` on either job, so GitHub's default of **360 minutes** applies.
The `gates` job used **3.0%** of it. This is the check that sent a story back
from REVIEW once before — a green run clearing a 30 s hook default by three
seconds — and there is no analogous margin here.

**Drift against MT-002's first run**, the only comparable baseline, on a branch
that changed **four documentation files and no code**:

| Step | MT-002 | MT-030 | Δ |
|---|---|---|---|
| `boundaries` job | 6s | 6s | — |
| `gates` job | 10m16s | 10m43s | +27s (+4.4%) |
| `selftest.sh` | 6m26s | 6m36s | +10s |
| `--list` + `--audit` | 1m32s | 1m40s | +8s |
| `gates.sh` | 2m02s | 2m12s | +10s |

Every delta is single-digit seconds on a docs-only diff, so this is **runner
variance, not growth this story caused**. Recorded because the next story to
read this table should compare against it rather than against MT-002, and
because the trend is what will matter once the weights land: `build` runs
PyInstaller and `project.conf` already warns it goes from 34 s to minutes once
~1 GB of weights is bundled. **`selftest.sh` at 6m36s is 62% of the job and
belongs to the harness, not the project** — it is the first thing to look at if
this job ever approaches a limit, and nothing in this project's own gates is
close.

**No gate reached REVIEW marked *pending CI*.** The one BLOCKED report during
this story (`uv: command not found`, five required gates) was path 1 of the
triage — a missing tool, resolved locally against `environment.md` §5 — not path
2, so there is no gate id awaiting a CI run to be quoted here. PO-1 carries the
detail.

**Branch check after merge**, per the hazard `manga-translator-8e` raised: a
follow-up push to an auto-deleted branch silently recreates it and strands the
commit.

```
$ git branch -r --contains 1302c63
  origin/HEAD -> origin/main
  origin/main
  origin/story/MT-030-a-permissively-licensed-text-detector-or
```

`origin/main` contains the head commit, so nothing is stranded. The branch was
not auto-deleted; this closing commit lands on it and needs its own PR, exactly
as MT-002's did.

### Verdict on the story

**Delivered.** All five acceptance criteria discharged, verified by the
orchestrator against the document rather than against the subagent's report
(PO-4). The outcome is the one the timebox named as a success: no
permissively-licensed replacement found, both remaining options put to the user
with the licence text quoted in front of them.

**What this story changed beyond its own brief**, and the reason it was worth
more than a "no" — the premise was wrong. One of the two objections to the
incumbent does not exist, and correcting it took four files: `stack.md`,
MT-002's audit, `MT-007.md` and `MT-024.md` (PO-7, PO-8, PO-9).

**Still open, and it is the user's:** ship GPL-3.0 knowingly, or commission a
detector. `architecture.md` has no decision record and deliberately will not
until that is answered (PO-3).
