---
id: MT-034
title: Directory-shaped paths classify as source
slug: directory-shaped-paths-classify-as-sourc
epic: EPIC-01
type: chore
status: done
phase: DONE
branch: story/MT-034-directory-shaped-paths-classify-as-sourc
depends_on: [MT-033]   # MT-033 shipped the workaround this story revisits, and the role machinery AC-2 asserts against
required_gates: []  # gate ids that are optional for the repo but binding for THIS story
---

## Context

**Filed 2026-09-14 by the orchestrator, out of [[MT-033]]'s corrective RED.** The
Test Developer hit it, escalated it rather than resolving it quietly, and the
orchestrator reproduced it independently against a real `main` checkout; MT-033
then routed around it deliberately and recorded that it had. This story is the
follow-up that escalation asked for — MT-033 `## Regressions` R-7 names it *"the
recommended next story"*.

### The defect

Every `paths.conf` glob for a directory carries a `/` — `docs/**`,
`**/tests/**`, `scripts/**`, `fixtures/**` — so a **bare** directory name matches
none of them and falls to the classifier's restrictive default, `source`. And the
trailing slash an agent actually types does not survive to the classifier:
`normalize_rel` skips the empty last segment.

```
$ bash -c '. .claude/hooks/lib.sh; for p in docs docs/ docs/sub docs/backlog src src/ tests tests/ scripts fixtures; do
    printf "classify %-14s -> %s\n" "$p" "$(classify "$p")"; done'
classify docs           -> source
classify docs/          -> docs
classify docs/sub       -> docs
classify docs/backlog   -> docs
classify src            -> source
classify src/           -> source
classify tests          -> source
classify tests/         -> test
classify scripts        -> source
classify fixtures       -> source

$ bash -c '. .claude/hooks/lib.sh; normalize_rel "docs/"'
docs
```
`[MEASURED — 2026-09-14, orchestrator]`

So `docs`, `tests`, `scripts` and `fixtures` all classify `source`. It is not a
`docs` quirk and it is not a `mv` or a `-t` quirk: it is `classify`, and it
reaches `cp`, `mv`, `rm`, `touch` and redirects identically, because every branch
of the guard funnels through the same call at `phase-guard.sh:42`.

### It fails in both directions, and only one of them was known

**Direction 1 — a false positive, refusing a write the phase permits.** Measured
against a real `main` checkout at `b1583d5` via `git worktree`, driving `main`'s
own hooks against the `_lib.sh` fixture:

```
RED     mv src/main.ts docs/          BLOCKED  docs
RED     cp docs/notes.md docs/        BLOCKED  docs
RED     mv docs/notes.md docs/        BLOCKED  docs
REVIEW  cp docs/notes.md docs/        BLOCKED  docs
REVIEW  mv docs/notes.md docs/sub/    ALLOWED
```
`[MEASURED — 2026-09-14, orchestrator, git worktree at main]`

Line 4 is the sharp one. `cp docs/notes.md docs/` is **refused in REVIEW**, a
phase in which `docs` is explicitly writable, on a command that writes only into
`docs`. `cp x docs/` is a command an agent writes every day, which makes this the
false positive most likely to teach an agent that denials are noise — precisely
the instinct law 5 of `CLAUDE.md` exists to suppress, and the reason [[MT-031]]
and [[MT-033]] exist at all.

**Direction 2 — a bypass, permitting a write into a directory the phase freezes.
This was not in the escalation.** It follows from the same table: `tests`
classifies `source`, and GREEN *permits* `source`. Measured by the Lead PO at
PLANNED, driving the real hook against the `_lib.sh` fixture on this branch:

```
GREEN   cp docs/notes.md tests/                    ALLOWED
GREEN   mv docs/notes.md tests/                    ALLOWED
GREEN   touch tests/                               ALLOWED
GREEN   cp docs/notes.md tests/sub/                BLOCKED  tests/sub
RED     cp docs/notes.md src/                      BLOCKED  src
RED     cp docs/notes.md src/sub/                  BLOCKED  src/sub
```
`[MEASURED — 2026-09-14, Lead PO, .claude/tests/_lib.sh fixture]`

**A write into the frozen test tree is permitted in GREEN when the destination is
named at the top level, and refused one directory down.** That is law 2 — tests
are frozen during GREEN — with a hole in it, reachable by a command shorter than
the one that is caught. It is the same single defect from the other side, and it
matters for the shape of the fix: **the cheapest way to make direction 1 stop is
to weaken the `source` default, which makes direction 2 worse.** See AC-4.

**Direction 3 — the same write, two spellings, two verdicts.**

```
REVIEW  cp docs/notes.md /tmp/tmp.MNotHfZPZT/docs/   ALLOWED
REVIEW  cp docs/notes.md docs/                       BLOCKED  docs
```
`[MEASURED — 2026-09-14, Lead PO, same fixture]`

The absolute branch of `check_path` reaches `classify` through `to_rel`, which
**keeps** a trailing slash; the relative branch goes through `normalize_rel`
first, which strips it. Verified directly:

```
$ bash -c '. .claude/hooks/lib.sh; to_rel "$PWD/docs/"'   -> docs/
$ bash -c '. .claude/hooks/lib.sh; normalize_rel "docs/"' -> docs
```
`[MEASURED — 2026-09-14, Lead PO]`

### MT-033's shipped workaround, which this story should let someone remove

MT-033 R-7 could not assert its `mv -t` table with `docs/` as the permitted
target directory, because `docs` denies first however `mvops()` is written. RED
substituted **`docs/backlog/`** for `docs/` in fifteen places in
`.claude/tests/phase-guard.test.sh`, with a thirty-line comment at line ~1153
explaining why. That workaround is correct, deliberate and shipped — and once
this story lands, the reason for it is gone. **Revisiting it is explicitly NOT
part of this story** (see `## Out of scope`); it is named here so the next person
finds it rather than rediscovering it.

### The verifier

`bash scripts/selftest.sh`, which `.github/workflows/gates.yml` runs as the
**required** CI step *"Harness self-test"*, ahead of the gates. Confirmed
2026-09-14 rather than inherited from MT-033: `grep -n "^covers"
.claude/harness/project.conf` names `src/mangatl/**`, its subtrees and
`packaging/**` and **nothing under `.claude/`**, so no gate reads a file this
story touches and no `required_gates` entry would bite. `required_gates: []` is a
decision, not an oversight. `bash scripts/gates.sh` is still run in full at GATES
and must stay green; it will not exercise the change.

### Epic

Filed into [[EPIC-01]], which is where MT-031, MT-032 and MT-033 live because
MT-001 built the machinery they correct. **Like them, this story is not a clause
of EPIC-01's `## Done when`** — that was discharged by MT-001, MT-002 and MT-003
and is not reopened here. EPIC-01's `## Stories` list and its closing note need
one line adding for MT-034; that edit is part of this story's PLANNED work and is
already made.

## Acceptance criteria

- **AC-1** — Given a command in `REVIEW` whose only write target is a
  **directory-shaped path in a category the phase permits** — each of
  `cp docs/notes.md docs/`, `mv docs/notes.md docs/`, `rm -rf docs/` and
  `touch docs/` — when the guard runs, then each is **permitted**. Measured
  BLOCKED on `docs` today for the first two, on `main` at `b1583d5` and on the
  fixture.
  *Control (the criterion must not be satisfiable by making the guard quieter):*
  in the same phase, `cp docs/notes.md src/` and `cp docs/notes.md src/sub/`
  must stay **BLOCKED** on `src` and `src/sub`. Both are blocked today.

- **AC-2** — Given `mv src/main.ts docs/` in `RED`, when the guard runs, then it
  is **blocked**, the reported `path:` is **`src/main.ts`**, and the denial
  carries `operand:  source of mv (removed by the move)`. Today it is blocked on
  `docs` — the wrong operand, in a category RED permits, with the role line
  naming it a destination. This is [[MT-033]] AC-1 finally landing on the command
  shape MT-033 could not reach.

- **AC-3** — Given `cp docs/notes.md tests/`, `mv docs/notes.md tests/` and
  `touch tests/` in `GREEN`, when the guard runs, then each is **blocked** naming
  `tests`, category `test`. All three are **ALLOWED today** — a write into the
  frozen test tree during GREEN. The one-level-deeper form
  `cp docs/notes.md tests/sub/` is blocked today and must stay blocked.

- **AC-3b** — Given `rm -rf tests/` in `GREEN`, when the guard runs, then it is
  **blocked** naming `tests`, category `test`. **Allowed today.** Added by the
  orchestrator on reproducing AC-3, 2026-09-14 — see the verification block
  below. It is the same defect as AC-3 and needs no separate mechanism, but it
  is stated as its own criterion because it is the one instance whose
  consequence is unrecoverable, and a story that lists only `cp`, `mv` and
  `touch` invites a fix tested against those three.

### Orchestrator's verification of AC-3, 2026-09-14, and one case the plan did not test

The planner reported AC-3 from its own fixture run. Reproduced here independently
before the story was accepted, against the real hook and the `_lib.sh` fixture,
with the `src` control in the same run:

```
=== GREEN freezes tests. Does it? ===
  cp docs/notes.md tests/                      ALLOWED
  mv docs/notes.md tests/                      ALLOWED
  touch tests/                                 ALLOWED
  rm -rf tests/                                ALLOWED          <- NOT IN THE PLAN
  cp docs/notes.md tests/sub/                  BLOCKED  tests/sub          test
  cp docs/notes.md tests/main.test.ts          BLOCKED  tests/main.test.ts test

=== and the src control, which must NOT change ===
  cp docs/notes.md src/                        BLOCKED  src          source
  cp docs/notes.md src/sub/                    BLOCKED  src/sub      source
  touch src/                                   BLOCKED  src          source
```
`[MEASURED — 2026-09-14, orchestrator, on main at 84341c9]`

**`rm -rf tests/` is permitted in GREEN.** The planner tested `cp`, `mv` and
`touch` and did not test deletion. This is the sharpest instance in the whole
story: GREEN is the phase that freezes tests precisely because it is the phase in
which an agent has a motive to make a failing test stop failing, and the guard
permits deleting the entire test tree provided the path is spelled without a
trailing segment. `rm -rf tests/main.test.ts` is blocked; `rm -rf tests/` is not.

It also reframes the story's priority. The `## Context` leads with the false
positive — `cp docs/notes.md docs/` refused in REVIEW — which is the *annoying*
half and the one that teaches agents to ignore denials. This is the *dangerous*
half, and the two share one cause, so the fix is the same. Both are recorded, and
neither should be dropped if the story is ever trimmed: AC-1 is the false
positive, AC-3/AC-3b are the bypass.

**The `src` control holds in the same run**, which is what makes AC-4 more than a
formality: whatever fix lands must move `tests` and leave `src` alone, and both
were measured in one pass rather than argued about.

- **AC-4** — Given `src`, `src/`, `src/mangatl`, `src/mangatl/ui`, `packaging`
  and `src/brand-new.ts`, when classified, then **every one is `source`**, before
  and after this change. `classify src` → `source` is *correct* and must survive:
  `src` is not a category directory, and a fix that resolves a bare directory to
  its children's category, or that stops defaulting to `source`, breaks it.
  *Control, and it is a mutation somebody runs rather than a footnote:* replace
  the fix with the cheapest thing that satisfies AC-1 — make an unmatched path
  default to any category `RED` permits instead of to `source`. AC-1 then passes
  and **every assertion in AC-4 must go red**, along with AC-3's, whose subject is
  the same default. Predicted: 6 red on AC-4, 4 red on AC-3, 0 movement on AC-1.
  If AC-4 does not move under that mutation, AC-4 is asserting nothing.

- **AC-5** — Given a directory that does **not exist on disk** — `fixtures/`,
  which `paths.conf` classifies `test` and which this repository has not yet
  created (`ls -d fixtures` → *No such file or directory*, 2026-09-14) — when
  classified, then it is `test`, and the answer is **identical** whether or not
  the directory exists.
  *Control:* run the same classification twice in one fixture repository, once
  before `mkdir fixtures` and once after. The two answers must match exactly. A
  fix that stats the path fails this, and `classify` consulting the filesystem is
  a contract change — see C-5.

- **AC-6** — Given every path `git ls-files` reports for this repository, when
  each is classified through `classify_stdin`, then the category is **identical
  before and after this change**: `git ls-files | classify_stdin` diffed across
  the change yields **zero** differing lines. This is the blast-radius criterion.
  `classify_stdin` is what `gate_tree_hash`, `check-boundaries.sh` and
  `gates.sh`'s changed-source check all read; a fix that lands there, or in
  `paths.conf`, changes what a recorded gate hash means, and this criterion is
  what proves the fix did not.
  *Control:* the same diff run against a `paths.conf`-based fix (add
  `docs | docs`, `test | **/tests`, …) — it must be possible to show which
  option this criterion rejects, not merely to assert that the chosen one passes.

## Contract

Pinned by the Lead PO before dispatch, 2026-09-14. **RED may amend any block
below in place, with a reason stated in the block; GREEN builds what the amended
block says.**

### C-1 Files, and their phase category

| Path | Category | Writable in |
|---|---|---|
| `.claude/hooks/lib.sh` | `harness` | every phase |
| `.claude/tests/lib.test.sh` | `harness` | every phase |
| `.claude/tests/phase-guard.test.sh` | `harness` | every phase |

`.claude/hooks/phase-guard.sh` is expected **not** to change — see C-4.

As in MT-031 and MT-033, **this story has no phase geometry**: nothing it touches
is frozen by any phase, so the lock will not enforce test-first here and the
discipline is the only thing that does. Two consequences carry over unchanged:
the mutation probes below **can run in RED**, and they still go through
`bash scripts/mutate.sh` so the restore is verified and recorded. `sed -i` on
`lib.sh` is working around the lock even though the lock would allow it.

### C-2 The defect, stated exactly

`.claude/hooks/lib.sh`, `classify()`:

```bash
classify() {
  local rel="$1" cat
  [ -z "$rel" ] && { printf 'outside'; return; }
  cat="$(printf '%s\n' "$rel" | classify_stdin | awk -F'\t' 'NR == 1 { print $1 }')"
  [ -z "$cat" ] && cat=source
  [ "$cat" = "source" ] && is_ignored "$rel" && cat=ignored
  printf '%s' "$cat"
}
```

`classify_stdin` converts each `paths.conf` glob to a regex anchored `^…$`.
`docs/**` becomes `^docs/.*$`, which **requires** a slash, so the string `docs`
matches nothing and the function's documented default — *"the right default for a
file about to be authored"* — applies to something that is not a file.

Two facts make this reachable rather than theoretical:

1. `normalize_rel` drops the trailing slash (`''|.) continue`), so a candidate the
   author wrote as `docs/` arrives at `classify` as `docs`.
2. `paths.conf` already contains the fix for one category and only one. Its
   vendor section carries thirteen twin rules under the comment *"The directories
   themselves, so that `rm -rf node_modules` and friends are classified as vendor
   rather than falling through to source."* Somebody met this defect, solved it
   locally for `vendor`, and the same thing was never done for `docs`, `test` or
   `harness`. **That is evidence about which fix to choose, not merely context —
   see C-3.**

### C-3 The fix: a trailing-slash retry inside `classify`, and nowhere else

**Decision, and it is the Lead PO's to make rather than GREEN's.** When a path
matches no `paths.conf` rule and would therefore take the `source` default,
`classify` tries the same path **with a single trailing `/` appended** before
falling back. If the slashed form matches a rule, that rule's category is the
answer. If it does not, behaviour is exactly as today: `is_ignored`, then
`source`.

Ordering, which is part of the pin: **rules on the bare form → rules on the
slashed form → `git check-ignore` → `source`.** Putting `is_ignored` last
preserves today's precedence, where an explicit rule beats `.gitignore`.

Measured behaviour of that rule, run at PLANNED against the real `paths.conf`:

| path | today | under C-3 | note |
|---|---|---|---|
| `docs` | source | **docs** | the false positive, AC-1 |
| `tests` | source | **test** | the bypass, AC-3 |
| `scripts` | source | **harness** | |
| `fixtures` | source | **test** | does not exist on disk, AC-5 |
| `.claude` | source | **harness** | |
| `.github` | source | **harness** | |
| `src` | source | **source** | **AC-4, the trap. Unchanged.** |
| `src/` | source | **source** | |
| `src/mangatl` | source | **source** | |
| `src/mangatl/ui` | source | **source** | |
| `packaging` | source | **source** | |
| `src/tests` | source | **test** | not only top-level — any bare dir whose rule needs a following slash |
| `src/test`, `src/__tests__`, `src/spec` | source | **test** | |
| `docs/backlog`, `tests/core`, `docs/wiki/design` | unchanged | unchanged | the parent glob already supplies the slash |
| `node_modules`, `dist`, `coverage`, `target`, `addons` | vendor | vendor | the twin block already covers them |
| `.vitest`, `playwright-report`, `spikes` | ignored | ignored | no rule either way; `is_ignored` still decides |

`[MEASURED — 2026-09-14, Lead PO, by classifying "$p" and "$p/" against the
shipped paths.conf]`

**The `src` row is the whole reason this shape was chosen over the alternatives,
and it is safe for a reason worth stating: the retry is rule-driven. There is no
rule whose glob begins `src/`, so no retry can invent a category for `src`.** The
fix cannot resolve a directory to its children because it never looks at
children.

**Rejected: `paths.conf` (add bare-directory twin globs).** Cheapest to write, has
precedent in the file, and would satisfy AC-1 through AC-5. Rejected on two
grounds. First, **AC-6**: `paths.conf` is read by `classify_stdin`, which is what
`gate_tree_hash` and `check-boundaries.sh` consume, so the fix lands in the one
function whose output a CI check compares hashes of — today no tracked path is
spelled like a bare directory so the hash would not move, but the surface is
wider than the defect. Second, and decisively, **the precedent argues against
itself**: a twin-rule list is exactly what shipped for `vendor`, and it did not
generalise — nobody remembered to write the `docs`, `test` and `harness` twins,
and the result is the live false positive above. A fix that depends on a future
maintainer remembering is a fix that has already failed once, in this file.

**Rejected: `normalize_rel` (preserve the trailing slash).** It would work, and it
would fix direction 3 at the source. Rejected because `normalize_rel`'s output is
used for **path identity**, not only for classification: `phase-guard.sh:209`
feeds it straight into the `$EXEMPT` membership test, which compares the candidate
string for **exact equality** against the resolved `scripts/mutate.sh` FILE
argument. A trailing slash surviving there silently un-exempts the one diagnostic
the harness itself requires in RED — MT-033's R-5 hazard in a new coat.
`command_cwd` also joins its output with `/`, so `docs/` + `/` + `x` would need
re-collapsing. Wider blast radius, for a narrower gain.

### C-4 Mechanism, and the one process budget

`classify` is called once per candidate, from `phase-guard.sh:42`, under a 15 s
`PreToolUse` budget, and **a hook that times out fails OPEN — the lock silently
off**. Measured at PLANNED on this machine, 20 iterations each: one `classify`
call costs 50–175 ms, dominated by process spawn, and an unmatched path already
pays two extra `git check-ignore` forks on top.

**Constraint: the retry must not add a process.** `classify_stdin` reads one path
per line and prints one answer per line, so both spellings can be judged in the
**same** `awk` invocation — feed it `rel` and `rel/`, take the first line that is
not the default, fall through otherwise. That is a suggestion, not a mandate; any
mechanism that adds no fork in the common path is acceptable, and a lazy second
call performed only when the first missed is acceptable too, because that branch
already pays for `git check-ignore`.

**`phase-guard.sh` should not need to change.** If GREEN finds it must, that is a
signal the fix has drifted out of `classify` — stop and say so in the handoff
rather than widening quietly.

Guard against the obvious slip: do not append a slash to a path that already ends
in one, and do not retry a path that **did** match a rule.

### C-5 A directory that does not exist is still classifiable — AC-5, decided

`classify` consults `git check-ignore` but **never the filesystem**, and that is
deliberate: MT-031 pinned it in `write_candidates`' header — *"Nothing here
consults the filesystem"* — for a reason this story must not undo. The guard
judges a **token in a command string**, not an inode. A `stat` would make the
verdict depend on the order commands run in (`mkdir docs` then `mv x docs/` would
be judged differently from the same two commands reversed), it would add a
syscall per candidate to a hook that fails open on timeout, and it would make the
lock unable to judge a write to a path that is about to be created — which is most
of them.

**So the fix stays filesystem-blind, and AC-5 pins it.** The price, recorded here
rather than discovered later: a **file** named exactly `docs`, `tests`, `scripts`
or `fixtures`, with no extension, would classify as that directory's category
rather than `source`. No such file exists in this repository, none is plausible,
and the alternative costs the contract above. If GREEN proposes a
filesystem-consulting fix, that is an escalation to the Lead PO, not a choice.

### C-6 What must keep working

Beyond the whole of the existing suite:

```
REVIEW  cp docs/notes.md src/          -> BLOCKED src        (AC-1's control)
REVIEW  cp docs/notes.md src/sub/      -> BLOCKED src/sub
GREEN   cp docs/notes.md tests/sub/    -> BLOCKED tests/sub  (AC-3's control)
REVIEW  mv docs/notes.md docs/sub/     -> ALLOWED            (already correct)
RED     cp docs/notes.md docs/backlog/ -> ALLOWED            (already correct)
RED     rm -rf .vitest                 -> ALLOWED  (ignored; is_ignored must still win)
RED     rm -rf node_modules            -> ALLOWED  (vendor twin rule, unchanged)
RED     bash scripts/mutate.sh src/main.ts 's/a/b/' -- true  -> ALLOWED (the exemption)
RED     git commit -m "move the docs/ fixtures"              -> ALLOWED (prose, MT-031 AC-1)
```

The first five are measured above; the rest are MT-031's and MT-033's, already
asserted in `.claude/tests/phase-guard.test.sh`.

### C-7 Callers of every changed signature

Compiled by the Lead PO before dispatch, 2026-09-14, by
`grep -rn '<name>' --include=*.sh`. **RED's handoff must state this list was
re-checked against the tree** — during RED the old behaviour still works and its
callers still pass, so RED cannot derive it.

No exported name changes: `classify` keeps its name and its single argument. What
changes is the **category it returns for a directory-shaped path**.

- **`.claude/hooks/lib.sh`** — `classify` itself. **Twelve** files source this
  library; a syntax error here takes all of them down, and
  `bash scripts/selftest.sh` is what notices.
- **`.claude/hooks/phase-guard.sh:42`** — the **only** production caller of
  `classify`. Expected to be unchanged (C-4).
- **`.claude/tests/lib.test.sh:52,55,61–69`** — eight direct `classify`
  assertions, including `classify ".vitest"` → `ignored` and
  `classify "playwright-report"` → `ignored`, which are the `is_ignored`
  precedence cases C-3's ordering must preserve. The table at line 52 is driven by
  a `case%%=*` list and is where AC-4's and AC-5's classification assertions
  belong.
- **`.claude/tests/phase-guard.test.sh`** — the end-to-end assertions. AC-1, AC-2
  and AC-3 belong here; ~268 existing assertions must stay green.
- **NOT callers, and the point of AC-6:** `classify_stdin`, and therefore
  `gate_tree_hash`, `code_changed_since`, `scripts/check-boundaries.sh:97` and
  `scripts/gates.sh:621`. They are unchanged by C-3 and AC-6 is what proves it.
- **`.claude/harness/paths.conf`** — **not changed.** Anything in it would land in
  `classify_stdin`; see C-3's rejected options.

### C-8 The oracle partition

Nothing here is a metric, a tolerance or a threshold. Every criterion is a path
or a command string, a phase, and the exact category or `path:` the guard must
report.

| Kind | Criteria | Instruction to RED |
|---|---|---|
| **Settled** | AC-1, AC-2, AC-3, AC-4, AC-5 — C-3's measured table is the oracle | *Read the categories out of C-3. Do not re-derive them, and do not "calibrate" the rule list.* |
| **Mechanical** | AC-2's `path:` and `operand:` strings, AC-6's diff | *Pin exactly. The three role strings are MT-033 C-5's vocabulary, verbatim.* |
| **Oracle-free** | none | — |

The one thing RED **is** asked to invent is the AC-4 and AC-6 mutation
harnesses — the cheapest-wrong-fix mutation and the `git ls-files` diff. Both are
described as conditions, not as implementations.

### RED's escalations, reproduced independently by the orchestrator, 2026-09-15

Both are claims about the Contract, so both are reproduced before acceptance —
different inputs, without reusing RED's code.

**Escalation 2 — a third false positive, and it is sharper than RED reported.**
RED found `cp docs/notes.md tests/` BLOCKED in `RED`. Reproduced here with the
one-segment-deeper control in the same run, which RED did not include:

```
=== RED: the phase whose JOB is writing tests ===
  cp docs/notes.md tests/                    BLOCKED  tests      source
  mv docs/notes.md tests/                    BLOCKED  tests      source
  touch tests/                               BLOCKED  tests      source
  cp docs/notes.md tests/new.test.ts         ALLOWED
=== and RED writing docs, which RED also permits ===
  cp src/main.ts docs/                       BLOCKED  docs       source
  touch docs/                                BLOCKED  docs       source
```
`[MEASURED — 2026-09-15, orchestrator, _lib.sh fixture on this branch]`

**In RED — the phase whose entire job is writing tests — `touch tests/` is
refused and `tests/new.test.ts` is allowed.** The Test Developer role is blocked
from the directory-shaped spelling of the one thing it exists to do, and
permitted the file-shaped spelling of the same thing. That is the same single
defect as AC-1 and AC-3, seen from a third angle, and it needs no extra
mechanism.

**Accepted as AC-1 coverage, not as an amendment, and RED was right to ask.**
AC-1's *Given* names `REVIEW`, so a strict reading puts `RED` + `tests/` outside
it. But the criterion behind AC-1 is *"a directory-shaped path in a category the
phase permits is permitted"*, and the (phase, category) pair is incidental to
both the defect and the fix. Asserting more than a criterion literally requires
is additional coverage, never a criterion change — what would require an
`## Amendments` entry is a test demanding *different behaviour* from what the ACs
specify, and this demands the same behaviour on more inputs.
`## Acceptance criteria` is byte-identical to `main`, which
`check-boundaries.sh` confirms.

**Escalation 1 — C-7's file count is wrong, and so is RED's correction. It is
12.** C-7 said nineteen files source `lib.sh`; RED measured nine. Counted here by
both spellings, because the two groups source it differently — the hooks by
`"$(dirname "${BASH_SOURCE[0]}")/lib.sh"`, everything else by a path containing
`hooks/lib.sh`:

```
$ grep -ln "lib.sh" .claude/hooks/*.sh                       -> 4
    gate-reminder.sh  inject-state.sh  phase-guard.sh  statusline.sh
$ grep -rln -E '(\. |source )[^ ]*lib\.sh|hooks/lib\.sh' --include=*.sh scripts .claude/tests
    scripts/check-boundaries.sh  scripts/gates.sh  scripts/phase.sh
    .claude/tests/boundaries.test.sh  .claude/tests/gate-reminder.test.sh
    .claude/tests/lib.test.sh  .claude/tests/phase-guard.test.sh
    .claude/tests/phase.test.sh                              -> 8
$ (both, sorted -u)                                          -> 12
```
`[MEASURED — 2026-09-15, orchestrator]`

RED's nine undercounted the test suites (it found two; there are five). Nineteen
was simply wrong. **The clause's point is untouched either way** — a syntax error
in `lib.sh` takes all of them down and `bash scripts/selftest.sh` is what
notices — and C-7 is corrected to twelve rather than to nine.

**C-7's two load-bearing claims were verified exactly, and one of them matters
more than the count:**

```
$ grep -rn -E '\bclassify "' --include=*.sh .claude/hooks scripts
.claude/hooks/phase-guard.sh:42:  cat="$(classify "$rel")"
```

One production caller, as pinned. And `classify_stdin`'s consumers are
`lib.sh:707` (which is `classify` itself), `lib.sh:857` (`code_changed_since`),
`lib.sh:903` (`gate_tree_hash`), `check-boundaries.sh:97` and `gates.sh:621`.

**`classify` delegating to `classify_stdin` at line 707 is what makes C-3
buildable at all**, and it is worth stating because it is not obvious from C-3:
the retry changes how `classify` *uses* `classify_stdin`, not `classify_stdin`
itself. So AC-6 — `classify_stdin`'s output identical before and after — is not a
constraint the fix has to be careful around; it falls out of putting the retry in
the caller. A fix that edited `classify_stdin` or `paths.conf` would have to earn
AC-6 instead.

## Deferred verifications

Named by the Lead PO at PLANNED, 2026-09-14.

- **DV-1 — AC-4's control cannot run in RED and must not be claimed there.
  Owner: GATES.** It requires mutating the *finished* fix so that an unmatched
  path stops defaulting to `source`, and in RED that fix does not exist. Expected:
  AC-4's 6 assertions and AC-3's 4 go red; AC-1's stay green. **RED is expected to
  decline this in writing** and to record, instead, the mirror-image probe it
  *can* run: mutate the shipped `classify` so that the `source` default becomes
  something RED permits, and confirm AC-4's assertions are red against the
  unfixed tree. Run it with `bash scripts/mutate.sh`, never `sed -i`.
  **RED, 2026-09-14: DECLINED IN WRITING, and it is GATES'.** I cannot run DV-1.
  It mutates *the finished fix*, and in RED there is no fix to mutate: `classify`
  still has no trailing-slash retry, so "replace the retry with the cheapest
  wrong thing" has nothing to replace. I have not run it and I am not claiming
  its result.
  The mirror-image probe DV-1 asks RED for instead **was** run, twice —
  `## Regressions` P-1 (unit) and P-6 (guard) — with
  `bash scripts/mutate.sh`, never `sed -i`. P-1 measured **exactly 6 red in the
  AC-4 block**, which is the number DV-1 predicts, against the unfixed tree.
  **Result: _(GATES to paste — what was mutated, what went red, that the file was
  restored — or `WAIVED` with a reason.)_**

- **DV-2 — AC-6's diff is only meaningful against both sides. Owner: GATES.** Run
  `git ls-files | classify_stdin > before.txt` against `main`, the same against
  the finished branch, and `diff` them: **zero** differing lines. RED can run the
  `main` side only. Paste both commands and the diff.
  **RED, 2026-09-14: HALF DECLINED IN WRITING.** A diff needs two sides and RED
  has one; I have not run the diff and am not claiming it. What I did run is the
  `before` side, at `35afba6` on this branch with the fix absent, which is the
  same code as `main` for every file `classify_stdin` reads:

  ```
  $ bash -c '. .claude/hooks/lib.sh; git ls-files | classify_stdin' > before.txt
  $ wc -l before.txt
  174 before.txt
  $ awk -F'\t' '{c[$1]++} END{for(k in c) printf "%-9s %d\n", k, c[k]}' before.txt
  config    4
  docs      73
  source    15
  harness   79
  test      3
  $ sha256sum before.txt | cut -c1-16
  87fca95b68292e8f
  ```

  And I ran the prediction the diff will confirm: every tracked path re-judged
  with a `/` appended, compared against `before.txt`. **Zero lines move.**

  ```
  $ paste -d'\t' <(cut -f1 before.txt) \
      <(git ls-files | sed 's|$|/|' | classify_stdin | cut -f1) <(cut -f2 before.txt) \
    | awk -F'\t' '$1=="source" && $2!="source" { print "CHANGES: " $3 }'
  (no output)
  ```

  The fifteen `source` paths are `.mcp.json.example` and fourteen `src/mangatl/**`
  Python files; not one is spelled like a bare directory, so C-3's retry cannot
  reach any of them. GATES still owes the real two-sided diff — this is a
  prediction, not the verification.
  **Result: _(GATES to paste.)_**

- **DV-3 — AC-6's rejected-option control. Owner: GATES.** The same diff against a
  throwaway `paths.conf` carrying bare-directory twin rules, to show the criterion
  can distinguish the two designs rather than merely passing. Expected: a non-empty
  diff, or an explicit statement that no tracked path is spelled like a bare
  directory and the criterion therefore separates the options by *surface* rather
  than by *today's output* — whichever is true, measured, not reasoned. A
  `paths.conf` mutated for this probe is restored via `mutate.sh`.
  **RED, 2026-09-14: DECLINED IN WRITING, and it is GATES'.** DV-3 compares the
  chosen design against the rejected one across the AC-6 diff, and RED has
  neither side of that diff. I have not run it.
  RED did, however, settle the question DV-3 says must be *measured, not
  reasoned*, and the answer is the second branch: **no tracked path is spelled
  like a bare directory**, so the criterion separates the two designs by
  *surface* rather than by today's output — see DV-2 above, where the prediction
  moves zero lines. RED also built the *surface* half into the suite as an
  assertion rather than an argument: `.claude/tests/lib.test.sh` now pins that
  `classify_stdin` still gives a bare directory name the `source` default, and
  `## Regressions` P-2 shows that assertion going red under exactly the rejected
  twin-rule `paths.conf`. GATES still owes the diff.
  **Result: _(GATES to paste, or `WAIVED` with a reason.)_**

- **DV-4 — AC-5's existence control. Owner: RED.** Classify `fixtures` in one
  fixture repository before and after `mkdir fixtures`; the two answers must be
  identical. RED **can** run this — it needs no fix, only a fixture — so it is
  listed here for its result rather than for a decline.
  **Result: RUN IN RED, 2026-09-14, against the UNFIXED tree.** One `_lib.sh`
  fixture, `classify "fixtures"` called before and after `mkdir fixtures` plus a
  file inside it, with `HARNESS_ROOT`/`HARNESS_DIR` repointed at a second
  fixture for the "after" half:

  ```
  $ ls -d fixtures                      # in this repository
  ls: cannot access 'fixtures': No such file or directory

  DV-4 (unfixed tree):
    classify fixtures   before mkdir = source   after mkdir = source
    classify fixtures/  before mkdir = test     after mkdir = test
    IDENTICAL: yes
  ```

  The control **holds today** — `classify` consults `git check-ignore` and never
  the filesystem, so creating the directory changes nothing. The *value* is
  wrong today (`source`, not `test`) and that half is AC-5's red assertion. Both
  halves are now assertions in `.claude/tests/lib.test.sh`; the identity half is
  green on arrival and is earned by a probe that makes `classify` stat the path
  — see `## Regressions` P-3, where it goes red with
  `expected: source / actual: ignored`. GREEN must re-read this row as
  `test`/`test`, still identical.


- **DV-5 — the hook latency budget, which no assertion can guard. Owner: GATES.**
  Added by the orchestrator at the end of RED, 2026-09-15, on RED's own
  observation that *"if GREEN's retry adds a fork on the common path, nothing
  goes red and the failure mode is a silently disabled lock on a slow
  machine."* That is exactly the shape a deferred verification exists for: a
  property the suite cannot express, whose failure is invisible and fails
  **open**.

  C-4 measured one `classify` call at 50–175 ms at PLANNED, and MT-031 PO-15
  fixed the `PreToolUse` budget at 15 s. **At GATES, measure the changed path
  the way MT-033 DV-4 did** — N iterations in one process, `main`'s `lib.sh`
  against this branch's, interleaved, on the same inputs — for at least:
  a path that **matches a rule on the bare form** (`src/main.ts`, the common
  path, which must not pay for the retry at all), one that **matches only on the
  slashed form** (`docs`, the miss path), and one that **matches neither**
  (`spikes`, which still falls through to `is_ignored`). Then one whole
  `PreToolUse` invocation end to end, for the budget number.

  **Condition:** the common path must not gain a fork, and no case may move into
  a different order of magnitude. A regression here is not a failing test; it is
  a lock that turns itself off under load.
  **Result: _(GATES to paste — the interleaved A/B, or `WAIVED` with a reason.)_**

### DV-1, DV-2, DV-3 and DV-5 — DISCHARGED at GATES, 2026-09-15, by the orchestrator

Run before `gates.sh`, as the phase requires, against the finished
implementation.

#### DV-1 — AC-4 is asserting something. **All six redden under the cheapest wrong fix.**

The mutation DV-1 names: make an unmatched path stop defaulting to `source`.
That is the cheapest thing that satisfies AC-1, and it is the fix this story
exists to *not* make.

```
bash scripts/mutate.sh .claude/hooks/lib.sh '827s/c = "source"/c = "docs"/' \
  -- bash .claude/tests/lib.test.sh
```

```
  827 -       lp = tolower(path); c = "source"
  827 +       lp = tolower(path); c = "docs"

    FAIL AC-4: classify src
    FAIL AC-4: classify src/
    FAIL AC-4: classify src/mangatl
    FAIL AC-4: classify src/mangatl/ui
    FAIL AC-4: classify packaging
    FAIL AC-4: classify src/brand-new.ts
    FAIL AC-1/AC-3: classify tests
    FAIL AC-1/AC-3: classify scripts
    FAIL AC-1/AC-3: classify .claude
    FAIL AC-1/AC-3: classify .github
    FAIL AC-1/AC-3: classify src/tests   (+ src/test, src/__tests__, src/spec)
    FAIL AC-5: classify fixtures, which does not exist
    FAIL an ignored directory / a file inside one / an ignored report directory
    FAIL classify_stdin still gives a bare directory name the source default
    FAIL source moves the hash
lib: 121 passed, 27 failed
```
`[MEASURED — 2026-09-15, GATES, orchestrator]`

**Six of six AC-4 assertions red, exactly as predicted.** `src`, `src/`,
`src/mangatl`, `src/mangatl/ui`, `packaging` and `src/brand-new.ts` all move off
`source` the moment the default moves, which is precisely the trap C-3 was chosen
to avoid — and AC-4 catches it. The `is_ignored` precedence cases redden too,
because the default is what gates that branch.

Restored and verified: `.claude/state/mutations/log` records
`restored (verified)`, line 827 reads `c = "source"` again, and no `.bak`
remains.

#### DV-2 — AC-6, the blast-radius criterion. **174 paths, zero differing lines.**

```
$ git show main:.claude/hooks/lib.sh > main-lib.sh
$ git ls-files > tracked.txt
$ bash -c '. main-lib.sh;                  classify_stdin < tracked.txt' > before.txt
$ bash -c '. .claude/hooks/lib.sh;         classify_stdin < tracked.txt' > after.txt
$ wc -l < before.txt   ->  174
$ diff before.txt after.txt | wc -l  ->  0
```
`[MEASURED — 2026-09-15, GATES, orchestrator]`

Every tracked path in the repository classifies identically before and after. So
the recorded gate hash, `check-boundaries.sh`'s changed-file classification and
`gates.sh`'s changed-source check are all untouched — which is what let this
story change `classify` without re-baselining anything.

#### DV-3 — the rejected-option control, and it answers in DV-3's *second* branch

DV-3 asked for the same diff against a `paths.conf` carrying bare-directory twin
rules, expecting *"a non-empty diff, or an explicit statement that no tracked
path is spelled like a bare directory and the criterion therefore separates the
options by surface rather than by today's output — whichever is true, measured,
not reasoned."*

Measured: three twin rules appended (`docs | docs`, `test | **/tests`,
`harness | scripts`), the same 174 paths re-classified, `paths.conf` restored and
the restore verified with `git diff` (0 lines).

```
  differing lines vs main: 0
  paths.conf restored: 0 lines of diff
```
`[MEASURED — 2026-09-15, GATES, orchestrator]`

**So AC-6 does NOT distinguish the two designs by output — both move zero
lines — and that is worth saying plainly rather than letting AC-6 look like a
stronger discriminator than it is.** No tracked path in this repository is
spelled like a bare directory (`git ls-files` lists files, never directories), so
no classification of a tracked path can move under either design. AC-6's real
job is the one it does: proving the *chosen* fix moves nothing. The argument that
selects C-3 over the `paths.conf` design is C-3's own — surface, and the twin-rule
precedent that already failed once in that file — not AC-6.

**Method note:** the twin rules were appended and restored with `cp` rather than
through `mutate.sh`, because `mutate.sh`'s `sed` rejected the multi-line insert
expression. `paths.conf` classifies `harness` and is writable in every phase, so
this is not a frozen-file mutation and law 5 is not engaged; the restore was
verified with `git diff --stat` showing zero lines. Recorded because a
hand-rolled mutation should be visible, not because it was improper.

#### DV-5 — the latency budget, which no assertion can guard. **No fork added; the miss path halves.**

Interleaved A/B, `main`'s `lib.sh` against this branch's, 30 `classify` calls per
measurement, same inputs, same process:

```
  src/main.ts      main       116 ms/call      branch     131 ms/call
  docs             main       118 ms/call      branch      55 ms/call
  spikes           main        88 ms/call      branch      96 ms/call
  packaging        main       115 ms/call      branch     113 ms/call
```

The common-path figure looked like a 13 % regression, so it was re-measured three
times rather than reported as one number:

```
  src/main.ts (common path, matches a rule)
    rep 1:  main   226 ms   branch   241 ms
    rep 2:  main   228 ms   branch   228 ms
    rep 3:  main   228 ms   branch   231 ms
```
`[MEASURED — 2026-09-15, GATES, orchestrator]`

**Identical within noise.** (The absolute numbers double between the two runs
because another session on this machine was running its own suite concurrently —
which is a live demonstration of why MT-032 exists, and why every wall-clock
figure in these stories carries the machine it came from.)

**DV-5's condition is met.** The common path gains **no fork**: both spellings go
into the one `classify_stdin` process that `classify` already spawned. Nothing
moves an order of magnitude. And the miss path is **halved** — `docs` used to
fall through to `is_ignored` and pay two `git check-ignore` forks, and now it is
answered from `paths.conf` before `is_ignored` runs. The 15 s `PreToolUse` budget
gains margin rather than losing it, which matters because a hook that times out
fails **open** — the lock silently off.

## Model guidance

**Resolved model of every dispatch is recorded here, by name, never the word
"default"** — `.claude/harness/rules.md`, and the reason: a session setting or an
explicit override can beat the `model:` line in an agent definition, and the
orchestrator cannot see which won unless it writes it down.

| Phase | Agent definition says | Resolved model actually dispatched |
|---|---|---|
| PLANNED | `.claude/agents/lead-po.md` → `model: opus` | **Opus** (`claude-opus-5`). No override: the `lead-po` subagent was dispatched with no `model` argument, so `.claude/agents/lead-po.md` won. Same model as the orchestrator. |
| RED | `.claude/agents/test-developer.md` → `model: opus` | **Opus 5** (`claude-opus-5`), as reported by the running agent itself at the end of RED. **No override was visible to me**, and that is the honest limit of what RED can attest: an agent cannot see whether its `model:` line won or a session setting did — it can only report what it resolved to. The orchestrator that dispatched this phase owns the other half of the record; if it passed a `model` argument, it should say so here. |
| GREEN | `.claude/agents/feature-developer.md` → `model: opus` | **Opus** (`claude-opus-5`); no override observed in the dispatch |
| GATES | `.claude/agents/feature-developer.md` → `model: opus` | **Opus** (`claude-opus-5`); no override observed in the dispatch |

**No model experiment is proposed for this story, and that is deliberate.** The
one verdict recorded in this repository points at the *brief*, not the model — a
partitioned RED brief on the weaker model produced sharper negative controls than
the stronger model without one. This story's criteria are entirely **settled and
mechanical** (C-8): there is no metric to invent, so the variable that has been
measured to matter has nothing to act on here. Running an experiment anyway would
produce a verdict about a story that cannot discriminate.

**Carry C-8's oracle partition into the RED dispatch prompt verbatim.** The
failure mode it prevents here is specific: a Test Developer told "design the
metric" would re-derive the category table in C-3 and could easily decide that
`src` *ought* to resolve to its children — which is AC-4's trap, arrived at
honestly.

## Out of scope

The defect sits next to several tempting adjacent ones. **Every item below is a
non-goal. Touching one is a reason to stop and raise it, not a free win.**

1. **`cp -t` / `--target-directory` for `cp`.** A real, recorded hole
   ([[MT-033]] PO-3): `cp -t src/ docs/notes.md` is pinned **permissive** by an
   existing assertion so it cannot be fixed by accident. `mv -t` was fixed in
   MT-033; `cp -t` was not. Leave it. It is a story of its own.
2. **`install`, `ln -f`, `rsync`, `dd`.** Not in `isname()`, unjudged in every
   phase, recorded out of scope by MT-033 and still out of scope here. Do not add
   a command name to `isname()` in this story — that changes *which commands are
   parsed*, and this story changes *how one path is categorised*.
3. **`git` as a command name.** MT-033 C-4 and PO-2 measured why: `git` is not
   write-capable, and naming it makes `git` start a command context and puts
   every `git commit -m` in the repository back where MT-031 found it. `git mv` is
   already judged **through** `mv`.
4. **`phase.sh`'s detached-HEAD branch comparison.** MT-032's, out of scope there
   and here.
5. **MT-033's `docs/backlog/` workaround in `.claude/tests/phase-guard.test.sh`**
   (~fifteen call sites and a thirty-line comment near line 1153). It becomes
   unnecessary when this lands, and **reverting it is not this story's job**: it
   would touch fifteen assertions that currently pass, for no behaviour change,
   in a story whose own assertions are what must be watched. File it afterwards if
   anyone wants it. The comment should be left in place — it is the record of why.
6. **Removing `paths.conf`'s vendor twin-rule block.** C-3 makes those thirteen
   lines redundant. Redundant is not wrong, `paths.conf` is out of this story's
   range entirely (C-7), and deleting rules that `classify_stdin` reads is exactly
   the AC-6 surface this design was chosen to avoid.
7. **Making `classify` consult the filesystem.** C-5 decides this: it stays
   blind. A fix that stats is an escalation, not an implementation choice.
8. **Changing what any phase permits.** `phases.conf` is untouched. If the right
   answer to "`tests/` should be writable in GREEN" is yes, that is a change to
   the phase table with its own story and its own argument — not a reason to leave
   `tests` classifying `source`.
9. **`normalize_rel`, `to_rel`, `write_candidates`, `mvops()`, the denial format,
   the candidate filter, `$EXEMPT`.** C-3 and C-4 say the fix is confined to
   `classify`. All of these are MT-031's and MT-033's, they are heavily asserted,
   and a change to any of them here means the fix has drifted.
10. **Any production source, `project.conf`, any gate command, `paths.conf`,
    `phases.conf`.** `git diff --name-only` at the end of this story should name
    `.claude/hooks/lib.sh`, two files under `.claude/tests/`, and this story file.
    Nothing else.

## Test plan

Written in RED, 2026-09-14. **46 new assertions across two existing suites.** No
new file: both suites already exist, both already drive the real code, and a
third file would only make the failure harder to locate.

### Level, and why each criterion sits where it does

| Criterion | Level | File | Why |
|---|---|---|---|
| AC-1, AC-2, AC-3, AC-3b | end-to-end, the real hook | `.claude/tests/phase-guard.test.sh` | Each is stated as *a command, in a phase, permitted or refused*. The contract that can falsify it is the whole guard — parse, role, `to_rel`/`normalize_rel`, `classify`, `phase_allows` — and AC-2 in particular is about which of two operands the denial lands on, which does not exist below the guard. |
| AC-4, AC-5 | unit | `.claude/tests/lib.test.sh` | Both are pure statements about `classify`'s return value. Driving them through the hook would cost ~2 s each and prove less. |
| AC-6 | unit (surface), plus a GATES diff | `.claude/tests/lib.test.sh` + DV-2 | The diff needs two sides and RED has one. What RED *can* pin is the surface: that `classify_stdin` — the function `gate_tree_hash`, `check-boundaries.sh:97` and `gates.sh:621` all read — is untouched. |
| C-6 | end-to-end | `.claude/tests/phase-guard.test.sh` | The two rows not already asserted by MT-031/MT-033. |

`.claude/tests/lib.test.sh` also gained one probe-driven block that is not an
acceptance criterion: **C-3's ordering pin** (rules → slashed rules →
`git check-ignore` → `source`). It is the one clause of C-3 whose violation is
silent, because the categories it decides between are both plausible.

### Assertion inventory

`.claude/tests/lib.test.sh` — **25 new assertions, 14 red on arrival**

| Assertions | Asserts | Covers | On arrival |
|---|---|---|---|
| `AC-4: classify {src, src/, src/mangatl, src/mangatl/ui, packaging, src/brand-new.ts}` (6) | each is `source`, before and after the change | AC-4 | **green** — earned, P-1 |
| `AC-1/AC-3: classify {docs, tests, scripts, .claude, .github, src/tests, src/test, src/__tests__, src/spec}` (9) | the category C-3's measured table gives each bare directory | AC-1, AC-3 | **RED** |
| `the two spellings of {docs, tests, scripts, .claude} agree` (4) | `classify "$d/"` equals `classify "$d"` | AC-1, `## Context` direction 3 | **RED** |
| `AC-5: classify fixtures, which does not exist` (1) | `test` for a directory absent from disk | AC-5 | **RED** |
| `DV-4: creating the directory does not change the answer` (1) | the answer before `mkdir fixtures` equals the answer after | AC-5 control, DV-4 | **green** — earned, P-3 |
| `a vendor directory the project also gitignores` (1) | `dist` is `vendor`, not `ignored` | C-3 ordering | **green** — earned, P-4 |
| `a nested {docs, tests} directory` (2) | `docs/backlog`, `tests/core` unmoved | C-3 "the parent glob already supplies the slash" | **green** — earned, P-5 |
| `classify_stdin still gives a bare directory name the source default` (1) | six bare directory names, all `source`, straight out of `classify_stdin` | AC-6 surface, C-7, C-3's rejected option | **green** — earned, P-2 |

`.claude/tests/phase-guard.test.sh` — **21 new assertions, 12 red on arrival**

| Assertions | Asserts | Covers | On arrival |
|---|---|---|---|
| `allows: {cp, mv} into docs/ during REVIEW`, `rm -rf docs/`, `touch docs/` (4) | the four commands AC-1 enumerates are permitted | AC-1 | **RED** |
| `allows: cp into tests/ during RED`, `rm -rf docs/ during RED` (2) | AC-1's shape in the other phase that has a writable directory the guard refuses | AC-1 | **RED** |
| `allows: the absolute spelling of the same permitted write` (1) | `cp docs/notes.md $FIX/docs/` stays permitted, so the two spellings converge | AC-1, direction 3 | **green** — earned by its pair |
| `blocks: cp into src/`, `cp one level inside src/` — REVIEW (2) | still refused, path `src`/`src/sub`, category `source` | AC-1 control | **green** — earned, P-6 |
| `blocks: mv of a frozen file into docs/ is refused on the file, not on docs` (1) | path `src/main.ts`, category `source` | AC-2 | **RED** |
| `role: … calls it the source of the mv` (1) | `operand:  source of mv (removed by the move)`, verbatim, after `path:` | AC-2 (mechanical) | **RED** |
| `blocks: {cp, mv, touch, rm -rf} … tests/ during GREEN` (4) | path `tests`, category `test` | AC-3, AC-3b | **RED** |
| `blocks: cp one level inside the frozen test tree`, `rm of a named frozen test` (2) | still refused | AC-3 control, out of scope 8 | **green** — earned, P-7 |
| `blocks: rm -rf src/`, `touch src/` during RED (2) | AC-4's trap at the guard: the restrictive default still applies | AC-4 | **green** — earned, P-6 |
| `allows: mv into docs/sub/ (REVIEW)`, `cp into docs/backlog/ (RED)` (2) | already correct, must not start refusing | C-6 | **green** |

### One new helper, and why a bare `assert_blocked` was not enough

`assert_blocked_as <fixture> <command> <path> <category> [label]` in
`.claude/tests/phase-guard.test.sh`. AC-1, AC-3 and AC-3b each name a **category**
as well as a path, and `_lib.sh`'s `assert_blocked` reads only the `path:` line.
Without the category half, *"block `tests/` for any reason"* satisfies AC-3 —
including a fix that classified it `vendor`, or one that left it `source` and
changed what GREEN permits, which is `## Out of scope` item 8. It is one
assertion per command, deliberately, so AC-4's predicted *"4 red on AC-3"* is a
number somebody can check.

### Counts, as a fact rather than a promise

```
             before        after
lib          123 passed    134 passed, 14 failed
phase-guard  268 passed    277 passed, 12 failed
```

### Edges covered, and edges deliberately not

- **Zero / one / many segments** — `docs` (one), `docs/backlog` (two, already
  matched), `docs/wiki/design` via the existing suite; `classify ""` → `outside`
  already asserted at `lib.test.sh:55` and untouched.
- **Boundary between "is a category directory" and "is not"** — `tests` vs `src`,
  and `src/tests` vs `src`, which is the pair that makes the rule *rule-driven*
  rather than *positional*.
- **Trailing slash both ways** — `docs` vs `docs/`, and `src` vs `src/`, so that
  "do not append a slash to a path that already ends in one" cannot be satisfied
  by accident in one direction only.
- **`.gitignore` precedence** — `dist` (rule and gitignore agree that it is not
  source; the rule must win) against `.vitest` and `playwright-report` (no rule
  either way; `is_ignored` must win). One without the other proves nothing.
- **Out of scope, pinned where it was cheap** — item 8 (`phases.conf`) via the
  two AC-3 controls and P-7; the rejected `paths.conf` design via the AC-6
  surface assertion and P-2; item 1 (`cp -t`) is already pinned twice by MT-033
  and is untouched.
- **Not covered, and stated so:** the retry's *suffix character*. Measured in
  RED, appending `/`, `/x` or `/**` gives an identical category for every row of
  C-3's table, because every directory glob ends `/**` and `.*` matches the empty
  string. No assertion in this suite can distinguish them, and inventing one
  would pin an implementation detail no criterion asks for. `/` is what C-3 says;
  GREEN should write `/`.

## Handoff: RED -> GREEN

Written at the end of RED, 2026-09-14. You have not read anything above; this
block is meant to be enough.

### 1. The exact commands

```bash
bash scripts/selftest.sh lib           # fast, ~5 s
bash scripts/selftest.sh phase-guard   # SLOW: 8-10 min on this machine
bash scripts/selftest.sh               # all 12 suites, ~9 min
bash scripts/gates.sh --fast           # the gates; see §8 - they do NOT judge this story
```

`bash scripts/selftest.sh phase-guard` ran for **roughly nine minutes** on this
machine (Windows 11, Git Bash), observed wall-clock across four full runs in
RED, not stopwatch-timed. It is not hanging: every one of its 289 assertions
forks the real hook, which forks `git check-ignore`. Budget for it and do not
kill it.

### 2. Counts, before and after

```
             before (main, 35afba6)   after RED
lib           123 passed, 0 failed    134 passed, 14 failed
phase-guard   268 passed, 0 failed    277 passed, 12 failed
```

**26 red, 20 green on arrival.** The 20 are listed in §9 with the probe that
earns each; none of them is a forgotten test.

The full run confirms there is no collateral — **only the two suites this story
touches moved**:

```
$ bash scripts/selftest.sh
boundaries:  26 passed, 0 failed      mutate:       39 passed, 0 failed
ci-local:     8 passed, 0 failed      new-story:    22 passed, 0 failed
doctor:       7 passed, 0 failed      phase-guard: 277 passed, 12 failed
gate-reminder: 32 passed, 0 failed    phase:        24 passed, 0 failed
gates:      108 passed, 0 failed      profiles:     44 passed, 0 failed
lib:        134 passed, 14 failed     settings:     20 passed, 0 failed

2 of 12 harness suite(s) FAILED.
```

All 26 failures are assertions MT-034 added. GREEN is finished when this reads
`0 of 12`, with `lib: 148 passed` and `phase-guard: 289 passed`.

### 3. The verbatim failure output

`bash scripts/selftest.sh lib`:

```
  MT-034 AC-4: a bare directory that is NOT a category directory stays source

  MT-034 AC-1/AC-3 and C-3: a bare directory name takes its directory's category
    FAIL AC-1/AC-3: classify docs
         expected: docs
         actual:   source
    FAIL AC-1/AC-3: classify tests
         expected: test
         actual:   source
    FAIL AC-1/AC-3: classify scripts
         expected: harness
         actual:   source
    FAIL AC-1/AC-3: classify .claude
         expected: harness
         actual:   source
    FAIL AC-1/AC-3: classify .github
         expected: harness
         actual:   source
    FAIL AC-1/AC-3: classify src/tests
         expected: test
         actual:   source
    FAIL AC-1/AC-3: classify src/test
         expected: test
         actual:   source
    FAIL AC-1/AC-3: classify src/__tests__
         expected: test
         actual:   source
    FAIL AC-1/AC-3: classify src/spec
         expected: test
         actual:   source
    FAIL the two spellings of docs agree
         expected: docs
         actual:   source
    FAIL the two spellings of tests agree
         expected: test
         actual:   source
    FAIL the two spellings of scripts agree
         expected: harness
         actual:   source
    FAIL the two spellings of .claude agree
         expected: harness
         actual:   source

  MT-034 AC-5: a directory that does not exist on disk is still classifiable
    FAIL AC-5: classify fixtures, which does not exist
         expected: test
         actual:   source

  MT-034 C-3 ordering: a rule still beats .gitignore, and nested paths do not move

  MT-034 AC-6 and C-7: the fix lands in classify, not in classify_stdin

lib: 134 passed, 14 failed
```

`bash scripts/selftest.sh phase-guard` (the denial text is flattened by
`guard()` in `_lib.sh`, which turns newlines into spaces — that is why the
message runs on). The **first** failure is reproduced in full; the rest are
elided at `...` only across the identical `phase_message` boilerplate every
denial carries, because it is the same 240 characters nine times:

```
  MT-034 AC-1: a directory-shaped path in a category the phase PERMITS
    FAIL allows: cp into docs/ during REVIEW, where docs is writable
         blocked with: BLOCKED by the harness phase lock.    story:    T-1   phase:    REVIEW   path:     docs   operand:  destination of cp   category: source  Story is in REVIEW awaiting PR feedback. Only docs and backlog may change. Return to GREEN or GATES to make code changes.  If this write is genuinely correct, change the phase deliberately rather than working around the lock:  bash scripts/phase.sh set T-1 <PHASE>
    FAIL allows: mv into docs/ during REVIEW
         blocked with: ... path:     docs   operand:  destination of mv   category: source ...
    FAIL allows: rm -rf docs/ during REVIEW
         blocked with: ... path:     docs   category: source ...
    FAIL allows: touch docs/ during REVIEW
         blocked with: ... path:     docs   category: source ...
    FAIL allows: cp into tests/ during RED, where test is writable
         blocked with: ... phase:    RED   path:     tests   operand:  destination of cp   category: source ...
    FAIL allows: rm -rf docs/ during RED, where docs is writable
         blocked with: ... phase:    RED   path:     docs   category: source ...

  MT-034 AC-2: mv into a permitted directory is judged on the operand it REMOVES
    FAIL blocks: mv of a frozen file into docs/ is refused on the file, not on docs
         blocked, but on the wrong path (wanted 'src/main.ts'): BLOCKED by the harness phase lock.    story:    T-1   phase:    RED   path:     docs   operand:  destination of mv   category: source ...
    FAIL role: and the denial calls it the source of the mv, not a destination
         the denial carries no 'operand:  source of mv (removed by the move)' line: ... path:     docs   operand:  destination of mv   category: source ...

  MT-034 AC-3 and AC-3b: GREEN freezes the test tree however the path is spelled
    FAIL blocks: cp into the frozen test tree during GREEN
         not blocked at all
    FAIL blocks: mv into the frozen test tree during GREEN
         not blocked at all
    FAIL blocks: touch of the frozen test tree during GREEN
         not blocked at all
    FAIL blocks: AC-3b: rm -rf of the whole frozen test tree during GREEN
         not blocked at all

  MT-034 C-6: what the directory rule must not start refusing

phase-guard: 277 passed, 12 failed
```

**Why this is the right failure.** Not one of the 26 is an import error, a
syntax error, a fixture problem or a timeout. Every red line is the assertion
itself, reporting the real denial text or the real category: `expected: docs /
actual: source` is `classify` taking the `source` default for a path that names
a directory, and `not blocked at all` is the guard permitting a write into the
frozen test tree during GREEN. The two suites are 100 % green on `main` at
`35afba6`; the only thing that moved is the assertions this story added.

### 4. What every test asserts, and which AC it covers

See `## Test plan` for the full inventory with per-assertion detail. In one
sentence each:

| Assertions | Asserts | AC |
|---|---|---|
| 9 × `AC-1/AC-3: classify <dir>` | a bare directory name takes its directory's category, read out of C-3's table | AC-1, AC-3 |
| 4 × `the two spellings of <d> agree` | `classify "$d/"` equals `classify "$d"` | AC-1, direction 3 |
| 6 × `AC-4: classify <p>` | `src`, `src/`, `src/mangatl`, `src/mangatl/ui`, `packaging`, `src/brand-new.ts` stay `source` | AC-4 |
| `AC-5: classify fixtures` + `DV-4` | `test` for a directory absent from disk, and the same answer once it exists | AC-5 |
| `classify_stdin still gives a bare directory name the source default` | the fix is **not** in `classify_stdin` or `paths.conf` | AC-6 surface |
| `dist` / `docs/backlog` / `tests/core` | C-3's ordering and its "already matched, do not retry" rows | C-3 |
| 4 × `allows: … docs/ …` (REVIEW) + 2 × (RED) | the enumerated AC-1 commands are permitted | AC-1 |
| 2 × `blocks: … src/ …` (REVIEW) | AC-1's control: still refused | AC-1 |
| `blocks: mv … docs/` + `role: …` | path `src/main.ts`, `operand:  source of mv (removed by the move)` | AC-2 |
| 4 × `blocks: … tests/ … GREEN` | path `tests`, category `test` | AC-3, AC-3b |
| 2 × `blocks: … tests/sub/`, `tests/main.test.ts` | AC-3's control: still refused | AC-3 |
| 2 × `blocks: rm -rf src/`, `touch src/` (RED) | AC-4's trap at the guard | AC-4 |
| 2 × `allows: … docs/sub/`, `docs/backlog/` | must not start refusing more | C-6 |

### 5. Files touched

```
.claude/tests/lib.test.sh           +25 assertions  (inserted after line 69)
.claude/tests/phase-guard.test.sh   +21 assertions, +1 helper (inserted before `summary`)
docs/backlog/stories/MT-034.md      ## Test plan, ## Handoff, ## Regressions,
                                    ## Deferred verifications (DV-1..DV-4), ## Model guidance
```

Nothing else. `git diff --name-only` names exactly those three. **`.claude/hooks/lib.sh`, `.claude/hooks/phase-guard.sh` and `.claude/harness/paths.conf` are byte-identical to `main`** — every mutation in `## Regressions` went through `bash scripts/mutate.sh`, which restored each file and verified the restore with `cmp`; `git status --porcelain` shows none of them.

### 6. The export shape the tests already pin

**Nothing below is a suggestion.** Each name and signature is already called by
an assertion, so getting it wrong is a broken suite rather than a debate.

```
.claude/hooks/lib.sh

  classify <relpath>
      unchanged name, ONE positional argument, prints the category on stdout
      with no trailing newline, and nothing on stderr.
      Return set, exactly as today:
        outside | vendor | harness | docs | test | config | ignored | source
      `classify ""` still prints `outside` (lib.test.sh:55, untouched).

  classify_stdin
      unchanged name, unchanged behaviour, unchanged OUTPUT.
      Reads one repo-relative path per line, prints "<category>\t<path>" per
      line, defaults unmatched paths to `source`, and does NOT consult git.
      An assertion now pins that a bare directory name still comes out
      `source` here. Putting the retry in this function, or adding twin globs
      to paths.conf, turns that assertion red - deliberately; it is the AC-6
      surface and C-3's rejected option.

  is_ignored <relpath>
      unchanged. It must still run AFTER both rule lookups: an explicit rule
      beats .gitignore (asserted with `dist`).
```

What the tests do **not** constrain, and is yours to choose:

- the mechanism of the retry. C-4 *suggests* feeding `rel` and `rel/` to one
  `classify_stdin` invocation and taking the first non-default line; a lazy
  second call on the miss path is explicitly acceptable too. No assertion can
  tell them apart, and I did not write one that pretends to.
- the retry's **suffix character**. Measured in RED against the shipped
  `paths.conf`: appending `/`, `/x` or `/**` yields an identical category for
  every row of C-3's table, because every directory glob ends `/**` and `.*`
  matches the empty string. C-3 says `/`; write `/`, but know that no test can
  falsify the choice.
- the order of the two lookups *within* one `awk` process, local variable names,
  and whether the guard against re-slashing an already-slashed path is a `case`
  or a parameter expansion.

### 7. C-7's caller list, re-checked against the tree — TWO CORRECTIONS

C-7 says RED's handoff must state this was re-checked. It was, at `35afba6`, with
`grep -rn --include=*.sh`. The list is accurate in the two places that matter and
**wrong in one number**:

| C-7 says | Measured in RED | Verdict |
|---|---|---|
| `phase-guard.sh:42` is the **only** production caller of `classify` | `./.claude/hooks/phase-guard.sh:42:  cat="$(classify "$rel")"` — the only non-test match for the word `classify` outside `lib.sh` | **confirmed** |
| `classify_stdin`'s callers are `classify`, `code_changed_since`, `gate_tree_hash`, `check-boundaries.sh:97`, `gates.sh:621` | `lib.sh:707` (classify), `lib.sh:857` (code_changed_since), `lib.sh:903` (_hash_blob_listing → gate_tree_hash), `check-boundaries.sh:97`, `gates.sh:621` | **confirmed, exactly** |
| `lib.test.sh:52,55,61-69` — "eight direct `classify` assertions" | seven `assert_eq` **statements** at 52, 55, 61, 62, 63, 66, 69; line 52 is a loop over a 31-case table, so it is 37 assertions at runtime | **off by one, harmless** — the line numbers are still right, my insert went in after 69 |
| "**Nineteen** files source this library" | **Nine** by RED's count; **twelve** by the orchestrator's, which counted both spellings — 4 hooks + 3 scripts + 5 test suites. RED undercounted the suites (found 2, there are 5). | **wrong number, right point** — a syntax error in `lib.sh` takes all twelve down, and `bash scripts/selftest.sh` is what notices. C-7 corrected to twelve. |

Neither correction changes the design or any criterion. Recorded because C-7 asks
RED to state the recheck, and a recheck that repeats a number without looking is
not one.

### 8. The gates, and why `--fast` being green is the correct shape here

```
$ bash scripts/gates.sh --fast
PASS         format (2s, observed 17)
PASS         lint (1s, observed 5, floor 5)
PASS         typecheck (1s, observed 14, floor 14)
PASS         unit (1s, observed 25, floor 25)
PASS         coverage (1s, observed 25, floor 25)
PASS         coverage-core (1s, observed 20, floor 20)
All required gates passed (6 ran, 0 unconfigured, 0 known).
```

Normally a green test gate in RED means RED is not finished. **Not here, and the
story says why.** `grep -n "^covers" .claude/harness/project.conf` names
`src/mangatl/**` and `packaging/**` and nothing under `.claude/`, so no gate
reads a file this story touches; `required_gates: []` is a decision. The verifier
for MT-034 is `bash scripts/selftest.sh`, which CI runs as the **required** step
*"Harness self-test"* ahead of the gates — and that is red, with 26 failures, as
§3 shows. What `--fast` establishes is the thing it is for: the new test files
are **admissible** — nothing times out, no lint or format gate trips on them, and
the shape of the failure is entirely in the selftest.

### 9. Negative controls: the numbers, and what GREEN owes

**This story is not the usual case, and the difference matters.** The usual RED
warning is that the suite fails at *import*, so no assertion in the file ever
runs and every control is an unverified claim. That did not happen here: bash
suites have no import to fail, both suites executed **every** assertion, and each
control below was measured by a real run rather than reasoned about. The numbers
are observations, not predictions.

| Control | What it must show | Expected | Measured in RED | Where |
|---|---|---|---|---|
| AC-1 control — `cp docs/notes.md src/` in REVIEW | stays BLOCKED, path `src`, category `source` | blocked | blocked, `src`, `source` | green on arrival; red under P-6 |
| AC-1 control — `cp docs/notes.md src/sub/` in REVIEW | stays BLOCKED, `src/sub`, `source` | blocked | blocked, `src/sub`, `source` | green on arrival; red under P-6 |
| AC-3 control — `cp docs/notes.md tests/sub/` in GREEN | stays BLOCKED, `tests/sub`, `test` | blocked | blocked, `tests/sub`, `test` | green on arrival; red under P-7 |
| AC-3 control — `rm -rf tests/main.test.ts` in GREEN | stays BLOCKED, `tests/main.test.ts`, `test` | blocked | blocked, `tests/main.test.ts`, `test` | green on arrival; red under P-7 |
| AC-4 trap — `classify src` | stays `source` | `source` | `source` | green on arrival; red under P-1 |
| AC-4 trap at the guard — `rm -rf src/`, `touch src/` in RED | stay BLOCKED, `src`, `source` | blocked | blocked, `src`, `source` | green on arrival; red under P-6 |
| AC-5 control (DV-4) — `classify fixtures` either side of `mkdir` | the two answers identical | identical | `source` == `source` | green on arrival; red under P-3 (`source` vs `ignored`) |
| C-3 ordering — `classify dist` | `vendor`, not `ignored` | `vendor` | `vendor` | green on arrival; red under P-4 |
| AC-6 surface — `classify_stdin` on six bare directory names | all `source` | all `source` | all `source` | green on arrival; red under P-2 (`docs docs`) |
| Direction 3 — `cp docs/notes.md $FIX/docs/` in REVIEW | stays ALLOWED | allowed | allowed | green on arrival; earned by its red partner |

**What GREEN owes on these, and it is one comparison each.** Re-read the same
table against the shipped fix and confirm the *values*, not merely that the
tests pass. Five rows change value by design and must land exactly here:

| Row | RED measured | GREEN must measure |
|---|---|---|
| DV-4 `classify fixtures` before/after `mkdir` | `source` == `source` | **`test` == `test`**, still identical |
| `classify docs` | `source` | `docs` |
| `classify tests` | `source` | `test` |
| `classify scripts` | `source` | `harness` |
| `classify fixtures` | `source` | `test` |

Every other row above must be **byte-identical** to its RED value. A row that
moved is the fix having reached further than C-3 licenses.

### 10. Mutation table for GATES

Eight probes were run in RED and are pasted in `## Regressions`. These are the
ones worth re-running against the **finished** fix, with predicted counts. The
orchestrator runs one and compares; **P-8 is the one to pick** — its predicted
catch is a *single* assertion.

| # | Mutation (all via `bash scripts/mutate.sh`) | Predicted catch, post-fix | Status |
|---|---|---|---|
| **P-8** | `.claude/harness/paths.conf`, `s#^config \| packaging/\*\.spec$#config \| packaging/**#` — give `packaging` a `/**` glob, so the retry finds one | **exactly 1**: `AC-4: classify packaging` (`expected: source / actual: config`). No other assertion in either suite mentions `packaging`; grepped. | measured pre-fix as **inert** (14 red, unchanged, `packaging` not among them) — which is the point: it acts only *through* the retry |
| **DV-1** | the story's own control: strip the retry and default an unmatched path to a category RED permits | 6 red in AC-4 + 4 red in AC-3, 0 movement in AC-1 | **GATES owes this**; declined in RED, see `## Deferred verifications` |
| P-1 | `.claude/hooks/lib.sh`, `s/c = "source"/c = "docs"/` | blunt: everything that leans on the `source` default. In RED it gave 27 red in `lib` (6 of them exactly the AC-4 block) and 139 red in `phase-guard` | measured, RED |
| P-2 | `.claude/harness/paths.conf`, add the twin rule `docs \| docs` | the AC-6 surface assertion, red; and *two of AC-1's assertions go green* — which is exactly why C-3 rejected the design and why AC-6 exists | measured, RED |
| P-9 | break the retry only — make it append the empty string | AC-1's 6, AC-2's 2, AC-3's 4, AC-5's 1, the 4 spelling-agreement assertions: **17 red**, AC-4 untouched | predicted; RED cannot run it, there is no retry yet |

A note on reading P-1's 139: it is blunt *by design* — it removes the default the
whole classifier rests on. Its informative part is not the total but the six, and
the six are the AC-4 block exactly.

### 11. What GREEN must not do

1. **Do not touch `.claude/harness/paths.conf`.** C-3 rejected the twin-rule
   design on AC-6 grounds and P-2 shows the assertion that catches it. Out of
   scope items 6 and 10.
2. **Do not touch `.claude/harness/phases.conf`.** Making GREEN permit `test`
   turns AC-3's four assertions green and takes AC-3's two controls red — P-7
   measured exactly that. Out of scope item 8.
3. **Do not make `classify` consult the filesystem.** C-5 decides it; P-3 shows
   DV-4 going red under a `[ -d ]`. If you believe a stat is necessary, that is
   an escalation to the Lead PO, not an implementation choice. Out of scope 7.
4. **Do not change `normalize_rel`, `to_rel`, `write_candidates`, `mvops()`, the
   denial format or `$EXEMPT`.** C-3 and C-4 confine the fix to `classify`.
5. **`phase-guard.sh` should not need to change.** If you find it must, C-4 says
   stop and say so rather than widening quietly.
6. **Do not edit either test file.** They are frozen in GREEN. If one is wrong,
   the story goes back to RED and the reason goes in `## Regressions`. In
   particular, the new `assert_blocked_as` helper asserts the **category** as
   well as the path, on purpose: without it, "block `tests/` for any reason"
   satisfies AC-3.
7. **Do not retry a path that already ends in `/`, and do not retry one that
   already matched.** C-4's slip guard. `classify "src/"` → `source` and
   `classify "docs/backlog"` → `docs` are the assertions that would notice.

### 12. Things discovered in RED that should change the approach

- **A third false positive, not in the story, and it bites this very phase.**
  `cp docs/notes.md tests/` is **BLOCKED in RED** today — on `tests`, category
  `source`. RED permits `test`, so the phase whose entire job is writing tests
  refuses a copy into the test tree. Same single cause; no extra mechanism; two
  assertions added for it under AC-1 (`cp … tests/` and `rm -rf docs/`, both in
  RED). Worth knowing because it means the fix is not only about REVIEW.
- **C-3's measured table reproduces exactly**, re-run in RED against the shipped
  `paths.conf` both in this checkout and in the `_lib.sh` fixture, all 27 rows,
  today-column and under-C-3-column. I did not re-derive it; I checked it, as
  C-8 asks, and it is right. One fixture-only caveat: `spikes` is `ignored` in
  this repository and `source` in the fixture, because the fixture's `.gitignore`
  is a four-line stub. No assertion uses `spikes`.
- **The retry's suffix is unobservable** (see §6). Do not be surprised that no
  test fails if you write `/x`; write `/`.
- **`mv src/main.ts docs/` depends on candidate ordering to land on the right
  operand.** `phase-guard.sh` `sort -u`s the candidates, so `docs/…` is judged
  before `src/main.ts`. Today `docs` denies first, which is the whole of AC-2's
  defect. Once `docs` classifies `docs` the first candidate is permitted and the
  denial falls through to `src/main.ts` with its existing `R_MV_SRC` role. **No
  change to `mvops()` or to the role strings is needed** — AC-2 is bought
  entirely by the classification. If you find yourself editing roles, stop.
- **`git ls-files | classify_stdin` moves zero lines under C-3**, measured — see
  DV-2. The fifteen `source` paths are `.mcp.json.example` and fourteen
  `src/mangatl/**` `.py` files; none is spelled like a bare directory.
- **Timings, and where each came from.** Every number in this handoff is from a
  **local** run on this machine (Windows 11, Git Bash); **none is from CI**, and
  nothing here was measured on runner hardware. That is acceptable for this story
  and it is worth saying why: these are bash suites with no per-test timeout and
  no framework to impose one, so there is no budget to blow — the only clock that
  matters is the hook's **15 s `PreToolUse`** budget, and **a hook that times out
  fails OPEN, which is the lock silently off.** C-4 measured one `classify` call
  at 50–175 ms at PLANNED, dominated by process spawn. **RED did not re-measure
  that, and no assertion in either suite would catch a regression in it.** If
  GREEN's retry adds a fork on the common path, nothing goes red and the failure
  mode is a silently disabled lock on a slow machine — so keep the retry inside
  the existing `classify_stdin` call, or pay for it only on the miss path, which
  already spends two `git check-ignore` forks. The nine-minute `phase-guard`
  runtime in §1 is a property of the *suite* forking the hook 289 times, not of
  the hook.

## Regressions

**This is not a return to RED.** MT-034 arrived at RED from PLANNED and 26 of its
46 assertions were watched to fail for the first time in §3 of the handoff. The
eight probes below exist for the *other* twenty: the assertions that **passed on
arrival**, because they pin behaviour that is already correct and must survive the
fix. `rules.md` says such an assertion has never been observed to fail and is not
yet a test; each one below is earned by a deliberate, reverted mutation of the
specific production behaviour it claims to pin, with the red pasted.

Every mutation went through `bash scripts/mutate.sh`, which backs the file up
under `.claude/state/mutations/`, applies the expression, runs the command,
restores the file and **verifies the restore with `cmp`**. No `sed -i` was used
on any file in this story. After all eight, `git status --porcelain` names only
`.claude/tests/lib.test.sh`, `.claude/tests/phase-guard.test.sh` and this story
file — `lib.sh`, `phase-guard.sh`, `paths.conf` and `phases.conf` are untouched.

Baseline for every count below: **`lib` 134 passed / 14 failed**, **`phase-guard`
277 passed / 12 failed**.

---

### P-1 — AC-4's six assertions, and DV-1's mirror image

DV-1 cannot run in RED: it mutates the *finished* fix. What RED can run is the
mirror — mutate the shipped `classify` so the `source` default becomes a category
RED permits, and confirm AC-4 goes red against the **unfixed** tree.

```
$ bash scripts/mutate.sh .claude/hooks/lib.sh 's/c = "source"/c = "docs"/' \
    -- bash scripts/selftest.sh lib

=== mutate: .claude/hooks/lib.sh (1 line(s) changed by s/c = "source"/c = "docs"/) ===
  796 -       lp = tolower(path); c = "source"
  796 +       lp = tolower(path); c = "docs"

  MT-034 AC-4: a bare directory that is NOT a category directory stays source
    FAIL AC-4: classify src
         expected: source
         actual:   docs
    FAIL AC-4: classify src/
         expected: source
         actual:   docs
    FAIL AC-4: classify src/mangatl
         expected: source
         actual:   docs
    FAIL AC-4: classify src/mangatl/ui
         expected: source
         actual:   docs
    FAIL AC-4: classify packaging
         expected: source
         actual:   docs
    FAIL AC-4: classify src/brand-new.ts
         expected: source
         actual:   docs

lib: 121 passed, 27 failed

=== mutate: command exited 1; restored (verified byte-for-byte against
    .claude/state/mutations/.claude_hooks_lib.sh.20260915T023355Z.192037.bak) ===
  796:       lp = tolower(path); c = "source"
```

**Exactly six red in the AC-4 block**, which is the number AC-4's control and
DV-1 both predict. 27 red overall — 14 baseline plus 13 collateral, because the
mutation removes the default the whole classifier rests on. The informative
number is the six.

### P-6 — the same mutation, at the guard, for AC-4's four end-to-end controls

```
$ bash scripts/mutate.sh .claude/hooks/lib.sh 's/c = "source"/c = "docs"/' \
    -- bash scripts/selftest.sh phase-guard

  796 -       lp = tolower(path); c = "source"
  796 +       lp = tolower(path); c = "docs"

    FAIL blocks: cp into src/ stays refused in REVIEW
         not blocked at all
    FAIL blocks: cp one level inside src/ stays refused in REVIEW
         not blocked at all
    FAIL blocks: rm -rf of the whole frozen source tree during RED stays refused
         not blocked at all
    FAIL blocks: touch of the frozen source tree during RED stays refused
         not blocked at all

phase-guard: 150 passed, 139 failed

=== mutate: command exited 1; restored (verified byte-for-byte against
    .claude/state/mutations/.claude_hooks_lib.sh.20260915T023722Z.13139.bak) ===
  796:       lp = tolower(path); c = "source"
```

All four `src` controls go red with `not blocked at all` — the exact failure they
exist to catch. 139 red overall; blunt, for the same reason as P-1.

### P-2 — the AC-6 surface assertion, against C-3's *rejected* design

The rejected option was "add bare-directory twin globs to `paths.conf`". This
applies it for `docs` and runs the suite.

```
$ bash scripts/mutate.sh .claude/harness/paths.conf \
    's#^docs | docs/\*\*$#docs | docs/**\ndocs | docs#' -- bash scripts/selftest.sh lib

=== mutate: .claude/harness/paths.conf (81 line(s) changed by ...) ===
  82 - docs | README.md
  82 + docs | docs
  ...

  MT-034 AC-6 and C-7: the fix lands in classify, not in classify_stdin
    FAIL classify_stdin still gives a bare directory name the source default
         expected: source docs
         source tests
         source scripts
         source fixtures
         source .claude
         source .github
         actual:   docs docs
         source tests
         source scripts
         source fixtures
         source .claude
         source .github

lib: 13 failed

=== mutate: command exited 1; restored (verified byte-for-byte against
    .claude/state/mutations/.claude_harness_paths.conf.20260915T023652Z.*.bak) ===
```

Read the count: **13, not 14.** The twin rule turns two of AC-1's currently-red
assertions green (`classify docs`, `the two spellings of docs agree`) and takes
the AC-6 surface assertion red. That is the trade C-3 rejected, demonstrated
rather than argued: the cheap fix buys AC-1 and pays for it in the one function
whose output a CI check compares hashes of.

### P-3 — DV-4's identity control, against a filesystem-consulting `classify`

C-5 decides that `classify` stays blind. This makes it stat.

```
$ bash scripts/mutate.sh .claude/hooks/lib.sh \
    's|is_ignored "$rel" && cat=ignored|[ -d "$HARNESS_ROOT/$rel" ] \&\& cat=ignored|' \
    -- bash scripts/selftest.sh lib

  709 -   [ "$cat" = "source" ] && is_ignored "$rel" && cat=ignored
  709 +   [ "$cat" = "source" ] && [ -d "$HARNESS_ROOT/$rel" ] && cat=ignored

    FAIL DV-4: creating the directory does not change the answer
         expected: source
         actual:   ignored

lib: 20 failed

=== mutate: command exited 1; restored (verified byte-for-byte against
    .claude/state/mutations/.claude_hooks_lib.sh.20260915T023539Z.2640.bak) ===
  709:   [ "$cat" = "source" ] && is_ignored "$rel" && cat=ignored
```

The answer before `mkdir fixtures` and the answer after stop matching the moment
the classifier looks at the disk. That is DV-4 earned, and it is the assertion
that would catch out-of-scope item 7.

### P-4 — C-3's ordering: an explicit rule beats `.gitignore`

Drops the `[ "$cat" = "source" ]` guard, so `is_ignored` runs ahead of the rules.

```
$ bash scripts/mutate.sh .claude/hooks/lib.sh \
    's|\[ "$cat" = "source" \] && is_ignored|is_ignored|' -- bash scripts/selftest.sh lib

  709 -   [ "$cat" = "source" ] && is_ignored "$rel" && cat=ignored
  709 +   is_ignored "$rel" && cat=ignored

  classify: paths.conf rules
    FAIL classify node_modules/left-pad/index.js
         expected: vendor
         actual:   ignored
    FAIL classify node_modules
         expected: vendor
         actual:   ignored
    FAIL classify dist/bundle.js
         expected: vendor
         actual:   ignored

  MT-034 C-3 ordering: a rule still beats .gitignore, and nested paths do not move
    FAIL a vendor directory the project also gitignores
         expected: vendor
         actual:   ignored

lib: 18 failed

=== mutate: command exited 1; restored (verified byte-for-byte against
    .claude/state/mutations/.claude_hooks_lib.sh.20260915T023607Z.5653.bak) ===
```

`dist` is the one path that is both matched by a rule and covered by
`.gitignore`, so it is the only one whose answer says which ran first. Paired
with the existing `.vitest` / `playwright-report` assertions — no rule either
way, `is_ignored` must win — the two halves make the precedence specific instead
of accidental.

### P-5 — the two "already matched, do not retry" rows

Breaks the parent globs, so `docs/backlog` and `tests/core` have nothing to match.

```
$ bash scripts/mutate.sh .claude/harness/paths.conf \
    's#^docs | docs/\*\*$#docs | docsXX#;s#^test | \*\*/tests/\*\*$#test | XXtests#' \
    -- bash scripts/selftest.sh lib

  81 - docs | docs/**
  81 + docs | docsXX
  108 - test | **/tests/**
  108 + test | XXtests

    FAIL a nested docs directory
         expected: docs
         actual:   source
    FAIL a nested tests directory
         expected: test
         actual:   source

lib: 14 failed

=== mutate: command exited 1; restored (verified byte-for-byte against
    .claude/state/mutations/.claude_harness_paths.conf.20260915T023652Z.*.bak) ===
  81: docs | docs/**
  108: test | **/tests/**
```

Still 14 red overall — two new reds in, and the two spelling-agreement assertions
out, because with the globs gone both spellings agree on `source`. Worth noticing
rather than glossing: *a suite can hold its count and mean something different.*

### P-7 — AC-3's two controls, against out-of-scope item 8

If the "fix" were to make GREEN permit `test`, AC-3's four assertions would go
green without a line of `classify` changing. This mutation does exactly that.

```
$ bash scripts/mutate.sh .claude/harness/phases.conf \
    's/^GREEN    | vendor,ignored,source/GREEN    | vendor,ignored,test,source/' \
    -- bash scripts/selftest.sh phase-guard

  16 - GREEN    | vendor,ignored,source,config,docs,harness | ...
  16 + GREEN    | vendor,ignored,test,source,config,docs,harness | ...

  MT-034 AC-3 and AC-3b: GREEN freezes the test tree however the path is spelled
    FAIL blocks: cp into the frozen test tree during GREEN
         not blocked at all
    FAIL blocks: mv into the frozen test tree during GREEN
         not blocked at all
    FAIL blocks: touch of the frozen test tree during GREEN
         not blocked at all
    FAIL blocks: AC-3b: rm -rf of the whole frozen test tree during GREEN
         not blocked at all
    FAIL blocks: cp one level inside the frozen test tree stays refused
         not blocked at all
    FAIL blocks: rm of a named frozen test stays refused
         not blocked at all

phase-guard: 262 passed, 27 failed

=== mutate: command exited 1; restored (verified byte-for-byte against
    .claude/state/mutations/.claude_harness_phases.conf.20260915T024708Z.72223.bak) ===
  16: GREEN    | vendor,ignored,source,config,docs,harness | ...
```

The two **controls** are what matter: `cp docs/notes.md tests/sub/` and
`rm -rf tests/main.test.ts`, green on arrival, go red. So the phases.conf route
cannot buy AC-3 — it breaks the controls in the same block. 27 red overall
(12 baseline + 15), the extra fifteen being MT-033's GREEN/test assertions,
which is the correct and reassuring collateral.

### P-8 — the single-assertion probe, offered to GATES

Post-fix, giving `packaging` a `/**` glob should flip **exactly one** assertion:
`AC-4: classify packaging`. Nothing else in either suite mentions `packaging`
(`grep -rn packaging .claude/tests/` → no matches). RED ran it against the
**unfixed** tree to establish that it is inert without the retry:

```
$ bash scripts/mutate.sh .claude/harness/paths.conf \
    's#^config | packaging/\*\.spec$#config | packaging/**#' -- bash scripts/selftest.sh lib

  134 - config | packaging/*.spec
  134 + config | packaging/**

lib: 14 failed          # unchanged from baseline; `AC-4: classify packaging` is NOT among them

=== mutate: command exited 1; restored (verified byte-for-byte against
    .claude/state/mutations/.claude_harness_paths.conf.20260915T030150Z.148046.bak) ===
  134: config | packaging/*.spec
```

Zero movement — which is the evidence, not the absence of it. The added glob is
only ever consulted through the trailing-slash retry, and in RED there is no
retry. **Predicted post-fix: exactly 1 red,
`AC-4: classify packaging — expected: source / actual: config`.** That is a
prediction and is labelled as one; GATES measures it.

---

### What none of this shows, said plainly

- **DV-1 itself was not run.** P-1 and P-6 are its mirror image against the
  unfixed tree, not DV-1. See `## Deferred verifications`.
- **AC-6's diff was not run.** RED has one side. DV-2 carries the `before` side
  and a prediction; the diff is GATES'.
- **P-8's post-fix count is predicted, not measured.** Pre-fix it is inert, by
  design.

## Handoff: GREEN -> GATES

Written at the end of GREEN, 2026-09-15. Everything below is a local run on this
machine (Windows 11, Git Bash); nothing here was measured on CI hardware.

### 1. Files changed, and why

```
$ git status --porcelain
 M .claude/hooks/lib.sh                 <- GREEN's only production change
 M .claude/tests/lib.test.sh            <- RED's, untouched in GREEN
 M .claude/tests/phase-guard.test.sh    <- RED's, untouched in GREEN
 M docs/backlog/stories/MT-034.md       <- this handoff and ## Gate probes

$ git diff -U0 .claude/hooks/lib.sh | grep -E '^@@'
@@ -703,0 +704,27 @@ command_cwd() {     <- 27 lines of comment above classify
@@ -705 +732 @@ classify() {            <- local rel="$1" cat  gains  probe=""
@@ -707 +734,5 @@ classify() {          <- the one-line lookup becomes five
```

Three hunks, all inside `classify`. `classify_stdin`, `is_ignored`,
`normalize_rel`, `to_rel`, `write_candidates`, `mvops()` and the denial format
are byte-identical. **`.claude/hooks/phase-guard.sh`, `.claude/harness/paths.conf`
and `.claude/harness/phases.conf` were not opened** - C-4 says needing to change
`phase-guard.sh` means the fix has drifted out of `classify`, and it did not.

The change, in full:

```bash
classify() {
  local rel="$1" cat probe=""
  [ -z "$rel" ] && { printf 'outside'; return; }
  case "$rel" in */) ;; *) probe="$rel/" ;; esac
  cat="$(printf '%s\n%s\n' "$rel" "$probe" | classify_stdin | awk -F'\t' '
    NR == 1                  { c = $1 }
    NR == 2 && c == "source" { c = $1 }
    END                      { print c }')"
  [ -z "$cat" ] && cat=source
  [ "$cat" = "source" ] && is_ignored "$rel" && cat=ignored
  printf '%s' "$cat"
}
```

Four things a reviewer should check against C-3 and C-4:

1. **The suffix is `/`**, as C-3 pins and as RED's handoff §6 says no test can
   falsify. I wrote `/`, and re-confirmed RED's observation that `/x` and `/**`
   are indistinguishable against this `paths.conf`.
2. **No extra fork on any path.** Today's `classify` was `printf | classify_stdin
   | awk`; it still is. `classify_stdin` judges both lines in its single `awk`,
   which is C-4's suggested mechanism, and the fact that `classify` already
   delegates to `classify_stdin` is what makes it a two-line change. See §5.
3. **A path already ending in `/` is not re-slashed** - the `case` leaves
   `probe` empty, `printf` then emits a trailing empty line, and `classify_stdin`
   skips it (`if (path == "") next`), so exactly one answer comes back.
   `classify "src/"` -> `source` and `classify "docs/"` -> `docs` notice.
4. **"Did not match a rule" is read off the `source` answer**, which is sound only
   because `paths.conf` has no rule whose *category* is `source`:

```
$ grep -vE '^[[:space:]]*(#|$)' .claude/harness/paths.conf | cut -d'|' -f1 \
    | tr -d ' ' | sort -u
config
docs
harness
test
vendor
```

   So `source` comes only from the default, and "retry the miss" and "retry the
   `source` answer" are the same predicate. **This is the one assumption in the
   fix that a future `source | ...` rule would silently break**, it is called out
   in the comment above `classify` as well as here, and it is the thing to look
   at most sceptically.

### 2. Counts, from real runs

Before, re-run by me at the start of GREEN rather than taken from RED's handoff:

```
$ bash scripts/selftest.sh lib
lib: 134 passed, 14 failed

1 of 1 harness suite(s) FAILED.
```

After, the full suite:

```
$ bash scripts/selftest.sh
boundaries: 26 passed, 0 failed
ci-local: 8 passed, 0 failed
doctor: 7 passed, 0 failed
gate-reminder: 32 passed, 0 failed
gates: 108 passed, 0 failed
lib: 148 passed, 0 failed
mutate: 39 passed, 0 failed
new-story: 22 passed, 0 failed
phase-guard: 289 passed, 0 failed
phase: 24 passed, 0 failed
profiles: 44 passed, 0 failed
settings: 20 passed, 0 failed

12 harness suite(s) passed.
```

`lib: 148`, `phase-guard: 289`, `0 of 12 failed` - the three numbers RED's §2
named as the finish line, exactly. The other ten suites are unmoved from RED's
recorded values. `bash scripts/selftest.sh phase-guard` ran for over nine minutes
on its own and the full run for over ten, as RED warned - both from poll
timestamps rather than a stopwatch, so treat them as "budget for it", not as
measurements.

### 3. C-3's table, re-measured against the shipped fix

All 27 rows, this checkout, real `paths.conf`:

```
classify docs               -> docs
classify docs/              -> docs
classify docs/sub           -> docs
classify docs/backlog       -> docs
classify src                -> source
classify src/               -> source
classify src/mangatl        -> source
classify src/mangatl/ui     -> source
classify packaging          -> source
classify src/brand-new.ts   -> source
classify tests              -> test
classify tests/             -> test
classify tests/core         -> test
classify scripts            -> harness
classify fixtures           -> test
classify .claude            -> harness
classify .github            -> harness
classify src/tests          -> test
classify src/test           -> test
classify src/__tests__      -> test
classify src/spec           -> test
classify dist               -> vendor
classify node_modules       -> vendor
classify .vitest            -> ignored
classify playwright-report  -> ignored
classify spikes             -> ignored
classify (empty)            -> outside
```

Every row matches C-3's `under C-3` column. **Nothing in the Contract was
contradicted by measurement** - not C-3's table, not C-4's no-extra-fork claim
(§5), not the note that `classify` already delegates to `classify_stdin` (§1).

### 4. Negative controls, confirmed against the shipped module

RED's §9 table re-measured **independently of the suite**: a throwaway script in
the scratchpad sources `.claude/tests/_lib.sh` for `make_fixture`/`guard_bash`
only, and prints the path and category itself rather than asserting. That matters
because RED's controls are assertions that *passed on arrival* - "the control
test passes" would have been the weaker check.

```
=== AC-1 controls (must stay BLOCKED) ===
  REVIEW   cp docs/notes.md src/                      BLOCKED  src                source
  REVIEW   cp docs/notes.md src/sub/                  BLOCKED  src/sub            source
=== AC-3 controls (must stay BLOCKED) ===
  GREEN    cp docs/notes.md tests/sub/                BLOCKED  tests/sub          test
  GREEN    rm -rf tests/main.test.ts                  BLOCKED  tests/main.test.ts test
=== AC-4 trap at the guard (must stay BLOCKED on src/source) ===
  RED      rm -rf src/                                BLOCKED  src                source
  RED      touch src/                                 BLOCKED  src                source
  REVIEW   touch src/                                 BLOCKED  src                source
=== direction 3 control (must stay ALLOWED) ===
  REVIEW   cp docs/notes.md /tmp/tmp.P1lfoux58Z/docs/ ALLOWED
=== C-6 rows that must not start refusing ===
  REVIEW   mv docs/notes.md docs/sub/                 ALLOWED
  RED      cp docs/notes.md docs/backlog/             ALLOWED
  RED      rm -rf .vitest                             ALLOWED
  RED      rm -rf node_modules                        ALLOWED
  RED      bash scripts/mutate.sh src/main.ts 's/a/b/' -- true ALLOWED
  RED      git commit -m "move the docs/ fixtures"    ALLOWED
=== the criteria themselves, measured independently of the suite ===
  REVIEW   cp docs/notes.md docs/                     ALLOWED
  REVIEW   mv docs/notes.md docs/                     ALLOWED
  REVIEW   rm -rf docs/                               ALLOWED
  REVIEW   touch docs/                                ALLOWED
  RED      mv src/main.ts docs/                       BLOCKED  src/main.ts        source
  GREEN    cp docs/notes.md tests/                    BLOCKED  tests              test
  GREEN    mv docs/notes.md tests/                    BLOCKED  tests              test
  GREEN    touch tests/                               BLOCKED  tests              test
  GREEN    rm -rf tests/                              BLOCKED  tests              test
```

AC-2's denial, verbatim, with **no change to `mvops()` and no change to any role
string** - RED predicted this falls out of the classification via the guard's
`sort -u` candidate ordering, and it does:

```
BLOCKED by the harness phase lock.    story:    T-1   phase:    RED   path:
src/main.ts   operand:  source of mv (removed by the move)   category: source
Story is in RED. Production code is frozen: ...
```

The unit-level controls:

```
$ printf 'docs\ntests\nscripts\nfixtures\n.claude\n.github\n' | classify_stdin | tr '\t' ' '
source docs
source tests
source scripts
source fixtures
source .claude
source .github
$ classify dist               -> vendor
$ classify .vitest            -> ignored
$ classify playwright-report  -> ignored
$ classify src                -> source
```

DV-4's identity control re-run against the shipped fix, in a throwaway repo with
`paths.conf` copied in and `HARNESS_ROOT`/`HARNESS_DIR` repointed, classified
before and after `mkdir fixtures` plus a file inside it:

```
DV-4 identity, shipped fix:
  classify fixtures   before mkdir = test   after mkdir = test
  classify fixtures/  before mkdir = test   after mkdir = test
  IDENTICAL: yes
```

**Control ledger - every row of RED's §9 against the shipped code.** Five rows
were expected to change value; nothing else moved.

| Control | RED measured | GREEN measured | Verdict |
|---|---|---|---|
| `cp docs/notes.md src/` (REVIEW) | blocked, `src`, `source` | blocked, `src`, `source` | identical |
| `cp docs/notes.md src/sub/` (REVIEW) | blocked, `src/sub`, `source` | blocked, `src/sub`, `source` | identical |
| `cp docs/notes.md tests/sub/` (GREEN) | blocked, `tests/sub`, `test` | blocked, `tests/sub`, `test` | identical |
| `rm -rf tests/main.test.ts` (GREEN) | blocked, `tests/main.test.ts`, `test` | blocked, `tests/main.test.ts`, `test` | identical |
| `classify src` | `source` | `source` | identical |
| `rm -rf src/`, `touch src/` (RED) | blocked, `src`, `source` | blocked, `src`, `source` | identical |
| `classify dist` | `vendor` | `vendor` | identical |
| `classify_stdin` on six bare directory names | all `source` | all `source` | identical |
| `cp docs/notes.md $FIX/docs/` (REVIEW) | allowed | allowed | identical |
| DV-4 `classify fixtures` either side of `mkdir` | `source` == `source` | **`test` == `test`**, still identical | changed by design |
| `classify docs` | `source` | **`docs`** | changed by design |
| `classify tests` | `source` | **`test`** | changed by design |
| `classify scripts` | `source` | **`harness`** | changed by design |
| `classify fixtures` | `source` | **`test`** | changed by design |

**No divergence from RED's recorded values, benign or otherwise.** One sentence on
why that is more credible here than usual: a bash suite has no import to fail, so
every control RED recorded had genuinely executed rather than being an unverified
claim - and the re-measurement above went through the real module rather than
through the suite, which is the check that can tell a passing control from a
control measuring the wrong thing.

### 5. DV-5, measured early because its failure mode is a lock that fails OPEN

DV-5 is GATES' and GATES still owes the official run. But C-4's "the retry must
not add a fork on the common path" is the one constraint with no assertion behind
it, so I measured rather than reasoned. `main`'s `lib.sh` (from
`git show main:.claude/hooks/lib.sh`) against this branch's, interleaved, N=30
calls per rep in one process, three reps each, same tree, same inputs:

```
path           main (ms/call, 3 reps)       branch (ms/call, 3 reps)
src/main.ts     113 108 112                  115 122 109
docs            114 110 138                  55  58  66
spikes           90 100 107                  97 101  98
```

- **`src/main.ts`** - the common path, matches a rule on the bare form: unmoved.
  No fork added, which is what C-4 requires.
- **`docs`** - the miss path: roughly **halved**, and the reason is worth
  recording because it is not luck. On `main`, `docs` fell through to `source` and
  then paid `is_ignored`'s two `git check-ignore` forks. Under C-3 the retry
  answers `docs` from `paths.conf` and `is_ignored` never runs. The retry does not
  cost a fork; on this path it saves two.
- **`spikes`** - matches neither form, still falls through to `is_ignored`:
  unmoved.

No case moved into a different order of magnitude and nothing approaches the 15 s
`PreToolUse` budget. GATES should still run the whole-invocation half DV-5 asks
for; this covers `classify` only.

### 6. What GATES owns

- **DV-1** - the cheapest-wrong-fix mutation against the *finished* fix. Now
  runnable: strip the retry and default an unmatched path to a category RED
  permits. Predicted 6 red in AC-4, 4 red in AC-3, 0 movement in AC-1. **Not run
  in GREEN.**
- **DV-2** - the two-sided AC-6 diff. I ran it; the output is in `## Gate probes`
  and it is zero differing lines. GATES may take it as discharged or re-run it in
  its own shape.
- **DV-3** - the rejected-option control, `paths.conf` twin rules under
  `mutate.sh`. **Not run in GREEN.**
- **DV-5** - the official interleaved A/B plus one whole `PreToolUse` invocation.
  §5 is the `classify` half only.
- **P-8**, RED's §10 pick, predicted post-fix catch of exactly one assertion
  (`AC-4: classify packaging`, `expected: source / actual: config`). **Not run in
  GREEN.** It is live rather than inert now, because it acts *through* the retry.
- The full `bash scripts/gates.sh`. GREEN ran `--fast` only (`## Gate probes`).

### 7. Things to look at sceptically

1. **The "`source` means no rule matched" assumption** (§1 item 4). Sound today
   and measured today; silently wrong the day somebody adds a `source | ...` rule
   to `paths.conf`. No assertion would catch that. The comment above `classify` is
   the only guard, and a reviewer who dislikes that should say so now.
2. **`.claude/tests/**` classifies `harness`, so the phase lock did NOT freeze the
   test files during GREEN.** C-1 says so explicitly and it is the right
   classification for this repository, but it means "I did not edit the tests" is
   discipline here rather than enforcement. What a reviewer can check instead:
   `git diff --stat` shows `+146/-0` and `+171/-0` in the two test files, all of
   it RED's insert, with **no deletions and no modified lines** in either file;
   and `## Test plan`'s promised `+25` and `+21` assertions are the counts that
   ran (`134 -> 148`, `277 -> 289`).
3. **The `docs` speed-up in §5 is a side effect, not a goal.** It exists because
   the retry moves a category decision *ahead* of `.gitignore` for bare directory
   names - which is exactly C-3's pinned ordering, and `classify dist` -> `vendor`
   is the assertion that pins it. Worth a second look anyway: it is the one
   behavioural consequence of the fix that no criterion names.
4. **RED's `docs/backlog/` workaround is still in place** in
   `.claude/tests/phase-guard.test.sh` (~15 call sites and the comment near line
   1153). `## Out of scope` item 5 forbids reverting it here and I did not. It is
   now unnecessary; the follow-up is somebody else's story.
5. **Nothing was escalated and no criterion was amended.** `## Acceptance
   criteria` is byte-identical to `main`; `## Gate results` was not touched by
   hand.

### 8. The model this phase actually ran on

`.claude/agents/feature-developer.md` says `model: opus`. GREEN resolved to
**Opus 5** (`claude-opus-5`), as reported by the running agent itself. **No
override was visible to me**, and that is the honest limit of what GREEN can
attest - an agent cannot see whether its `model:` line won or a session setting
did. The dispatch prompt stated the model comes from the agent definition rather
than from the dispatcher and passed no `model` argument I could observe. The
orchestrator owns the other half of this record; `## Model guidance` is the Lead
PO's section and I did not edit it.

## Gate probes

Written in GREEN, 2026-09-15.

### This story adds no gate and changes no gate command, and that is not the whole answer

`required_gates: []`, no line of `.claude/harness/project.conf` was touched, and
no gate command changed, so there is no new gate to break and watch fail. Stating
only that would be ambiguous, because MT-034 changes `classify` - a function in
`.claude/hooks/lib.sh`, the same file as `classify_stdin`, which
`scripts/check-boundaries.sh:97` and `scripts/gates.sh:621` both reach through,
and which `gate_tree_hash` feeds. **A change there would change what a recorded
gate hash means.** So the probe this story owes is not "break the gate" but
"show that the gate's input did not move", and that is AC-6.

**Callers, re-derived rather than copied from C-7:**

```
$ grep -rn '\bclassify\b' --include=*.sh . | grep -v classify_stdin \
    | grep -v '^\./\.claude/tests/' | grep -v ':#'
./.claude/hooks/phase-guard.sh:42:  cat="$(classify "$rel")"

$ grep -rn 'classify_stdin' --include=*.sh scripts .claude/hooks | grep -v ':#'
scripts/check-boundaries.sh:97:classified=$(printf '%s\n' "$changed" | classify_stdin)
scripts/gates.sh:621:                | sort -u | classify_stdin | awk -F'\t' '$1 == "source" { print $2 }')"
.claude/hooks/lib.sh:735:  cat="$(printf '%s\n%s\n' "$rel" "$probe" | classify_stdin | awk -F'\t' '
.claude/hooks/lib.sh:888:  printf '%s\n' "$kept" | classify_stdin | gated_stdin \
.claude/hooks/lib.sh:934:    printf '%s\n' "$listing" | cut -f2- | classify_stdin | gated_stdin | ...
```

`classify` has exactly one production caller and it is the phase guard, which no
gate runs. Every gate-facing consumer goes through `classify_stdin`, and
`classify_stdin` is byte-identical to `main`.

### AC-6, both sides, measured

`main`'s `lib.sh` extracted with `git show` and sourced from a scratch path, this
branch's sourced in place, over the same `git ls-files` in the same tree:

```
$ git show main:.claude/hooks/lib.sh > "$SP/lib_main.sh"
$ bash -c ". \"$SP/lib_main.sh\"; git ls-files | classify_stdin" > before_main.txt
$ bash -c '. .claude/hooks/lib.sh; git ls-files | classify_stdin' > after_branch.txt
$ diff before_main.txt after_branch.txt
$ echo "exit=$?"
exit=0
DIFF: zero differing lines (174 paths each side)
```

The hash RED recorded for its one-sided `before.txt` reproduces on both sides:

```
$ sha256sum ... | cut -c1-16
  branch: 87fca95b68292e8f
  main:   87fca95b68292e8f
  RED recorded (DV-2 before.txt): 87fca95b68292e8f
```

And the artefact the gate record is actually stamped with does not move either:

```
$ gate_tree_hash   under main's lib.sh:   a8e7375696a1e21708f2b743b17dcf0cd70f32de
$ gate_tree_hash   under branch lib.sh:   a8e7375696a1e21708f2b743b17dcf0cd70f32de
```

**So: no gate's verdict, and no gate's recorded hash, can move because of this
story.** That is the explicit answer, and it is measured rather than argued.

### The gates that did run in GREEN

`bash scripts/gates.sh --fast`, run as soon as the suites went green and before
the full selftest:

```
--- gate summary ---
PASS         format (1s, observed 17)
PASS         lint (0s, observed 5, floor 5)
PASS         typecheck (1s, observed 14, floor 14)
PASS         unit (1s, observed 25, floor 25)
PASS         coverage (1s, observed 25, floor 25)
PASS         coverage-core (1s, observed 20, floor 20)

--fast skipped: integration build mutation
This is a subset, not a verdict. The full run before REVIEW is what judges the story.

(not recorded in the story: a partial run is not evidence of anything)

All required gates passed (6 ran, 0 unconfigured, 0 known).
```

Every gate reports a non-zero `observed` count, so none of them is the "exits 0
in silence with nothing to do" shape. They are green for an uninteresting reason:
`grep -n "^covers" .claude/harness/project.conf` names `src/mangatl/**` and
`packaging/**` and nothing under `.claude/`, so no gate reads a file this story
changed. **The verifier for MT-034 is `bash scripts/selftest.sh`**, which
`.github/workflows/gates.yml` runs as the required step *"Harness self-test"*
ahead of the gates; it went 26 red -> 0 red, and the counts are in
`## Handoff: GREEN -> GATES` §2.

**`## Gate results` below is `scripts/gates.sh`'s to write, and GREEN did not
touch it.** The full run is GATES'.

## Gate results

<!-- gates.sh: written by bash scripts/gates.sh; do not edit or paste by hand -->

    run:    2026-09-15T06:00:50Z
    commit: 35afba6 (working tree had uncommitted changes)
    tree:   a8e7375696a1e21708f2b743b17dcf0cd70f32de
    result: pass (9 ran, 0 unconfigured, 2 known)

    PASS         format (1s, observed 17)
    PASS         lint (0s, observed 5, floor 5)
    PASS         typecheck (0s, observed 14, floor 14)
    PASS         unit (1s, observed 25, floor 25)
    PASS         coverage (1s, observed 25, floor 25)
    PASS         coverage-core (1s, observed 20, floor 20)
    KNOWN        integration (1s, exit 5; tests/integration/ is empty until MT-007; pytest exits 5 with "no tests ran") -> .claude/state/gate-logs/integration.log
    PASS         build (35s)
    KNOWN        mutation (0s, exit 2; mutmut is not installed and not configured until the suite is real; stack.md §6) -> .claude/state/gate-logs/mutation.log

## Notes

**MERGED and closed, 2026-09-15.** PR
[#12](https://github.com/ryanczhang7/manga-translator/pull/12) merged at
2026-09-15T10:10:13Z as `8898f0d`, two commits (`f10d7e0`, `0463488`), both
required checks green on the head commit (`gates` 16 m 53 s, `boundaries` 7 s).
The merge touched exactly the four files this story owns:

```
$ git diff --stat 35afba6 8898f0d
 .claude/hooks/lib.sh              |   35 +-
 .claude/tests/lib.test.sh         |  146 ++++
 .claude/tests/phase-guard.test.sh |  171 ++++
 docs/backlog/stories/MT-034.md    | 1650 ++++++++++++++++++++++++++++++++++++-
 4 files changed, 1988 insertions(+), 14 deletions(-)
```

**Nothing was left owed.** No gate reached REVIEW marked `pending CI`; the
acceptance criteria were never amended, so no `## Amendments` section is due; all
five deferred verifications carry pasted output — DV-4 in RED, DV-1, DV-2, DV-3
and DV-5 at GATES.

**What this story cost against its own sizing.** Sized at PLANNED as *"one cycle:
one function, one file, no signature change, no new state"* — and that held
exactly. The production change is **35 lines of `.claude/hooks/lib.sh`**, of which
27 are comment: `probe=""` added to a `local`, a `case` to skip a path that
already ends in `/`, and a one-line rule lookup becoming five. **46 new
assertions** (`lib` 123 → 148, `phase-guard` 268 → 289). One RED → GREEN cycle,
no return.

**The thing worth carrying out of it.** The defect was found by a *fixture
workaround*, not by a denial anyone reported. MT-033's RED could not assert its
`mv -t` table with `docs/` as the target, substituted `docs/backlog/` in fifteen
places, and wrote thirty lines explaining why. That comment is what made this
story findable — and the workaround was the *correct* local move, because the
alternative was widening MT-033 to cover an unrelated defect mid-cycle.

**So the lesson is not "do not work around"; it is "leave the reason where the
next reader will trip over it."** A silent substitution would have shipped the
same passing suite and buried a live `rm -rf tests/` bypass in GREEN. Two other
findings in this story arrived the same way: direction 2 (the GREEN bypass) fell
out of the Lead PO reading the classification table rather than from any report,
and `rm -rf tests/` specifically was found by the orchestrator testing a verb the
plan had not listed. **Every one of the three symptoms was found by reading or by
probing, and none by a user hitting it.**

**And one negative result worth keeping.** DV-3 set out to show AC-6
distinguishes the chosen design from the rejected `paths.conf` one, and measured
that it does not — both move zero lines, because `git ls-files` never lists a
directory. AC-6 still does its real job (proving the fix moves no recorded hash),
but it is not the discriminator it looked like. Recorded as measured rather than
quietly dropped, because a criterion that cannot fail is worth knowing about
before someone leans on it.

**The first CI run's step timings, read out of the log at REVIEW.** MT-031
PO-18/PO-30 established this check and a story has already been sent back from
REVIEW by it. PR [#12](https://github.com/ryanczhang7/manga-translator/pull/12),
run
[34935159037](https://github.com/ryanczhang7/manga-translator/actions/runs/34935159037),
`windows-latest`, commit on `story/MT-034-directory-shaped-paths-classify-as-sourc`.

```
JOB gates: success, 06:02:00Z -> 06:19:44Z              (17 m 44 s total)
  1. Set up job                    1 s
  2. actions/checkout@v5           9 s
  3. astral-sh/setup-uv@v5         9 s
  4. Harness self-test        13 m 22 s   <- 495 assertions, 289 of them phase-guard
  5. Show configured gates        51 s
  6. Audit the gate manifest      50 s
  7. Run gates                 2 m 15 s
  8. Upload gate logs        skipped      (no gate failed)

JOB boundaries: success                     5 s
```

**Both required checks pass and nothing is near a limit.** No step timeout in
`.github/workflows/gates.yml`, no per-test timeout in any harness suite, and the
GitHub job default is 360 minutes: the job uses **4.9 %** of it and the self-test
step **3.7 %**.

**The trend, continued from MT-033 PO-9 and now with a fourth point.** The
self-test step against `phase-guard` assertion count, all on `windows-latest`:

```
MT-031  6 m 55 s   185 phase-guard assertions
MT-033 12 m 14 s   268        (+45 %)   step +77 %
MT-033 12 m 08 s   268        docs-only rerun, identical code
MT-032 12 m 38 s   268        merge head
MT-034 13 m 22 s   289        (+8 %)    step +6 %
```

**MT-033 PO-9's open question is now answered, and the answer is "not
superlinear".** MT-034 added 21 `phase-guard` assertions (+8 %) and the step grew
6 % — tracking the assertion count almost exactly, and well inside the 9–24 %
run-to-run band MT-032 and MT-033 measured. The 77 % jump at MT-033 was a 45 %
assertion increase plus noise, not a scaling term. **The suite scales roughly
linearly in assertions; it is simply expensive per assertion, because each one
forks the real hook.** If a future story needs it faster, the lever is fewer
forks per assertion, not fewer assertions — and MT-034 itself removed two
`git check-ignore` forks from the miss path, which is the first movement in that
direction.

**Nothing is left owed.** No gate reached REVIEW marked `pending CI` — every
required gate ran and passed locally and on CI. `## Acceptance criteria` were
never amended (`check-boundaries.sh`: *"acceptance criteria unchanged since
origin/main"*), so no `## Amendments` section is due. All five deferred
verifications carry pasted output: DV-4 in RED, DV-1, DV-2, DV-3 and DV-5 at
GATES.

**Sizing: one RED→GREEN cycle, and here is the argument.** The change is one
function, `classify`, in one file, with no signature change and no new state.
Five of the six criteria are a single category table read out of C-3; the sixth
is a diff. The suite it lands in already exists, and the fixture already contains
`docs/`, `tests/`, `src/` and `docs/backlog/`. Nothing is split off except the
follow-ups in `## Out of scope`, each of which is a different mechanism in a
different function.

**Why this was not folded into [[MT-033]].** MT-033's subject is operand
*semantics* — which operands of a command constitute a write. This is *path
categorisation* — what a token means once it has been extracted. Different
function, different file section, and MT-033 had already bounced from GREEN back
to RED once and carried 268 assertions. The orchestrator recorded the decision at
the time (MT-033 `## Regressions`, "R-7's two escalations, reproduced
independently"): *"out of scope for MT-033, and filed… it is the recommended next
story, and it is worth more than the `-t` family."*

**Provenance of every measurement in this file.** The `classify` table and the
`main`-checkout table in `## Context` are the orchestrator's, taken 2026-09-14 and
**not re-derived here** by instruction. The GREEN/`tests/` bypass, the
absolute-versus-relative asymmetry, the `to_rel`/`normalize_rel` pair, C-3's
before/after category table and C-4's latency figures are the Lead PO's own, taken
2026-09-14 at PLANNED against the shipped `paths.conf` and the `_lib.sh` fixture.
The bypass in particular was **derived from the classification table and then
confirmed against the real hook**, because a consequence reasoned from a
measurement is not itself a measurement.
